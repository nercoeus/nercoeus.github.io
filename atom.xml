<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>nercoeus</title>
  
  <subtitle>勤奋、求实、进取、奉献</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://yoursite.com/"/>
  <updated>2019-10-31T15:02:30.080Z</updated>
  <id>http://yoursite.com/</id>
  
  <author>
    <name>nercoeus</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>设计模式-生成器模式</title>
    <link href="http://yoursite.com/2019/10/31/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E7%94%9F%E6%88%90%E5%99%A8%E6%A8%A1%E5%BC%8F/"/>
    <id>http://yoursite.com/2019/10/31/设计模式-生成器模式/</id>
    <published>2019-10-31T12:24:59.000Z</published>
    <updated>2019-10-31T15:02:30.080Z</updated>
    
    <content type="html"><![CDATA[<h1 id="1-生成器模式（Builder-pattern）"><a href="#1-生成器模式（Builder-pattern）" class="headerlink" title="1 生成器模式（Builder pattern）"></a>1 生成器模式（Builder pattern）</h1><p><strong>生成器模式定义</strong>：将一个复杂对象的构建和它的表示进行分离，让你可以一步步的创建对象。并且可以用相同的构建过程来创建不同的表示  </p><a id="more"></a><h1 id="2-目标问题"><a href="#2-目标问题" class="headerlink" title="2 目标问题"></a>2 目标问题</h1><p><img src="/2019/10/31/设计模式-生成器模式/1.png" alt><br>&emsp;&emsp;当你要创建一个复杂对象，有多个小部分祖成。例如房子=四面墙+门+窗子+房顶。一个房子也可以包含一些额外的部件，例如：车库，游泳池，花园等等。如果我们需要创建一个房子，代码的初始化部分会变得过于臃肿，需要多所有额外部件进行设置。可能需要游泳池不需要花园，也可能需要车库不需要泳池，也可能全部都需要。虽然大多数情况泳池，花园都是不必要的。<br>&emsp;&emsp;虽然一个巨大的构造函数消除了对子类的需求，但是它实在是太过于不方便，所以可以使用 Builder pattern 解决这个问题。</p><h1 id="3-解决方法"><a href="#3-解决方法" class="headerlink" title="3 解决方法"></a>3 解决方法</h1><p>&emsp;&emsp;新建一个类用来建造房子，即类似于施工队（GoF：ConcreteBuilder）的作用，将原来对象中用来构建的代码移动到该对象中进行管理。如果要创建对象的某一部分，直接在该管理对象中进行调用即可。这样做也可以让你构建不同表示的对象（eg：石头房子或是木头房子），石头房子用石头施工队，木头房子用木头施工队即可。虽然他们建造的对象相同，但是表示方式完全不同，即虽然都是房子，但是确实不同材质的房子。<br>&emsp;&emsp;为了实现一条流水线的操作，你还可以创建一个监督者（GoF：Director）用来使用 ConcreteBuilder 完成一系列操作，并将结果返回给你。虽然没有监督者客户也可以自己进行监督，即执行一些列的 build_XXX_Part 函数，但是创建一个监督者可以供代码复用，并将客户和 builder 分离开来。也是个不错的方式  </p><h1 id="4-所有类之间的关系"><a href="#4-所有类之间的关系" class="headerlink" title="4 所有类之间的关系"></a>4 所有类之间的关系</h1><p><img src="/2019/10/31/设计模式-生成器模式/2.png" alt></p><ul><li>Builder 接口<ul><li>为创造一个对象（Product）而设计的一系列方法的接口</li></ul></li><li>ConcreteBuilder 类：<ul><li>实现 Builder 的接口来构造 Product 的各个部分</li><li>定义并跟踪它所长创建的表示</li><li>提供一个检索产品的接口（GetProduct）</li></ul></li><li>Director 类：<ul><li>构造一个使用 builder 接口的对象</li></ul></li><li>Product 类：<ul><li>表示被构造的复杂对象。ConcreteBuilder 创建该 Product 的内部表示并定义其 builde 过程</li></ul></li></ul><h1 id="5-代码实现"><a href="#5-代码实现" class="headerlink" title="5 代码实现"></a>5 代码实现</h1><p>&emsp;&emsp;接下来使用 GO 代码来进行 Builder pattern 的实现</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> HouseBuilder <span class="keyword">interface</span> &#123;</span><br><span class="line">buildHouse()</span><br><span class="line">buildSwimmingPool()</span><br><span class="line">buildGarden()</span><br><span class="line">buildGarage()</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 木头房施工队</span></span><br><span class="line"><span class="keyword">type</span> WoodHouseBuilder <span class="keyword">struct</span>&#123;</span><br><span class="line">House</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 木头房子施工流程</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(wb *WoodHouseBuilder)</span><span class="title">buildHouse</span><span class="params">()</span></span>&#123;</span><br><span class="line">wb.Wall = <span class="string">"Wood Wall"</span></span><br><span class="line">wb.Door = <span class="string">"Wood Door"</span></span><br><span class="line">wb.Roof = <span class="string">"Wood Roof"</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(wb *WoodHouseBuilder)</span> <span class="title">buildSwimmingPool</span><span class="params">()</span></span>&#123;</span><br><span class="line">wb.SwimmingPool = <span class="string">"Wood SwimmingPool"</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(wb *WoodHouseBuilder)</span> <span class="title">buildGarden</span><span class="params">()</span></span>&#123;</span><br><span class="line">wb.Garden = <span class="string">"Wood Garden"</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(wb *WoodHouseBuilder)</span> <span class="title">buildGarage</span><span class="params">()</span></span>&#123;</span><br><span class="line">wb.Garage = <span class="string">"Wood Garage"</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 石头房施工队</span></span><br><span class="line"><span class="keyword">type</span> StoneHouseBuilder <span class="keyword">struct</span>&#123;</span><br><span class="line">House</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 石头房子的施工流程</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(sb *StoneHouseBuilder)</span><span class="title">buildHouse</span><span class="params">()</span></span>&#123;</span><br><span class="line">sb.Wall = <span class="string">"Stone Wall"</span></span><br><span class="line">sb.Door = <span class="string">"Stone Door"</span></span><br><span class="line">sb.Roof = <span class="string">"Stone Roof"</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(sb *StoneHouseBuilder)</span> <span class="title">buildSwimmingPool</span><span class="params">()</span></span>&#123;</span><br><span class="line">sb.SwimmingPool = <span class="string">"Stone SwimmingPool"</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(sb *StoneHouseBuilder)</span> <span class="title">buildGarden</span><span class="params">()</span></span>&#123;</span><br><span class="line">sb.Garden = <span class="string">"Stone Garden"</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(sb *StoneHouseBuilder)</span> <span class="title">buildGarage</span><span class="params">()</span></span>&#123;</span><br><span class="line">sb.Garage = <span class="string">"Stone Garage"</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>&emsp;&emsp;实际的 Builder,通过不同的 Builder 可以获取相同对象的不同表示(eg:木头房子和石头房子)</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> House <span class="keyword">struct</span> &#123;</span><br><span class="line">Wall <span class="keyword">string</span></span><br><span class="line">Door <span class="keyword">string</span></span><br><span class="line">Roof <span class="keyword">string</span></span><br><span class="line">SwimmingPool <span class="keyword">string</span></span><br><span class="line">Garden <span class="keyword">string</span></span><br><span class="line">Garage <span class="keyword">string</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>&emsp;&emsp;用户最后获得的 House 对象</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 监督者类</span></span><br><span class="line"><span class="keyword">type</span> Director <span class="keyword">struct</span> &#123;</span><br><span class="line">houseBuilder HouseBuilder  <span class="comment">// 真正的 Builder</span></span><br><span class="line">class <span class="keyword">string</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 构建 House 的基本部分</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(d *Director)</span> <span class="title">makeHouse</span><span class="params">()</span></span>&#123;</span><br><span class="line">d.houseBuilder.buildSwimmingPool()</span><br><span class="line">d.houseBuilder.buildGarage()</span><br><span class="line">d.houseBuilder.buildGarden()</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 设置需要的类型</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(d *Director)</span> <span class="title">SetHouseBuilder</span><span class="params">(t <span class="keyword">string</span>)</span></span>&#123;</span><br><span class="line"><span class="keyword">switch</span> t&#123;</span><br><span class="line"><span class="keyword">case</span> <span class="string">"Wood"</span>:</span><br><span class="line">d.houseBuilder = &amp;WoodHouseBuilder&#123;&#125;</span><br><span class="line"><span class="keyword">case</span> <span class="string">"Stone"</span>:</span><br><span class="line">d.houseBuilder = &amp;StoneHouseBuilder&#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line">d.houseBuilder.buildHouse()</span><br><span class="line">d.class = t</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 获取建好的房子</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(d *Director)</span> <span class="title">GetHouse</span><span class="params">()</span> *<span class="title">House</span></span>&#123;</span><br><span class="line"><span class="keyword">switch</span> d.class&#123;</span><br><span class="line"><span class="keyword">case</span> <span class="string">"Wood"</span>:</span><br><span class="line"><span class="keyword">return</span> &amp;d.houseBuilder.(*WoodHouseBuilder).House</span><br><span class="line"><span class="keyword">case</span> <span class="string">"Stone"</span>:</span><br><span class="line"><span class="keyword">return</span> &amp;d.houseBuilder.(*StoneHouseBuilder).House</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>&emsp;&emsp;上面展示了通过 Director 来控制对象的构建流程,可以将一系列的构建操作封装复用.</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">director := Director&#123;&#125;</span><br><span class="line">director.SetHouseBuilder(<span class="string">"Wood"</span>)</span><br><span class="line">director.makeHouse()</span><br><span class="line">house := director.GetHouse()</span><br><span class="line">fmt.Println(<span class="string">"------------------------"</span>)</span><br><span class="line">fmt.Println(house.Wall)</span><br><span class="line">fmt.Println(house.Door)</span><br><span class="line">fmt.Println(house.Roof)</span><br><span class="line">fmt.Println(house.SwimmingPool)</span><br><span class="line">fmt.Println(house.Garage)</span><br><span class="line">fmt.Println(house.Garden)</span><br><span class="line">fmt.Println(<span class="string">"------------------------"</span>)</span><br><span class="line">director.SetHouseBuilder(<span class="string">"Stone"</span>)</span><br><span class="line">director.makeHouse()</span><br><span class="line">house = director.GetHouse()</span><br><span class="line">fmt.Println(house.Wall)</span><br><span class="line">fmt.Println(house.Door)</span><br><span class="line">fmt.Println(house.Roof)</span><br><span class="line">fmt.Println(house.SwimmingPool)</span><br><span class="line">fmt.Println(house.Garage)</span><br><span class="line">fmt.Println(house.Garden)</span><br><span class="line">fmt.Println(<span class="string">"------------------------"</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>&emsp;&emsp;客户执行逻辑如上,新建一个 Director 再通知它你需要什么样的房子,之后交给它进行构建,最后直接获取即可.运行结果如下:<br><img src="/2019/10/31/设计模式-生成器模式/3.png" alt></p><h1 id="6-应用场景"><a href="#6-应用场景" class="headerlink" title="6 应用场景"></a>6 应用场景</h1><p>以下情况可以使用<strong>生成器模式</strong>：</p><ul><li>当创建复杂对象的算法应该独立于该对象的组成部分以及它们的装配方式时。</li><li>当构造过程必须允许被构造的对象有不同而表示时。</li><li>使用 Builder 来构造复合对象或其他的复杂对象时。</li></ul><h1 id="7-优缺点"><a href="#7-优缺点" class="headerlink" title="7 优缺点"></a>7 优缺点</h1><h2 id="7-1-优点"><a href="#7-1-优点" class="headerlink" title="7.1 优点"></a>7.1 优点</h2><ul><li>可以逐步构造对象，推迟构造过程或递归运行构造过程。即对构造过程提供更加精确的控制</li><li>可以改变一个 Product 的内部表示，即使用相同的构造代码获取不同表示的 Product</li><li>将构造代码和 Product 的业务逻辑代码进行隔离</li></ul><h2 id="7-2-缺点"><a href="#7-2-缺点" class="headerlink" title="7.2 缺点"></a>7.2 缺点</h2><ul><li>会让代码变得复杂：因为增加了许多接口以及新的类（貌似大多数模式都有这个😓。。。）</li></ul><h1 id="8-相关模式"><a href="#8-相关模式" class="headerlink" title="8 相关模式"></a>8 相关模式</h1><ul><li>很明显可以看出和 Factory 系列的模式类似，区别仅仅是，Builder 注重构造单个对象的过程，而 Factory 注重构造不同系列的对象。</li><li>Builder 也可以实现为单例模式。</li><li>创建复杂的 Composite 通常使用 Builder 进行递归创建。</li></ul><h1 id="9-reference"><a href="#9-reference" class="headerlink" title="9 reference"></a>9 reference</h1><p>《设计模式》-生成器模式<br><a href="https://refactoring.guru/design-patterns/builder" target="_blank" rel="noopener">design-patterns:Builder</a><br><a href="https://zh.wikipedia.org/wiki/%E7%94%9F%E6%88%90%E5%99%A8%E6%A8%A1%E5%BC%8F" target="_blank" rel="noopener">中文维基：生成器模式</a>  </p>]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;1-生成器模式（Builder-pattern）&quot;&gt;&lt;a href=&quot;#1-生成器模式（Builder-pattern）&quot; class=&quot;headerlink&quot; title=&quot;1 生成器模式（Builder pattern）&quot;&gt;&lt;/a&gt;1 生成器模式（Builder pattern）&lt;/h1&gt;&lt;p&gt;&lt;strong&gt;生成器模式定义&lt;/strong&gt;：将一个复杂对象的构建和它的表示进行分离，让你可以一步步的创建对象。并且可以用相同的构建过程来创建不同的表示  &lt;/p&gt;
    
    </summary>
    
    
      <category term="设计模式" scheme="http://yoursite.com/categories/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
    
  </entry>
  
  <entry>
    <title>设计模式-抽象工厂</title>
    <link href="http://yoursite.com/2019/10/31/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E6%8A%BD%E8%B1%A1%E5%B7%A5%E5%8E%82/"/>
    <id>http://yoursite.com/2019/10/31/设计模式-抽象工厂/</id>
    <published>2019-10-31T12:24:43.000Z</published>
    <updated>2019-10-31T13:59:13.407Z</updated>
    
    <content type="html"><![CDATA[<h1 id="1-抽象工厂模式（Abstract-factory-pattern）"><a href="#1-抽象工厂模式（Abstract-factory-pattern）" class="headerlink" title="1 抽象工厂模式（Abstract factory pattern）"></a>1 抽象工厂模式（Abstract factory pattern）</h1><p><strong>抽象工厂模式定义</strong>：提供接口，创建一系列相关或独立的对象，而不指定这些对象的具体类<br>&emsp;&emsp;这里强调了抽象工厂用来创建的是一系列的对象，当一个项目需要提供不同的系列的相同对象并且每个系列之间没有关联时，使用抽象工厂可以对构建一个总接口作为所有系列的共同父亲，用父类来统一创建所有对象。这样就不需要根据不同的系列硬编码到自己的工程中，只需要在总接口处进行系列的转换即可。</p><a id="more"></a><h1 id="2-目标问题"><a href="#2-目标问题" class="headerlink" title="2 目标问题"></a>2 目标问题</h1><p><img src="/2019/10/31/设计模式-抽象工厂/1.png" alt="1"></p><p>存在一个情景：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">你需要生产一系列的家具：椅子，沙发，桌子  </span><br><span class="line">分为不同形状的 3 个系列：圆形，矩形，三角形。</span><br></pre></td></tr></table></figure><p>&emsp;&emsp;如果单纯的进行生产，就需要 9 个工厂，分别是 圆形+椅子，圆形+沙发……三角形+桌子。并且在客户需要家具的时候还需要去不同的工厂进行取货。但这样不仅很繁琐，并且用户一不注意可能会拿到不是同一个系列的家具。当需要添加新的产品或系列的时候，就会导致你的核心代码会频繁的进行修改。  </p><h1 id="3-解决方法"><a href="#3-解决方法" class="headerlink" title="3 解决方法"></a>3 解决方法</h1><p>&emsp;&emsp;对每种家具构建一个接口，例如 Chair 接口，所有不同的椅子都实现这个统一的接口进行创建，所有的桌子都实现 Desktop 接口进行创建。<br>&emsp;&emsp;之后建造一个大工厂用来生产一套家具，包含所有产品（椅子，沙发，桌子）的总接口，可以让所有的系列工厂都实现这一系列接口。比如抽象工厂有 CreateChair 接口，所有系列的大工厂均有 CreateChair 接口，沙发，桌子依次类推。<br>&emsp;&emsp;现在，当客户想要购买家具时，仅需要到他需要的工厂（他喜欢系列的工厂），依次发出 CreateChair,CreateDesktop,CreateSofa 指令即可，它不需要关系这些家具是什么系列，因为他在需要购买家具选择系列的时候就进入了对应的工厂。所以客户在购买家具时仅仅需要关系的就是他需要的最基础的类型，是椅子还是桌子等。</p><h1 id="4-所有类之间的关系"><a href="#4-所有类之间的关系" class="headerlink" title="4 所有类之间的关系"></a>4 所有类之间的关系</h1><p><img src="/2019/10/31/设计模式-抽象工厂/2.png" alt="2"></p><ol><li>抽象产品(AbstractProduct):为一类产品对象声明一个接口(eg:椅子)</li><li>具体产品(ConcreteProduct):创建一个具体产品的对象(eg:圆形椅子)</li><li>抽象工厂(AbstractFactory):一个抽象工厂的接口,用来创建抽象接口</li><li>具体工厂(ConcreteFactory):用来生产一个系列的各种对象</li><li>客户(Client):获取特定的 ConcreteFactory 来创建同一系列的 Product.仅仅使用 AbstractFactory 定义的接口</li></ol><h1 id="5-代码实现"><a href="#5-代码实现" class="headerlink" title="5 代码实现"></a>5 代码实现</h1><p>&emsp;&emsp;最近一直使用 GO 语言，暂时的设计模式实现也使用 Go 来进行实现，因为 GO 语言不是完全面向对象语言，没有类似 C++ 中的类，所以使用 struct，interface 来实现设计模式的代码：</p><h2 id="5-1-对象的设计"><a href="#5-1-对象的设计" class="headerlink" title="5.1 对象的设计"></a>5.1 对象的设计</h2><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 具体的不同家具的 struct</span></span><br><span class="line"><span class="keyword">type</span> Chair <span class="keyword">struct</span> &#123;   <span class="comment">// Chair</span></span><br><span class="line">class <span class="keyword">string</span></span><br><span class="line">name  <span class="keyword">string</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">type</span> Sofa <span class="keyword">struct</span> &#123;    <span class="comment">// Sofa</span></span><br><span class="line">class <span class="keyword">string</span></span><br><span class="line">name  <span class="keyword">string</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">type</span> Table <span class="keyword">struct</span> &#123;   <span class="comment">// Table</span></span><br><span class="line">class <span class="keyword">string</span></span><br><span class="line">name  <span class="keyword">string</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 圆形家具工厂</span></span><br><span class="line"><span class="keyword">type</span> CircleFurnitureFactory <span class="keyword">struct</span> &#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 矩形家具工厂</span></span><br><span class="line"><span class="keyword">type</span> RectangleFurnitureFactory <span class="keyword">struct</span> &#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 三角形家具工厂</span></span><br><span class="line"><span class="keyword">type</span> TriangleFurnitureFactory <span class="keyword">struct</span> &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>&emsp;&emsp;依次设计了不同家具的 struct，这里为了不让代码太过冗长，将 Chair，Sofa，Table 都使用了一种 struct 进行表示，这里也可以将其拆分，并创建 Chair interface 等等。<br>&emsp;&emsp;还包括对应的不同系列的工厂的 struct，客户根据系统配置自行选择需要使用的对象即可</p><h2 id="5-2-接口的设计"><a href="#5-2-接口的设计" class="headerlink" title="5.2 接口的设计"></a>5.2 接口的设计</h2><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 抽象工厂类</span></span><br><span class="line"><span class="keyword">type</span> AbstractFurnitureFactory <span class="keyword">interface</span> &#123;</span><br><span class="line">MakeChair() Chair</span><br><span class="line">MakeSofa() Sofa</span><br><span class="line">MakeTable() Table</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 不同工厂的家具生产过程，这里仅展示一个系列的代码</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(a CircleFurnitureFactory)</span> <span class="title">MakeChair</span><span class="params">()</span> <span class="title">Chair</span></span> &#123;</span><br><span class="line"><span class="keyword">return</span> Chair&#123;</span><br><span class="line">class: <span class="string">"Circle"</span>,</span><br><span class="line">name:  <span class="string">"Chair"</span>,</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(a CircleFurnitureFactory)</span> <span class="title">MakeSofa</span><span class="params">()</span> <span class="title">Sofa</span></span> &#123;</span><br><span class="line"><span class="keyword">return</span> Sofa&#123;</span><br><span class="line">class: <span class="string">"Circle"</span>,</span><br><span class="line">name:  <span class="string">"Sofa"</span>,</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(a CircleFurnitureFactory)</span> <span class="title">MakeTable</span><span class="params">()</span> <span class="title">Table</span></span> &#123;</span><br><span class="line"><span class="keyword">return</span> Table&#123;</span><br><span class="line">class: <span class="string">"Circle"</span>,</span><br><span class="line">name:  <span class="string">"Table"</span>,</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>&emsp;&emsp;实现了抽象工厂接口，提供了三个接口用来生产不同的家具，之后所有工厂类依次实现该接口的所有方法即可进行统一的调用  </p><h2 id="5-3-代码执行"><a href="#5-3-代码执行" class="headerlink" title="5.3 代码执行"></a>5.3 代码执行</h2><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 选择自己需要的系列</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">getFurnitureFactor</span><span class="params">(class <span class="keyword">string</span>)</span> <span class="title">AbstractFurnitureFactory</span></span> &#123;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> res AbstractFurnitureFactory</span><br><span class="line"><span class="keyword">switch</span> class &#123;</span><br><span class="line"><span class="keyword">case</span> <span class="string">"Circle"</span>:</span><br><span class="line">res = CircleFurnitureFactory&#123;&#125;</span><br><span class="line"><span class="keyword">return</span> res</span><br><span class="line"><span class="keyword">case</span> <span class="string">"Rectangle"</span>:</span><br><span class="line">res = RectangleFurnitureFactory&#123;&#125;</span><br><span class="line"><span class="keyword">return</span> res</span><br><span class="line"><span class="keyword">case</span> <span class="string">"Triangle"</span>:</span><br><span class="line">res = TriangleFurnitureFactory&#123;&#125;</span><br><span class="line"><span class="keyword">return</span> res</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 主函数的逻辑</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">a := getFurnitureFactor(<span class="string">"Circle"</span>)</span><br><span class="line">b := getFurnitureFactor(<span class="string">"Rectangle"</span>)</span><br><span class="line">c := getFurnitureFactor(<span class="string">"Triangle"</span>)</span><br><span class="line">fmt.Println(a.MakeChair())</span><br><span class="line">fmt.Println(a.MakeSofa())</span><br><span class="line">fmt.Println(a.MakeTable())</span><br><span class="line">fmt.Println(b.MakeChair())</span><br><span class="line">fmt.Println(b.MakeSofa())</span><br><span class="line">fmt.Println(b.MakeTable())</span><br><span class="line">fmt.Println(c.MakeChair())</span><br><span class="line">fmt.Println(c.MakeSofa())</span><br><span class="line">fmt.Println(c.MakeTable())</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>运行结果如下：<br><img src="/2019/10/31/设计模式-抽象工厂/export.png" alt="export"></p><h1 id="6-应用场景"><a href="#6-应用场景" class="headerlink" title="6 应用场景"></a>6 应用场景</h1><p>以下情况可以使用<strong>抽象工厂模式</strong>：</p><ul><li>一个系统要独立于它的产品的创建、组合和表示</li><li>一个系统要由多个产品系列中的一个来进行配置</li><li>要强调一系列相关的产品对象的设计以便进行联合使用</li><li>提供一个产品类库，但只想显示接口而不是实现</li></ul><h1 id="7-优缺点"><a href="#7-优缺点" class="headerlink" title="7 优缺点"></a>7 优缺点</h1><h2 id="7-1-优点"><a href="#7-1-优点" class="headerlink" title="7.1 优点"></a>7.1 优点</h2><ul><li>它分离了具体的类：具体对象的生成被接口相对于客户隔离了开来，客户不需要关心它真是的类名等等</li><li>易于交换产品系列：只需要在初始化时进行不同系列的选择即可，不需要更改过多的客户代码，可以让用户很方便的从一个系列转换为另一个系列</li><li>有利于产品的一致性：当一个系列中的产品对象被设计成一起工作时，一个应用一次只能使用同一系列的对象</li></ul><h2 id="7-2-缺点"><a href="#7-2-缺点" class="headerlink" title="7.2 缺点"></a>7.2 缺点</h2><ul><li>会让代码变得复杂：因为增加了许多接口以及新的类</li><li>当需要添加新的系列时，需要对所有的已有接口进行匹配</li><li>并且当需要添加一个新的种类时（eg：床），需要对所有工厂进行修改，以支持这种新产品</li></ul><h1 id="8-相关模式"><a href="#8-相关模式" class="headerlink" title="8 相关模式"></a>8 相关模式</h1><ul><li>抽象工厂一般基于工厂方法，也可以用原型模式（prototype）实现</li><li>一个具体的工厂通常是单例模式（Singletion）的</li></ul><h1 id="9-reference"><a href="#9-reference" class="headerlink" title="9 reference"></a>9 reference</h1><ol><li>《设计模式》-抽象工厂  </li><li><a href="https://refactoring.guru/design-patterns/abstract-factory" target="_blank" rel="noopener">design-patterns:Abstract Factory</a>  </li><li><a href="https://zh.wikipedia.org/wiki/%E6%8A%BD%E8%B1%A1%E5%B7%A5%E5%8E%82" target="_blank" rel="noopener">中文维基：抽象工厂</a>  </li><li><a href="https://blog.csdn.net/wangshubo1989/article/details/79270450" target="_blank" rel="noopener">Go实战–Design Patterns in Golang 之工厂模式(简单工厂、工厂方法、抽象工厂)</a>  </li></ol>]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;1-抽象工厂模式（Abstract-factory-pattern）&quot;&gt;&lt;a href=&quot;#1-抽象工厂模式（Abstract-factory-pattern）&quot; class=&quot;headerlink&quot; title=&quot;1 抽象工厂模式（Abstract factory pattern）&quot;&gt;&lt;/a&gt;1 抽象工厂模式（Abstract factory pattern）&lt;/h1&gt;&lt;p&gt;&lt;strong&gt;抽象工厂模式定义&lt;/strong&gt;：提供接口，创建一系列相关或独立的对象，而不指定这些对象的具体类&lt;br&gt;&amp;emsp;&amp;emsp;这里强调了抽象工厂用来创建的是一系列的对象，当一个项目需要提供不同的系列的相同对象并且每个系列之间没有关联时，使用抽象工厂可以对构建一个总接口作为所有系列的共同父亲，用父类来统一创建所有对象。这样就不需要根据不同的系列硬编码到自己的工程中，只需要在总接口处进行系列的转换即可。&lt;/p&gt;
    
    </summary>
    
    
      <category term="设计模式" scheme="http://yoursite.com/categories/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
    
  </entry>
  
  <entry>
    <title>设计模式-简介</title>
    <link href="http://yoursite.com/2019/10/30/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E5%BC%80%E5%A7%8B/"/>
    <id>http://yoursite.com/2019/10/30/设计模式-开始/</id>
    <published>2019-10-30T12:52:34.000Z</published>
    <updated>2019-10-31T06:13:22.797Z</updated>
    
    <content type="html"><![CDATA[<p>&emsp;&emsp;最近有些空闲,准备新学习一下设计模式,开一个坑,预计年底之前完成,追求高质量,争取学到一些设计模式精髓.有一些小编写规则,按照&lt;设计模式&gt;一书的顺序进行总结总结,并按照博客的格式进行编写.</p><a id="more"></a><h1 id="什么是设计模式"><a href="#什么是设计模式" class="headerlink" title="什么是设计模式"></a>什么是设计模式</h1><p>&emsp;&emsp;<a href="https://zh.wikipedia.org/wiki/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F_(%E8%AE%A1%E7%AE%97%E6%9C%BA)" target="_blank" rel="noopener">设计模式</a>（design pattern）是对软件设计中普遍存在（反复出现）的各种问题，所提出的解决方案.<br>设计模式并不是教你如何编写代码，而是在描述各种不同的情况下，要怎样解决问题的方式<br>每一个模式描述了一个在我们周围不断重复发生的问题，以及该问题的核心解决办法   —–Christopher Alexander  </p><h1 id="设计模式的种类"><a href="#设计模式的种类" class="headerlink" title="设计模式的种类"></a>设计模式的种类</h1><p>本书中给出了 23 种设计模式：</p><table><thead><tr><th>name</th><th>describe</th></tr></thead><tbody><tr><td>Abstract Factory</td><td>提供一个创建一系列相关或相互依赖对象的接口，而无需制定具体的类</td></tr><tr><td>Adapter Pattern</td><td>将一个类的接口转换成客户希望的另一个接口。使得原本接口不兼容的类可以一起工作</td></tr><tr><td>Bridge Pattern</td><td>将一个抽象与实现解耦，以便两者可以独立的变化</td></tr><tr><td>Builder Pattern</td><td>将一个复杂对象的构建与它的表示分离，使得同样的构建过程可以创建不同的表示</td></tr><tr><td>Chain-of-responsibility pattern</td><td>为解除请求的发送者和接收者之间耦合，而使多个对象都有机会处理这个请求。将这些对象连成一条链，并沿着这条链传递该请求，直到有一个对象处理它</td></tr><tr><td>Command pattern</td><td>将一个请求封装为一个对象，从而使你可用不同的请求对客户进行参数化；对请求排队或记录请求日志，以及支持可取消的操作</td></tr><tr><td>Composite pattern</td><td>把多个对象组成树状结构来表示局部与整体，这样用户可以一样的对待单个对象和对象的组合</td></tr><tr><td>Decorator pattern</td><td>向某个对象动态地添加更多的功能。修饰模式是除类继承外另一种扩展功能的方法</td></tr><tr><td>Façade pattern</td><td>为子系统中的一组接口提供一个一致的界面， 外观模式定义了一个高层接口，这个接口使得这一子系统更加容易使用</td></tr><tr><td>Factory Method pattern</td><td>定义一个接口用于创建对象，但是让子类决定初始化哪个类。工厂方法把一个类的初始化下放到子类</td></tr><tr><td>Flyweight pattern</td><td>通过共享以便有效的支持大量小颗粒对象</td></tr><tr><td>Interpreter pattern</td><td>给定一个语言, 定义它的文法的一种表示，并定义一个解释器, 该解释器使用该表示来解释语言中的句子</td></tr><tr><td>Iterator pattern</td><td>提供一种方法顺序访问一个聚合对象中各个元素, 而又不需暴露该对象的内部表示</td></tr><tr><td>Mediator pattern</td><td>包装了一系列对象相互作用的方式，使得这些对象不必相互明显作用，从而使它们可以松散偶合。当某些对象之间的作用发生改变时，不会立即影响其他的一些对象之间的作用，保证这些作用可以彼此独立的变化</td></tr><tr><td>Memento pattern</td><td>回忆对象是一个用来存储另外一个对象内部状态的快照的对象。回忆模式的用意是在不破坏封装的条件下，将一个对象的状态捉住，并外部化，存储起来，从而可以在将来合适的时候把这个对象还原到存储起来的状态</td></tr><tr><td>Observer pattern</td><td>在对象间定义一个一对多的联系性，由此当一个对象改变了状态，所有其他相关的对象会被通知并且自动刷新</td></tr><tr><td>Prototype pattern</td><td>用原型实例指定创建对象的种类，并且通过拷贝这些原型,创建新的对象</td></tr><tr><td>Proxy pattern</td><td>为其他对象提供一个代理以控制对这个对象的访问</td></tr><tr><td>Singleton pattern</td><td>确保一个类只有一个实例，并提供对该实例的全局访问</td></tr><tr><td>State pattern</td><td>让一个对象在其内部状态改变的时候，其行为也随之改变。状态模式需要对每一个系统可能获取的状态创立一个状态类的子类。当系统的状态变化时，系统便改变所选的子类</td></tr><tr><td>Strategy pattern</td><td>定义一个算法的系列，将其各个分装，并且使他们有交互性。策略模式使得算法在用户使用的时候能独立的改变</td></tr><tr><td>Template method pattern</td><td>模板方法模式准备一个抽象类，将部分逻辑以具体方法及具体构造子类的形式实现，然后声明一些抽象方法来迫使子类实现剩余的逻辑。不同的子类可以以不同的方式实现这些抽象方法，从而对剩余的逻辑有不同的实现。先构建一个顶级逻辑框架，而将逻辑的细节留给具体的子类去实现</td></tr><tr><td>Visitor</td><td>封装一些施加于某种数据结构元素之上的操作。一旦这些操作需要修改，接受这个操作的数据结构可以保持不变。访问者模式适用于数据结构相对未定的系统，它把数据结构和作用于结构上的操作之间的耦合解脱开，使得操作集合可以相对自由的演化</td></tr></tbody></table><h1 id="设计模式的使用方法"><a href="#设计模式的使用方法" class="headerlink" title="设计模式的使用方法"></a>设计模式的使用方法</h1><p>&emsp;&emsp;书第一章的后面推荐了一种使用设计模式的方法：</p><ol><li><strong>大致浏览一遍模式</strong>：特别注意它的适用性部分和效果部分，确定适合你需要解决的问题</li><li><strong>研究其结构部分，参与者部分和协作部分</strong>：雀稗你理解这个设计模式的类和对象以及它们之间是怎么联系的</li><li><strong>看代码实例，来掌握它的实际使用</strong>：有助于你实现它</li><li><strong>选择模式参与者的名字，使它们在应用的上下文之间有意义</strong>：不要直接使用设计模式的名字，因为它过于抽象，要根据你自己的工程上下文设计有意义的名字，可以和设计模式的名字进行组合</li><li><strong>定义类</strong>：根据设计模式的类和对象结构，将其融入到你自己的工程中</li><li><strong>定义模式中专用于应用的操作名称</strong>：根据上一步定义的类，来定义其对应的操作</li><li><strong>实现执行模式中责任和写作的操作</strong>：实现部分提供线索指导你进行实现</li></ol><h1 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h1><p>&emsp;&emsp;下面开始介绍书中的设计模式。。。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&amp;emsp;&amp;emsp;最近有些空闲,准备新学习一下设计模式,开一个坑,预计年底之前完成,追求高质量,争取学到一些设计模式精髓.有一些小编写规则,按照&amp;lt;设计模式&amp;gt;一书的顺序进行总结总结,并按照博客的格式进行编写.&lt;/p&gt;
    
    </summary>
    
    
      <category term="设计模式" scheme="http://yoursite.com/categories/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
    
  </entry>
  
  <entry>
    <title>第25章：信号驱动式IO</title>
    <link href="http://yoursite.com/2019/09/25/25.%E4%BF%A1%E5%8F%B7%E9%A9%B1%E5%8A%A8%E5%BC%8FIO/"/>
    <id>http://yoursite.com/2019/09/25/25.信号驱动式IO/</id>
    <published>2019-09-25T14:54:48.822Z</published>
    <updated>2019-09-28T08:46:38.259Z</updated>
    
    <content type="html"><![CDATA[<h1 id="25-1-概述"><a href="#25-1-概述" class="headerlink" title="25.1 概述"></a>25.1 概述</h1><p>&emsp;&emsp;信号驱动式 I/O 是指进程预先告知内核，使得当某个描述符上发生某事时，内核使用信号通知相关进程。</p><a id="more"></a><h1 id="25-2-套接字的信号驱动式-I-O"><a href="#25-2-套接字的信号驱动式-I-O" class="headerlink" title="25.2 套接字的信号驱动式 I/O"></a>25.2 套接字的信号驱动式 I/O</h1><p>&emsp;&emsp;针对一个套接字使用信号驱动式I/O，要求进程执行以下3个步骤：</p><ol><li><p>建立SIGIO信号处理函数   </p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">signal(SIGIO, sig_io);</span><br></pre></td></tr></table></figure></li><li><p>设置该套接字的属主进程，通常使用fcntl的F_SETOWN命令设置  </p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">fcntl(sockfd, F_SETOWN, getpid());</span><br></pre></td></tr></table></figure></li><li><p>开启该套接字的信号驱动式I/O，通常通过使用fcntl的F_SETFL命令打开O_ASYNC标志  </p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> on = <span class="number">1</span>;  </span><br><span class="line">ioctl(sockfd,  O_ASYNC,  &amp;on);</span><br></pre></td></tr></table></figure></li></ol><p><strong>&emsp;&emsp;尽管很容易把一个套接字设置成以信号驱动式 I/O 模式工作，确定那些条件导致内核产生递交给套接字属主的 SIGIO 信号却不是简单的事情。这种判断取决于支撑协议</strong></p><h2 id="对于-UDP-套接字的-SIGIO-信号"><a href="#对于-UDP-套接字的-SIGIO-信号" class="headerlink" title="对于 UDP 套接字的 SIGIO 信号"></a>对于 UDP 套接字的 SIGIO 信号</h2><p>&emsp;&emsp;在UDP上使用信号驱动式 I/O 比较简单的。SIGIO 信号在发生以下事件时产生</p><ul><li>数据报到达套接字</li><li>套接字上产生异步错误  </li></ul><p>&emsp;&emsp;因此当捕获对于某个 UDP 套接字上的 SIGIO 信号时，我们调用 recvfrom 等读入到达的数据报，或者获取发生的异步错误。UDP 异步错误的前提是 UDP 套接字已经连接</p><h2 id="对于-TCP-套接字的-SIGIO-信号"><a href="#对于-TCP-套接字的-SIGIO-信号" class="headerlink" title="对于 TCP 套接字的 SIGIO 信号"></a>对于 TCP 套接字的 SIGIO 信号</h2><p>&emsp;&emsp;总的来说，信号驱动式 I/O 对于 TCP 套接字近乎无用，因为该信号产生得过于频繁，仅通过 SIGIO 信号不能区分具体是哪种事件</p><ul><li>监听套接字上某个连接请求请求已经完成</li><li>某个断连接请求已经发起</li><li>某个断连接请求已经完成</li><li>某个连接之半已经关闭</li><li>数据到达套接字</li><li>数据已经从套接字发送走(即输出缓冲区有空闲空间)</li><li>发生某个异步错误</li></ul><p>&emsp;&emsp;从上面看出，我们只能对监听套接字使用 SIGIO，因为对于监听套接字产生 SIGIO 的唯一条件一定是某个新连接的完成。</p><h1 id="25-3-使用-sigio-的-udp-回射服务器程序"><a href="#25-3-使用-sigio-的-udp-回射服务器程序" class="headerlink" title="25.3 使用 sigio 的 udp 回射服务器程序"></a>25.3 使用 sigio 的 udp 回射服务器程序</h1><p>&emsp;&emsp;书上给出了一个使用 SIGIO 信号的 UDP 回射服务器，使用了信号驱动 I/O。可以进行参考，实例比较简单</p><h1 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h1><p>&emsp;&emsp;信号驱动式 I/O 就是让内核在套接字上发生“某事”时使用 SIGIO 信号通知进程</p><ul><li>对于已连接 TCP 套接字，可以导致的事件众多，使得这个特性基本无用</li><li>对于监听 TCP 套接字，这种通知发生在有一个新连接已经准备好接收时</li><li>对于 UDP 套接字，这种通知通常意味着到达一个数据报，或者是一个异步的错误，这两种情况我们都是用 recvfrom 进行处理</li></ul>]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;25-1-概述&quot;&gt;&lt;a href=&quot;#25-1-概述&quot; class=&quot;headerlink&quot; title=&quot;25.1 概述&quot;&gt;&lt;/a&gt;25.1 概述&lt;/h1&gt;&lt;p&gt;&amp;emsp;&amp;emsp;信号驱动式 I/O 是指进程预先告知内核，使得当某个描述符上发生某事时，内核使用信号通知相关进程。&lt;/p&gt;
    
    </summary>
    
    
      <category term="UNP" scheme="http://yoursite.com/categories/UNP/"/>
    
    
  </entry>
  
  <entry>
    <title>第27章：IP选项</title>
    <link href="http://yoursite.com/2019/09/25/27.IP%E9%80%89%E9%A1%B9/"/>
    <id>http://yoursite.com/2019/09/25/27.IP选项/</id>
    <published>2019-09-25T14:54:48.822Z</published>
    <updated>2019-09-28T08:46:29.909Z</updated>
    
    <content type="html"><![CDATA[<h1 id="27-1-概述"><a href="#27-1-概述" class="headerlink" title="27.1 概述"></a>27.1 概述</h1><p>&emsp;&emsp;IPv4 允许在 20 字节首部固定部分之后跟最多 40 个字节的选项。尽管已经定义的 IPv4 选项共有 10 种，常用的确实源路径选项。这些选项访问途径是存取 IP_OPTIONS 套接字选项。<br>&emsp;&emsp;IPv6 允许在固定长度的 40 字节 IPv6 首部和传输层首部之间出现扩展首部。目前定义了 6 种不同的扩展首部。不同于 Ipv4 的是，IPv6 扩展首部的访问途径是函数接口。</p><a id="more"></a><h1 id="27-2-ipv4-选项"><a href="#27-2-ipv4-选项" class="headerlink" title="27.2 ipv4 选项"></a>27.2 ipv4 选项</h1><p>IP首部 4 位的头部长度把 IPv4 的首部总长度限制在了 60 字节，因此 IPv4 选项字段最长位 40 字节，IPv4 定义了 10 种选项（其中作废的 3 种不列举）：</p><ol><li>NOP：单字节选项，典型的用途是为某个后续选项落在 4 字节边界上进行填充</li><li>EOL：单字节选项，终止选项的处理</li><li>LSRR</li><li>SSRR</li><li>Timestamp</li><li>Record route</li><li>Router alert：包含该选项的 IP 数据报要求所有转发路由器都查看内容</li></ol><p>读取和设置 IP 选项字段使用 getsockopt 和 setsockopt。这两个函数的第四个参数是指向某个缓冲区的一个指针，第五个参数是缓冲区的大小。<br>使用 setsockopt 设置 IP 选项之后，在相应套接字上发送的所有 IP 数据报都将包含这些选项。可以在其上设置 IP 选项的套接字包括 TCP，UDP 和原始 IP 套接字。清楚这些选项也是用 setsockopt，只是把第四个参数置为空指针，第五个参数置为 0.</p><h1 id="27-3-ipv4-源路径选项"><a href="#27-3-ipv4-源路径选项" class="headerlink" title="27.3 ipv4 源路径选项"></a>27.3 ipv4 源路径选项</h1><p>源路径是由 IP 数据报的发送者指定的一个 IP 地址列表。如果源路径是严格的，那么数据报必须且只能逐一经过所有的节点。也就是说源路径中的所有节点必须前后为邻居。如果源路径是宽松的，那么数据报必须注意经过所有的节点，不过也可以经过未列在源路径中的其他节点。<br>IPv4源路径称为源和记录路径(source and record route, SRR)，宽松源路径选项称为LSRR，严格源路径选项称为SSRR，原因是当数据报穿过所列的全部节点到来时，每个节点都将自己的所列地址替换为自己的外出接口地址。这允许接收者将新的列表倒序以沿逆向的路径回到发送者。这两个源路径的例子以及相应的tcpdump输出在TCPv1的8.5节</p><h1 id="27-4-ipv6-扩展首部"><a href="#27-4-ipv6-扩展首部" class="headerlink" title="27.4 ipv6 扩展首部"></a>27.4 ipv6 扩展首部</h1><p>IPv6 首部可以后跟如下几种可选的扩展首部：</p><ol><li>步跳选项</li><li>目的地选项</li><li>路径首部</li><li>分片首部：完全由内核处理</li><li>认证首部</li><li>安全净荷封装</li></ol><h1 id="27-5-ipv6-步跳选项和目的地选项"><a href="#27-5-ipv6-步跳选项和目的地选项" class="headerlink" title="27.5 ipv6 步跳选项和目的地选项"></a>27.5 ipv6 步跳选项和目的地选项</h1><p>步跳选项和目的地选项有类似的格式，</p><h1 id="27-6-ipv6-路由首部"><a href="#27-6-ipv6-路由首部" class="headerlink" title="27.6 ipv6 路由首部"></a>27.6 ipv6 路由首部</h1><h1 id="27-7-ipv6-粘附选项"><a href="#27-7-ipv6-粘附选项" class="headerlink" title="27.7 ipv6 粘附选项"></a>27.7 ipv6 粘附选项</h1><p>有 7 种可以使用 sendmsg 和 recvmsg 发送和接受的辅助数据对象：</p><ol><li>IPv6 分组信息：in6_pktinfo 结构或者包含目的地址和外出接口索引，或者包含源地址和到达接口索引。</li><li>外出跳限或接收跳限</li><li>下一跳地址</li><li>外出流通类别或接收流通类别</li><li>步跳选项</li><li>目的地选项</li><li>路由首部</li></ol><p>如果这些辅助数据对象各自有单个值将应用于从某个套接字发送的所有分组，那么我们不必每次调用 sendmsg 时都发送它们，而是代之以设置相应的套接字选项。这些套接字选项所用常值与辅助数据对象一致，也就是说调用 setsockopt 的级别参数总是 IPPROTO_IPV6</p><h1 id="27-8-历史性-ipv6-高级-api"><a href="#27-8-历史性-ipv6-高级-api" class="headerlink" title="27.8 历史性 ipv6 高级 api"></a>27.8 历史性 ipv6 高级 api</h1><h1 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h1>]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;27-1-概述&quot;&gt;&lt;a href=&quot;#27-1-概述&quot; class=&quot;headerlink&quot; title=&quot;27.1 概述&quot;&gt;&lt;/a&gt;27.1 概述&lt;/h1&gt;&lt;p&gt;&amp;emsp;&amp;emsp;IPv4 允许在 20 字节首部固定部分之后跟最多 40 个字节的选项。尽管已经定义的 IPv4 选项共有 10 种，常用的确实源路径选项。这些选项访问途径是存取 IP_OPTIONS 套接字选项。&lt;br&gt;&amp;emsp;&amp;emsp;IPv6 允许在固定长度的 40 字节 IPv6 首部和传输层首部之间出现扩展首部。目前定义了 6 种不同的扩展首部。不同于 Ipv4 的是，IPv6 扩展首部的访问途径是函数接口。&lt;/p&gt;
    
    </summary>
    
    
      <category term="UNP" scheme="http://yoursite.com/categories/UNP/"/>
    
    
  </entry>
  
  <entry>
    <title>第24章：带外数据</title>
    <link href="http://yoursite.com/2019/09/25/24.%E5%B8%A6%E5%A4%96%E6%95%B0%E6%8D%AE/"/>
    <id>http://yoursite.com/2019/09/25/24.带外数据/</id>
    <published>2019-09-25T14:54:48.822Z</published>
    <updated>2019-09-28T08:46:38.258Z</updated>
    
    <content type="html"><![CDATA[<h1 id="24-1-概述"><a href="#24-1-概述" class="headerlink" title="24.1 概述"></a>24.1 概述</h1><p>&emsp;&emsp;许多传输层有带外数据的概念，它有时也称为<strong>经加速数据</strong>。其想法是一个已连接某端发生重要的事情，而且该端希望迅速通告其对端（也就是比正在缓冲区排队的数据还要快）带外数据的优先级高于普通数据。带外数据不会新建立连接，会被映射到已有连接中。<br>&emsp;&emsp;UDP 没有办法实现带外数据，所以我们仅仅关心 TCP 的带外数据模型。</p><a id="more"></a><h1 id="24-2-tcp-带外数据"><a href="#24-2-tcp-带外数据" class="headerlink" title="24.2 tcp 带外数据"></a>24.2 tcp 带外数据</h1><p>&emsp;&emsp;TCP 没有真正的带外数据，不过提供了<strong>紧急模式</strong>。<br>eg：send(fd,”a”,1 MSG_OOB); // 写入一个字节的带外数据<br>&emsp;&emsp;TCP 会把这个数据放在缓冲区待发送数据之后，并把该连接的 TCP 紧急指针设置成在下一个可用位置，会把带外字节标记为 OOB。<br>&emsp;&emsp;TCP 紧急模式的特点：TCP 首部指出发送端进入紧急模式，但是紧急指针所指的实际数据并不一定会发出。  </p><h2 id="接收端视角："><a href="#接收端视角：" class="headerlink" title="接收端视角："></a>接收端视角：</h2><ol><li>当接受到一个设置了URG标志的分节时，接受端检查紧急指针，确定它是否指向新的带外数据，比如：前面发送了两个包，只有第一个才会通知接受进程有新的带外数据到达。</li><li>当有新的紧急指针到达时，接受进程被通知到。首先，内核会给接受套接字的属主进程发送SIGURG信号，前提是接受进程调用了 fcntl或者ioctl为这个套接字建立了属主，并且该属主进程为该信号建立了信号处理函数 。</li><li>只有一个OOB标记，如果新的OOB字节在旧的OOB字节之前到达，旧的OOB字节就会被丢弃。</li><li>当由紧急指针指向的实际数据字节到达接受端TCP时，数据字节会有两个存储地区：一个是和普通数据一样的在线留存，另外一个是独立的单字节带外缓冲区，接受进程从这个单字节带外缓冲区读入数据的唯一方法是指定MSG_OOB调用recv，recvfrom，recvmsg。如果放在和普通数据一起的带内区域，接受进程就得通过检查该连接的带外标记OOB来获悉何时访问带这个数据字节。两个区域的使用通过套接字选项SO_OOBLINE来使用，默认情况下将带外数据字节放入独立的单字节带外缓冲区内。</li></ol><h2 id="可能发生的错误："><a href="#可能发生的错误：" class="headerlink" title="可能发生的错误："></a>可能发生的错误：</h2><ol><li>如果接受进程请求读入数据（通过MSG_OOB标志），但是对端并没有发送任何带外数据，读入操作将返回EINVAL。</li><li>在接受进程已被告知对端发送了一个带外字节（SIGURG和select）的前提下，如果接受进程试图读入该字节，但是该字节尚未到达，读入操作返回EWOULDBLOCK。接受进程此时做的就是从缓冲区中读入数据，腾出空间，以允许对端TCP发送出那个带外字节。</li><li>如果接受进程试图多次读入同一个带外字节，读入操作返回EINVAL。</li><li>如果开启了SO_OOBINLINE套接字选项，接受进程如果还是通过MSG_OOB读入带外数据，读入操作将返回EINVAL 。</li></ol><p>※ 下面介绍了两个发送接收带外数据的例子</p><h1 id="24-3-sockatmark-函数"><a href="#24-3-sockatmark-函数" class="headerlink" title="24.3 sockatmark 函数"></a>24.3 sockatmark 函数</h1><p>&emsp;&emsp;本小节提供了多个例子来解释带外数据的发送接收情况，并说明了带外数据的以下两个特性：</p><ol><li>带外标记总是指向带外数据最后一个字节的下一个位置</li><li>读操作总是停止在带外数据上</li></ol><h1 id="24-4-tcp-带外数据小结"><a href="#24-4-tcp-带外数据小结" class="headerlink" title="24.4 tcp 带外数据小结"></a>24.4 tcp 带外数据小结</h1><p>&emsp;&emsp;带外数据概念实际上时向接收端传送三个不同的信息：</p><ol><li>发送端进入紧急模式这个事实。接收进程得以通知这个事实的手段不外乎SIGURG信号或select调用。本通知在发送进程发送带外字节后由发送端TCP立即发送，即使往接收端的任何数据发送因流量控制而停止了，TCP仍然发送本通知。本通知可能导致接收端进入某种特殊处理模式，以处理接收的任何后继数据。</li><li>带外字节的位置，也就是它相对于来自发送端的其余数据的发送位置：带外标记。</li><li>带外字节的实际值。既然TCP是一个不解释应用进程所发送数据的字节流协议，带外字节就可以是任何8位值。</li></ol><p>&emsp;&emsp;对于TCP的紧急模式，我们可以认为URG标志时通知（信息1），紧急指针是带外标记（信息2），数据字节是其本身（信息3）。</p><h2 id="带外数据概念相关的问题有："><a href="#带外数据概念相关的问题有：" class="headerlink" title="带外数据概念相关的问题有："></a>带外数据概念相关的问题有：</h2><ul><li>每个连接只有一个TCP紧急指针；</li><li>每个连接只有一个带外标记；</li><li>每个连接只有一个单字节的带外缓冲区（该缓冲区只有在数据非在线读入时才需考虑）。  </li></ul><p>&emsp;&emsp;如果带外数据是在线读入的，那么当心的带外数据到达时，先前的带外字节字节并未丢失，不过他们的标记却因此被新的标记取代而丢失了。<br>&emsp;&emsp;带外数据的一个常见的用途体现在rlogin程序中。当客户中断运行在服务器主机上的程序时，服务器需要告知客户丢弃所有已在服务器排队的输出，因为已经排队等着从服务器发送到客户的输出最多有一个窗口的大小。服务器向客户发送一个特殊字节，告知后者清刷所有这些输出（在客户看来是输入），这个特殊字节就作为带外数据发送。客户收到由带外数据引发的SIGURG信号后，就从套接字中读入直到碰到带外数据发送。客户收到由带外数据引发的SIGURG信号后，就从套接字中读入直到碰到带外标记，并丢弃到标记之前的所有数据。这种情形下即使服务器相继地快速发送多个带外字节，客户也不受影响，因为客户只是读到最后一个标记为止，并丢弃所有读入的数据。<br>&emsp;&emsp;总之，带外数据是否有用取决于应用程序使用它的目的。如果目的是告知对端丢弃直到标记处得普通数据，那么丢失一个中间带外字节及其相应的标记不会有什么不良后果。但是如果不丢失带外字节本身很重要，那么必须在线收到这些数据。另外，作为带外数据发送的数据字节应该区别于普通数据，因为当前新的标记到达时，中间的标记将被覆写，从而事实上把带外字节混杂在普通数据之中。举例来说，telnet在客户和服务器之间普通的数据流中发送telnet自己的命令，手段是把值为255的一个字节作为telnet命令的前缀字节。（值为255的单个字节作为数据发送需要2个相继地值为255的字节。）这么做使得telnet能够区分其命令和普通用户数据，不过要求客户进程和服务器进程处理每个数据字节以寻找命令。</p><h1 id="24-5-客户-服务器心搏函数"><a href="#24-5-客户-服务器心搏函数" class="headerlink" title="24.5 客户/服务器心搏函数"></a>24.5 客户/服务器心搏函数</h1><p>&emsp;&emsp;给出一个例子模拟 KEPPALIVE 选项的操作，但是避免了其一些不好的特性（时间过长，只能对内核进行操作，不能判断短暂断开连接）<br>&emsp;&emsp;我们会使用 TCP 的紧急模式轮询对端，客户端每秒发送一个带外字节，服务端进行应答返回一个带外字节</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 客户端部分</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span>servfd;</span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span>nsec;<span class="comment">/* #seconds betweeen each alarm */</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span>maxnprobes;<span class="comment">/* #probes w/no response before quit */</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span>nprobes;<span class="comment">/* #probes since last server response */</span></span><br><span class="line">static voidsig_urg(int), sig_alrm(int);</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span></span><br><span class="line">heartbeat_cli(<span class="keyword">int</span> servfd_arg, <span class="keyword">int</span> nsec_arg, <span class="keyword">int</span> maxnprobes_arg)</span><br><span class="line">&#123;</span><br><span class="line">servfd = servfd_arg;<span class="comment">/* set globals for signal handlers */</span></span><br><span class="line"><span class="keyword">if</span> ( (nsec = nsec_arg) &lt; <span class="number">1</span>)</span><br><span class="line">nsec = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">if</span> ( (maxnprobes = maxnprobes_arg) &lt; nsec)</span><br><span class="line">maxnprobes = nsec;</span><br><span class="line">nprobes = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">Signal(SIGURG, sig_urg);</span><br><span class="line">Fcntl(servfd, F_SETOWN, getpid());</span><br><span class="line"></span><br><span class="line">Signal(SIGALRM, sig_alrm);</span><br><span class="line">alarm(nsec);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span></span><br><span class="line">sig_urg(<span class="keyword">int</span> signo)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">int</span>n;</span><br><span class="line"><span class="keyword">char</span>c;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">if</span> ( (n = recv(servfd, &amp;c, <span class="number">1</span>, MSG_OOB)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line"><span class="keyword">if</span> (errno != EWOULDBLOCK)</span><br><span class="line">err_sys(<span class="string">"recv error"</span>);</span><br><span class="line">&#125;</span><br><span class="line">nprobes = <span class="number">0</span>;<span class="comment">/* reset counter */</span></span><br><span class="line"><span class="keyword">return</span>;<span class="comment">/* may interrupt client code */</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span></span><br><span class="line">sig_alrm(<span class="keyword">int</span> signo)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">if</span> (++nprobes &gt; maxnprobes) &#123;</span><br><span class="line"><span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"server is unreachable\n"</span>);</span><br><span class="line"><span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line">Send(servfd, <span class="string">"1"</span>, <span class="number">1</span>, MSG_OOB);</span><br><span class="line">alarm(nsec);</span><br><span class="line"><span class="keyword">return</span>;<span class="comment">/* may interrupt client code */</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 服务器部分</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span>servfd;</span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span>nsec;<span class="comment">/* #seconds between each alarm */</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span>maxnalarms;<span class="comment">/* #alarms w/no client probe before quit */</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span>nprobes;<span class="comment">/* #alarms since last client probe */</span></span><br><span class="line">static voidsig_urg(int), sig_alrm(int);</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span></span><br><span class="line">heartbeat_serv(<span class="keyword">int</span> servfd_arg, <span class="keyword">int</span> nsec_arg, <span class="keyword">int</span> maxnalarms_arg)</span><br><span class="line">&#123;</span><br><span class="line">servfd = servfd_arg;<span class="comment">/* set globals for signal handlers */</span></span><br><span class="line"><span class="keyword">if</span> ( (nsec = nsec_arg) &lt; <span class="number">1</span>)</span><br><span class="line">nsec = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">if</span> ( (maxnalarms = maxnalarms_arg) &lt; nsec)</span><br><span class="line">maxnalarms = nsec;</span><br><span class="line"></span><br><span class="line">Signal(SIGURG, sig_urg);</span><br><span class="line">Fcntl(servfd, F_SETOWN, getpid());</span><br><span class="line"></span><br><span class="line">Signal(SIGALRM, sig_alrm);</span><br><span class="line">alarm(nsec);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span></span><br><span class="line">sig_urg(<span class="keyword">int</span> signo)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">int</span>n;</span><br><span class="line"><span class="keyword">char</span>c;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">if</span> ( (n = recv(servfd, &amp;c, <span class="number">1</span>, MSG_OOB)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line"><span class="keyword">if</span> (errno != EWOULDBLOCK)</span><br><span class="line">err_sys(<span class="string">"recv error"</span>);</span><br><span class="line">&#125;</span><br><span class="line">Send(servfd, &amp;c, <span class="number">1</span>, MSG_OOB);<span class="comment">/* echo back out-of-band byte */</span></span><br><span class="line"></span><br><span class="line">nprobes = <span class="number">0</span>;<span class="comment">/* reset counter */</span></span><br><span class="line"><span class="keyword">return</span>;<span class="comment">/* may interrupt server code */</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span></span><br><span class="line">sig_alrm(<span class="keyword">int</span> signo)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">if</span> (++nprobes &gt; maxnalarms) &#123;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"no probes from client\n"</span>);</span><br><span class="line"><span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line">alarm(nsec);</span><br><span class="line"><span class="keyword">return</span>;<span class="comment">/* may interrupt server code */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h1><p>&emsp;&emsp;TCP 没有真正的带外数据，不过提供了紧急模式和紧急指针。一旦发送端进入紧急模式，紧急指针就出现在发送端的分节中的 TCP 首部中。对端的接收端收取该指针是在告知接收进程发送端已经进入紧急模式，并且该指针指向紧急数据的最后一个字节。然而所有数据的发送依然遵循 TCP 的流量控制。<br>&emsp;&emsp;套接字 API 把 TCP 的紧急模式映射为带外数据，发送进程指定 MSG_OOB 发送带外数据。TCP 接收端收到后通过发送 SIGURG 信号或者 select 出错处理来通知接收进程。<br>&emsp;&emsp;带外数据并未得到广泛的使用。tenlent 和 rlogin 使用，FTP 也是用。它们使用带外数据是为了通知对端异常情况发生，并且服务器丢弃带外标记前的所有数据。</p>]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;24-1-概述&quot;&gt;&lt;a href=&quot;#24-1-概述&quot; class=&quot;headerlink&quot; title=&quot;24.1 概述&quot;&gt;&lt;/a&gt;24.1 概述&lt;/h1&gt;&lt;p&gt;&amp;emsp;&amp;emsp;许多传输层有带外数据的概念，它有时也称为&lt;strong&gt;经加速数据&lt;/strong&gt;。其想法是一个已连接某端发生重要的事情，而且该端希望迅速通告其对端（也就是比正在缓冲区排队的数据还要快）带外数据的优先级高于普通数据。带外数据不会新建立连接，会被映射到已有连接中。&lt;br&gt;&amp;emsp;&amp;emsp;UDP 没有办法实现带外数据，所以我们仅仅关心 TCP 的带外数据模型。&lt;/p&gt;
    
    </summary>
    
    
      <category term="UNP" scheme="http://yoursite.com/categories/UNP/"/>
    
    
  </entry>
  
  <entry>
    <title>第26章：线程</title>
    <link href="http://yoursite.com/2019/09/25/26.%E7%BA%BF%E7%A8%8B/"/>
    <id>http://yoursite.com/2019/09/25/26.线程/</id>
    <published>2019-09-25T14:54:48.822Z</published>
    <updated>2019-09-28T08:46:38.259Z</updated>
    
    <content type="html"><![CDATA[<h1 id="26-1-概念"><a href="#26-1-概念" class="headerlink" title="26.1 概念"></a>26.1 概念</h1><p>&emsp;&emsp;在 Unix 上使用 fork 来创建子进程一直以来有一些问题：</p><ul><li>fork 是昂贵的。fork 要把父进程的内存映像复制到子进程，并在子进程中复制所有描述符，等等。现在的实现是 copy-on-write 技术，用以避免在子进程切实需要自己的副本之前把父进程的数据空间复制到子进程，但是就算如此，fork 依旧是昂贵的。</li><li>fork 返回之后父子进程之间信息的传递需要进程间通信 IPC 机制。调用 fork 之前父进程尚未存在的子进程传递信息相当容易，因为子进程将从父进程数据空间及所有描述符的一个副本开始运行。然而从子进程中返回数据较为繁琐。</li></ul><p>&emsp;&emsp;线程有助于解决这两个问题。线程有时称为轻权进程，因为线程比进程权重较轻。也就是说线程创建比进程创建快 10 ~ 100 倍。<br>&emsp;&emsp;统一进程内的所有线程共享相同的全局内存。这使得线程之间易于共享信息，然而伴随着这种简易性而来的确是同步问题。  </p><h2 id="统一进程内的所有线程共享的资源如下："><a href="#统一进程内的所有线程共享的资源如下：" class="headerlink" title="统一进程内的所有线程共享的资源如下："></a>统一进程内的所有线程共享的资源如下：</h2><ul><li>全局变量</li><li>进程指令</li><li>大多数数据</li><li>描述符</li><li>信号处理函数和信号处置</li><li>当前的工作目录</li><li>用户 ID 和组 ID</li></ul><h2 id="每个线程单独使用的有："><a href="#每个线程单独使用的有：" class="headerlink" title="每个线程单独使用的有："></a>每个线程单独使用的有：</h2><ul><li>线程 ID</li><li>寄存器集合，包括程序计数器和栈指针</li><li>栈（存放局部变量和返回地址）</li><li>errno</li><li>信号掩码</li><li>优先级<a id="more"></a><h1 id="26-2-基本线程函数：创建和终止"><a href="#26-2-基本线程函数：创建和终止" class="headerlink" title="26.2 基本线程函数：创建和终止"></a>26.2 基本线程函数：创建和终止</h1></li></ul><p>本节包括 5 个基本线程函数（老朋友）</p><h2 id="pthread-create-函数"><a href="#pthread-create-函数" class="headerlink" title="pthread_create 函数"></a>pthread_create 函数</h2><p>&emsp;&emsp;当一个程序由 exec 启动执行时，称为<strong>初始线程</strong>或<strong>主线程</strong>的单个线程就创建了。其余线程就由 pthread_create 创建。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_create</span><span class="params">(<span class="keyword">pthread_t</span> *tid, <span class="keyword">const</span> <span class="keyword">pthread_attr_t</span> * attr, <span class="keyword">void</span>*(*func)(<span class="keyword">void</span> *), <span class="keyword">void</span> *arg)</span></span>;</span><br><span class="line"><span class="comment">// 返回：成功返回 0，出错是正的 Exxx 值</span></span><br></pre></td></tr></table></figure><ol><li>一个进程内的线程都由一个线程 ID 标识，其数据类型为 pthread_t（unsigned int），如果新的线程创建，其 ID 就通过 tid 指针返回。  </li><li>每个线程都有许多属性：优先级，初始栈大小，是否该成为一个守护线程 等等。初始化时可以修改 pthread_attr_t 结构来设置这些属性。通常情况下采用默认设置，此时 attr 是一个空指针。  </li><li>创建一个线程时，最后指定的是运行的函数以及参数。线程通过调用这个函数开始执行，然后或者显示的终止，或者隐士的终止。该函数的地址由 func 参数指定，唯一调用参数是指针 arg。如果我们需要给该函数传递多个参数，就需要把这些参数打包成一个结构，然后把这个结构的地址作为单个参数传递给这个起始函数。</li></ol><p>&emsp;&emsp;func 和 arg 的声明。func 所指函数作为参数接受一个通用指针 void* 有作为返回值返回一个 void* 指针。这使得我们可以把一个指针（指向任何内容）传递给线程，又允许线程返回一个指针（指向任何内容）<br>※ <strong><em>注意返回值出错全为正值，和 errno 不相同，且 pthread 不会设置 errno。各种 Exxx 参考&lt;sys/errno.h&gt;</em></strong></p><h2 id="pthread-join-函数"><a href="#pthread-join-函数" class="headerlink" title="pthread_join 函数"></a>pthread_join 函数</h2><p>&emsp;&emsp;可以通过 pthread_join 等待一个线程终止。对比线程和进程。pthread_create 类似于 fork，pthread_join 类似于 waitpid</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_join</span><span class="params">(<span class="keyword">pthread_t</span>* tid, <span class="keyword">void</span>** status)</span></span>;</span><br><span class="line"><span class="comment">// 返回：成功返回 0，出错是正的 Exxx 值</span></span><br></pre></td></tr></table></figure><p>※ 我们必须要指定等待线程的 tid。但是 pthread 并不能等待任意一个线程（类似于使用 -1 作为进程 ID 调用 waitpid）<br>※ 如果 status != null，来自所等待线程的返回值（一个指向某个对象的指针）将存入由 status 指向的位置。</p><h2 id="pthread-self-函数"><a href="#pthread-self-函数" class="headerlink" title="pthread_self 函数"></a>pthread_self 函数</h2><p>&emsp;&emsp;每个线程都有一个唯一的线程 ID。线程 ID 由 pthread_create 返回，而且我们已经看到 pthread_join 使用它，每个线程通过 pthread_self 获取自身的线程 ID</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="keyword">pthread_t</span> pthread_self(<span class="keyword">void</span>);</span><br><span class="line"><span class="comment">// 返回：调用线程的线程 ID</span></span><br></pre></td></tr></table></figure><p>※ 对比进程，类似于 getpid</p><h2 id="pthread-detach-函数"><a href="#pthread-detach-函数" class="headerlink" title="pthread_detach 函数"></a>pthread_detach 函数</h2><p>&emsp;&emsp;一个线程或者是可汇合的（joinable，默认），或者是脱离的（detached）当一个 joinable 线程终止时，它的线程 ID 和推出状态将留存到另一个线程对它调用 pthread_join。脱离的线程却像守护线程，当他们终止时，所有资源会释放，我们不能等待其终止。如果一个线程需要知道另一个线程什么时候终止，那就最好保持第二个线程的 joinable 状态。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_detach</span><span class="params">(<span class="keyword">pthread_t</span> tid)</span></span>;</span><br><span class="line"><span class="comment">// 返回：若成功则为 0，若出错则为正的 Exxx 值</span></span><br></pre></td></tr></table></figure><p>本函数通常有想让自己脱离的线程调用：  </p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pthread_detach(pthread_self());</span><br></pre></td></tr></table></figure><h2 id="pthread-exit-函数"><a href="#pthread-exit-函数" class="headerlink" title="pthread_exit 函数"></a>pthread_exit 函数</h2><p>让线程终止的方法之一是调用 pthread_exit</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">pthread_exit</span><span class="params">(<span class="keyword">void</span>* status)</span></span>;</span><br></pre></td></tr></table></figure><p>※ 如果本线程没有脱离，它的线程 ID 和退出状态会一直留存到调用进程内的某个其他线程对它调用 pthread_join<br>※ 指针 status 不能指向调用线程的局部对象，因为终止时局部对象会消失。  </p><h2 id="让一个进程终止的方法："><a href="#让一个进程终止的方法：" class="headerlink" title="让一个进程终止的方法："></a>让一个进程终止的方法：</h2><ol><li>调用 pthread_exit 函数</li><li>启动线程的函数（pthread_create）可以返回</li><li>如果进程的 main 函数返回或者任意线程调用了 exit，整个进程终止，包括其中的任何线程</li></ol><h1 id="26-3-使用线程的-str-cli-函数"><a href="#26-3-使用线程的-str-cli-函数" class="headerlink" title="26.3 使用线程的 str_cli 函数"></a>26.3 使用线程的 str_cli 函数</h1><p>把使用 fork 的 str_cli 改写成使用线程</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span>*<span class="title">copyto</span><span class="params">(<span class="keyword">void</span> *)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 全局变量传参，简单一些</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span>sockfd;<span class="comment">/* global for both threads to access */</span></span><br><span class="line"><span class="keyword">static</span> FILE*fp;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span></span><br><span class="line">str_cli(FILE *fp_arg, <span class="keyword">int</span> sockfd_arg)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">char</span>recvline[MAXLINE];</span><br><span class="line"><span class="keyword">pthread_t</span>tid;</span><br><span class="line"></span><br><span class="line">sockfd = sockfd_arg;<span class="comment">/* copy arguments to externals */</span></span><br><span class="line">fp = fp_arg;</span><br><span class="line">    <span class="comment">// 创建一个线程</span></span><br><span class="line">Pthread_create(&amp;tid, <span class="literal">NULL</span>, copyto, <span class="literal">NULL</span>);</span><br><span class="line">    <span class="comment">// 主循环调用 readline 以及 fputs，把从套接字读入的每个文本行复制到标准输出</span></span><br><span class="line"><span class="keyword">while</span> (Readline(sockfd, recvline, MAXLINE) &gt; <span class="number">0</span>)</span><br><span class="line">Fputs(recvline, <span class="built_in">stdout</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程执行函数</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> * <span class="title">copyto</span><span class="params">(<span class="keyword">void</span> *arg)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">char</span>sendline[MAXLINE];</span><br><span class="line">    <span class="comment">// 吧读入的数据写入到套接字</span></span><br><span class="line"><span class="keyword">while</span> (Fgets(sendline, MAXLINE, fp) != <span class="literal">NULL</span>)</span><br><span class="line">Writen(sockfd, sendline, <span class="built_in">strlen</span>(sendline));</span><br><span class="line">    <span class="comment">// 发送 FIN</span></span><br><span class="line">Shutdown(sockfd, SHUT_WR);<span class="comment">/* EOF on stdin, send FIN */</span></span><br><span class="line">    <span class="comment">// 返回，从启动线程的函数终止该线程，上面的第二种方式</span></span><br><span class="line"><span class="keyword">return</span>(<span class="literal">NULL</span>);</span><br><span class="line"><span class="comment">/* 4return (i.e., thread terminates) when EOF on stdin */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="26-4-使用线程的-TCP-回射服务器程序"><a href="#26-4-使用线程的-TCP-回射服务器程序" class="headerlink" title="26.4 使用线程的 TCP 回射服务器程序"></a>26.4 使用线程的 TCP 回射服务器程序</h1><p>&emsp;&emsp;把回射程序换成每个客户一个线程，而不是一个子进程。我们同样使用自己的 tcp_listen 函数使得该程序协议无关</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span>*<span class="title">doit</span><span class="params">(<span class="keyword">void</span> *)</span></span>;<span class="comment">/* each thread executes this function */</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span></span><br><span class="line">main(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">int</span>listenfd, connfd;</span><br><span class="line"><span class="keyword">pthread_t</span>tid;</span><br><span class="line"><span class="keyword">socklen_t</span>addrlen, len;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr</span>*<span class="title">cliaddr</span>;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (argc == <span class="number">2</span>)</span><br><span class="line">listenfd = Tcp_listen(<span class="literal">NULL</span>, argv[<span class="number">1</span>], &amp;addrlen);</span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span> (argc == <span class="number">3</span>)</span><br><span class="line">listenfd = Tcp_listen(argv[<span class="number">1</span>], argv[<span class="number">2</span>], &amp;addrlen);</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">err_quit(<span class="string">"usage: tcpserv01 [ &lt;host&gt; ] &lt;service or port&gt;"</span>);</span><br><span class="line"></span><br><span class="line">cliaddr = Malloc(addrlen);</span><br><span class="line"><span class="comment">// 创建一个子线程为客户服务</span></span><br><span class="line"><span class="keyword">for</span> ( ; ; ) &#123;</span><br><span class="line">len = addrlen;</span><br><span class="line">connfd = Accept(listenfd, cliaddr, &amp;len);</span><br><span class="line">Pthread_create(&amp;tid, <span class="literal">NULL</span>, &amp;doit, (<span class="keyword">void</span> *) connfd);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 唯一的参数是描述符 connfd</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> * <span class="title">doit</span><span class="params">(<span class="keyword">void</span> *arg)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="comment">// 首先变为脱离状态，因为主线程没有必要等待它拆功能键的所有线程。之后调用 str_echo</span></span><br><span class="line">Pthread_detach(pthread_self());</span><br><span class="line">str_echo((<span class="keyword">int</span>) arg);<span class="comment">/* same function as before */</span></span><br><span class="line">Close((<span class="keyword">int</span>) arg);<span class="comment">/* done with connected socket */</span></span><br><span class="line"><span class="keyword">return</span>(<span class="literal">NULL</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>※ 这里主线程不会进行描述符的 close，因为一个进程中的所有的线程共享所有描述符</p><h2 id="给新线程传递参数"><a href="#给新线程传递参数" class="headerlink" title="给新线程传递参数"></a>给新线程传递参数</h2><p>这里有一个点需要注意：<br>Pthread_create(&amp;tid, NULL, &amp;doit, (void *) connfd);<br>如果使用：<br>Pthread_create(&amp;tid, NULL, &amp;doit, &amp;connfd);<br>&emsp;&emsp;可能导致较短时间创建两个线程，参数是同一个指针所指的数据。所以我们通过把 connfd 的值传递给 pthread_create 就不会发生这种事，下面有更好的方式。  </p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span>*<span class="title">doit</span><span class="params">(<span class="keyword">void</span> *)</span></span>;<span class="comment">/* each thread executes this function */</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span></span><br><span class="line">main(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">int</span>listenfd, *iptr;</span><br><span class="line"><span class="keyword">thread_t</span>tid;</span><br><span class="line"><span class="keyword">socklen_t</span>addrlen, len;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr</span>*<span class="title">cliaddr</span>;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (argc == <span class="number">2</span>)</span><br><span class="line">listenfd = Tcp_listen(<span class="literal">NULL</span>, argv[<span class="number">1</span>], &amp;addrlen);</span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span> (argc == <span class="number">3</span>)</span><br><span class="line">listenfd = Tcp_listen(argv[<span class="number">1</span>], argv[<span class="number">2</span>], &amp;addrlen);</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">err_quit(<span class="string">"usage: tcpserv01 [ &lt;host&gt; ] &lt;service or port&gt;"</span>);</span><br><span class="line"></span><br><span class="line">cliaddr = Malloc(addrlen);</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> ( ; ; ) &#123;</span><br><span class="line">len = addrlen;</span><br><span class="line"><span class="comment">// 直接创建一个新的内存来进行传递</span></span><br><span class="line">iptr = Malloc(<span class="keyword">sizeof</span>(<span class="keyword">int</span>));</span><br><span class="line">*iptr = Accept(listenfd, cliaddr, &amp;len);</span><br><span class="line">Pthread_create(&amp;tid, <span class="literal">NULL</span>, &amp;doit, iptr);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> *</span><br><span class="line">doit(<span class="keyword">void</span> *arg)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">int</span>connfd;</span><br><span class="line"><span class="comment">// 这里需要注意释放，否则内存会泄露</span></span><br><span class="line">connfd = *((<span class="keyword">int</span> *) arg);</span><br><span class="line"><span class="built_in">free</span>(arg);</span><br><span class="line"></span><br><span class="line">Pthread_detach(pthread_self());</span><br><span class="line">str_echo(connfd);<span class="comment">/* same function as before */</span></span><br><span class="line">Close(connfd);<span class="comment">/* done with connected socket */</span></span><br><span class="line"><span class="keyword">return</span>(<span class="literal">NULL</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>※ malloc 和 free 是不可以重入的。在主线程处于这两个函数之一时，从某个信号处理函数调用这两个函数之一可能导致灾难性后果，因为这两个函数操纵相同的静态数据，这里在库函数中就进行了同步互斥操作，对用户透明。</p><h2 id="线程安全函数"><a href="#线程安全函数" class="headerlink" title="线程安全函数"></a>线程安全函数</h2><p>所有 c 标准定义的函数是线程安全的，需要注意的是网络编程的函数并不一定是线程安全的。</p><h1 id="26-5-线程特定数据-thread-specific-data（TSD）"><a href="#26-5-线程特定数据-thread-specific-data（TSD）" class="headerlink" title="26.5 线程特定数据 thread-specific data（TSD）"></a>26.5 线程特定数据 thread-specific data（TSD）</h1><p>&emsp;&emsp;把一个未线程化的程序转换成使用线程的版本时，有时会碰到其中函数使用静态数据引起的错误。错误结果不确定。当一个进程中的多个线程同时调用这个静态数据时就会产生问题，因为它们使用的是同一个值，并不是每个线程单独的值。解决办法如下：</p><ul><li>使用线程特定数据。这个版本转换成了只能在线程中使用的版本。</li><li>改变调用顺序</li><li>改变接口的结构，避免使用静态变量，这样函数就是线程安全的。</li></ul><h2 id="TSD-目的："><a href="#TSD-目的：" class="headerlink" title="TSD 目的："></a>TSD 目的：</h2><p>线程特定数据，也称为线程私有数据，是存储和查询某个特定数据相关数据的一种机制。</p><p>在单线程程序中，我们经常要用到“全局变量”以实现多个函数间共享数据。</p><p>在多线程环境下，由于数据空间是共享的，因此全局变量也为所有所有线程所共有。</p><p>但有时应用程序设计中有必要提供线程私有的全局变量，仅在某个线程中有效，但却可以跨多个函数访问。</p><h2 id="相关函数"><a href="#相关函数" class="headerlink" title="相关函数"></a>相关函数</h2><p>&emsp;&emsp;在分配线程特定数据之前，需要创建与该数据关联的键。这个键将用于获取对线程特定数据的访问。使用 pthread_key_create函数创建一个键。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_key_create</span><span class="params">(<span class="keyword">pthread_key_t</span> *key, <span class="keyword">void</span> (*destr_function) (<span class="keyword">void</span>*))</span></span>;</span><br></pre></td></tr></table></figure><p>&emsp;&emsp;创建的键存储在pkey指向的内存单元中，这个键可以被进程中的所有线程使用，但每个线程与不同的线程特定数据地址相关联。创建新键时，每个线程的数据地址设为空值。<br>&emsp;&emsp;除了创建键以外，pthread_key_create可以为该键关联一个可选择的析构函数。当这个线程退出时，如果数据地址已经被置为非空值，那么析构函数就会被调用，它唯一的参数就是该数据地址。如果传入的析构函数为空，就表明没有析构函数与这个键关联。<br>&emsp;&emsp;线程通常使用malloc为线程特定数据分配内存。析构函数通常释放已分配的内存。<br>&emsp;&emsp;对所有的线程，我们都可以通过调用pthread_key_delete函数来取消键与线程特定数据值之间的联系。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_key_delete</span><span class="params">(<span class="keyword">pthread_key_t</span> key)</span></span>;</span><br></pre></td></tr></table></figure><p>&emsp;&emsp;有些线程可能看到一个键值，而其他的线程看到的可能是另一个不同的键值，这取决于系统是如何调度线程的，解决这种竞争的办法是使用pthread_once函数 。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">pthread_once_t</span> once_control = PTHREAD_ONCE_INIT;</span><br><span class="line"><span class="function"><span class="keyword">int</span>  <span class="title">pthread_once</span><span class="params">(<span class="keyword">pthread_once_t</span>  *once_control,  <span class="keyword">void</span>  (*init_routine) (<span class="keyword">void</span>))</span></span>;</span><br></pre></td></tr></table></figure><p>&emsp;&emsp;once_control必须是一个非本地变量（如全局变量或静态变量），而且必须初始化为PTHREAD_ONCE_INIT。如果每个线程都调用pthread_once，系统就能保证初始化once_control只被调用一次，即系统首次调用pthread_once时。<br>&emsp;&emsp;键一旦创建以后，就可以通过调用pthread_setspecific函数把键和线程特定数据关联起来。可以通过pthread_getspecific获得线程特定数据的地址。  </p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_setspecific</span><span class="params">(<span class="keyword">pthread_key_t</span> key, <span class="keyword">const</span> <span class="keyword">void</span> *pointer)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> * <span class="title">pthread_getspecific</span><span class="params">(<span class="keyword">pthread_key_t</span> key)</span></span>;</span><br></pre></td></tr></table></figure><p>&emsp;&emsp;如果没有线程特定数据值与键关联，pthread_getspecific将返回一个空指针，我们可以用这个空指针确定是否需要调用pthread_setspecific。</p><h1 id="26-6-web-客户与同时连接"><a href="#26-6-web-客户与同时连接" class="headerlink" title="26.6 web 客户与同时连接"></a>26.6 web 客户与同时连接</h1><p>&emsp;&emsp;使用线程代替非阻塞 I/O 编写 web 客户程序例子。改用线程之后，我们可以让套接字停留在默认的阻塞模式，对每个连接创建一个线程。每个线程可以阻塞在它的 connect 调用中，因为内核会交替运行线程。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span>MAXFILES20</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>SERV<span class="meta-string">"80"</span><span class="comment">/* port number or service name */</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">file</span> &#123;</span></span><br><span class="line">  <span class="keyword">char</span>*f_name;<span class="comment">/* filename */</span></span><br><span class="line">  <span class="keyword">char</span>*f_host;<span class="comment">/* hostname or IP address */</span></span><br><span class="line">  <span class="keyword">int</span>    f_fd;<span class="comment">/* descriptor */</span></span><br><span class="line">  <span class="keyword">int</span> f_flags;<span class="comment">/* F_xxx below */</span></span><br><span class="line">  <span class="keyword">pthread_t</span> f_tid;<span class="comment">/* thread ID */</span></span><br><span class="line">&#125; file[MAXFILES];</span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>F_CONNECTING1<span class="comment">/* connect() in progress */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>F_READING2<span class="comment">/* connect() complete; now reading */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>F_DONE4<span class="comment">/* all done */</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>GET_CMD<span class="meta-string">"GET %s HTTP/1.0\r\n\r\n"</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span>nconn, nfiles, nlefttoconn, nlefttoread;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span>*<span class="title">do_get_read</span><span class="params">(<span class="keyword">void</span> *)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span><span class="title">home_page</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *, <span class="keyword">const</span> <span class="keyword">char</span> *)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span><span class="title">write_get_cmd</span><span class="params">(struct file *)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span></span><br><span class="line">main(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">int</span>i, n, maxnconn;</span><br><span class="line"><span class="keyword">pthread_t</span>tid;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">file</span>*<span class="title">fptr</span>;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (argc &lt; <span class="number">5</span>)</span><br><span class="line">err_quit(<span class="string">"usage: web &lt;#conns&gt; &lt;IPaddr&gt; &lt;homepage&gt; file1 ..."</span>);</span><br><span class="line">maxnconn = atoi(argv[<span class="number">1</span>]);</span><br><span class="line"></span><br><span class="line">nfiles = min(argc - <span class="number">4</span>, MAXFILES);</span><br><span class="line"><span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; nfiles; i++) &#123;</span><br><span class="line">file[i].f_name = argv[i + <span class="number">4</span>];</span><br><span class="line">file[i].f_host = argv[<span class="number">2</span>];</span><br><span class="line">file[i].f_flags = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"nfiles = %d\n"</span>, nfiles);</span><br><span class="line"></span><br><span class="line">home_page(argv[<span class="number">2</span>], argv[<span class="number">3</span>]);</span><br><span class="line"></span><br><span class="line">nlefttoread = nlefttoconn = nfiles;</span><br><span class="line">nconn = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> (nlefttoread &gt; <span class="number">0</span>) &#123;</span><br><span class="line"><span class="comment">// 创建另一个线程，新线程执行的函数是 do_get_read，传递给它的参数是指向 file 结构的指针</span></span><br><span class="line"><span class="keyword">while</span> (nconn &lt; maxnconn &amp;&amp; nlefttoconn &gt; <span class="number">0</span>) &#123;</span><br><span class="line"><span class="comment">/* 4find a file to read */</span></span><br><span class="line"><span class="keyword">for</span> (i = <span class="number">0</span> ; i &lt; nfiles; i++)</span><br><span class="line"><span class="keyword">if</span> (file[i].f_flags == <span class="number">0</span>)</span><br><span class="line"><span class="keyword">break</span>;</span><br><span class="line"><span class="keyword">if</span> (i == nfiles)</span><br><span class="line">err_quit(<span class="string">"nlefttoconn = %d but nothing found"</span>, nlefttoconn);</span><br><span class="line"></span><br><span class="line">file[i].f_flags = F_CONNECTING;</span><br><span class="line">Pthread_create(&amp;tid, <span class="literal">NULL</span>, &amp;do_get_read, &amp;file[i]);</span><br><span class="line">file[i].f_tid = tid;</span><br><span class="line">nconn++;</span><br><span class="line">nlefttoconn--;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 等待任何一个线程结束，使用的是 thr_join，因为 pthread 并不提供这种操作</span></span><br><span class="line"><span class="comment">// 后面会进行改变，使用性能更好的 Pthread 操作</span></span><br><span class="line"><span class="keyword">if</span> ( (n = thr_join(<span class="number">0</span>, &amp;tid, (<span class="keyword">void</span> **) &amp;fptr)) != <span class="number">0</span>)</span><br><span class="line">errno = n, err_sys(<span class="string">"thr_join error"</span>);</span><br><span class="line"></span><br><span class="line">nconn--;</span><br><span class="line">nlefttoread--;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"thread id %d for %s done\n"</span>, tid, fptr-&gt;f_name);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> *</span><br><span class="line">do_get_read(<span class="keyword">void</span> *vptr)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">int</span>fd, n;</span><br><span class="line"><span class="keyword">char</span>line[MAXLINE];</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">file</span>*<span class="title">fptr</span>;</span></span><br><span class="line"></span><br><span class="line">fptr = (struct file *) vptr;</span><br><span class="line"><span class="comment">// 创建一个 TCP 连接，是一个阻塞的套接字，会一直阻塞在 connect</span></span><br><span class="line">fd = Tcp_connect(fptr-&gt;f_host, SERV);</span><br><span class="line">fptr-&gt;f_fd = fd;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"do_get_read for %s, fd %d, thread %d\n"</span>,</span><br><span class="line">fptr-&gt;f_name, fd, fptr-&gt;f_tid);</span><br><span class="line"><span class="comment">// 向服务器写入请求</span></span><br><span class="line">write_get_cmd(fptr);<span class="comment">/* write() the GET command */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* 4Read server's reply */</span></span><br><span class="line"><span class="comment">// 读取服务器的应答</span></span><br><span class="line"><span class="keyword">for</span> ( ; ; ) &#123;</span><br><span class="line"><span class="keyword">if</span> ( (n = Read(fd, line, MAXLINE)) == <span class="number">0</span>)</span><br><span class="line"><span class="keyword">break</span>;<span class="comment">/* server closed connection */</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"read %d bytes from %s\n"</span>, n, fptr-&gt;f_name);</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"end-of-file on %s\n"</span>, fptr-&gt;f_name);</span><br><span class="line">Close(fd);</span><br><span class="line">fptr-&gt;f_flags = F_DONE;<span class="comment">/* clears F_READING */</span></span><br><span class="line"><span class="comment">// 终止线程</span></span><br><span class="line"><span class="keyword">return</span>(fptr);<span class="comment">/* terminate thread */</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span></span><br><span class="line">write_get_cmd(struct file *fptr)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">int</span>n;</span><br><span class="line"><span class="keyword">char</span>line[MAXLINE];</span><br><span class="line"></span><br><span class="line">n = <span class="built_in">snprintf</span>(line, <span class="keyword">sizeof</span>(line), GET_CMD, fptr-&gt;f_name);</span><br><span class="line">Writen(fptr-&gt;f_fd, line, n);</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"wrote %d bytes for %s\n"</span>, n, fptr-&gt;f_name);</span><br><span class="line"></span><br><span class="line">fptr-&gt;f_flags = F_READING;<span class="comment">/* clears F_CONNECTING */</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span></span><br><span class="line">home_page(<span class="keyword">const</span> <span class="keyword">char</span> *host, <span class="keyword">const</span> <span class="keyword">char</span> *fname)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">int</span>fd, n;</span><br><span class="line"><span class="keyword">char</span>line[MAXLINE];</span><br><span class="line"></span><br><span class="line">fd = Tcp_connect(host, SERV);<span class="comment">/* blocking connect() */</span></span><br><span class="line"></span><br><span class="line">n = <span class="built_in">snprintf</span>(line, <span class="keyword">sizeof</span>(line), GET_CMD, fname);</span><br><span class="line">Writen(fd, line, n);</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> ( ; ; ) &#123;</span><br><span class="line"><span class="keyword">if</span> ( (n = Read(fd, line, MAXLINE)) == <span class="number">0</span>)</span><br><span class="line"><span class="keyword">break</span>;<span class="comment">/* server closed connection */</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"read %d bytes of home page\n"</span>, n);</span><br><span class="line"><span class="comment">/* do whatever with data */</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"end-of-file on home page\n"</span>);</span><br><span class="line">Close(fd);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="26-7-互斥锁"><a href="#26-7-互斥锁" class="headerlink" title="26.7 互斥锁"></a>26.7 互斥锁</h1><p>&emsp;&emsp;同一段代码操作全局变量在多线程的情况下会发生错误。<br>&emsp;&emsp;我们称线程编程为<strong>并发编程</strong>，在多线程的秦观管辖需要注意全局变量等可能会因为计算机的执行顺序所导致的错误。<br>&emsp;&emsp;针对上面的问题可以使用互斥锁 mutex 保护这个全局变量进行解决。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_mutex_lock</span><span class="params">(<span class="keyword">pthread_mutex_t</span>* mptr)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_mutex_unlock</span><span class="params">(<span class="keyword">pthread_mutex_t</span>* mptr)</span></span>;</span><br><span class="line"><span class="comment">// 返回：成功返回 0，出错返回正值的 Exxx</span></span><br></pre></td></tr></table></figure><p>※ 如果试图对一个已经被其它的线程上锁的互斥锁上锁，会阻塞住，直到互斥锁解锁。<br>※ 如果某个互斥锁变量是静态分配的，就需要初始化为 PTHREAD_MUTEX_INITIALIZER。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 使用互斥锁的计数器</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>NLOOP 5000</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span>counter;<span class="comment">/* incremented by threads */</span></span><br><span class="line"><span class="keyword">pthread_mutex_t</span>counter_mutex = PTHREAD_MUTEX_INITIALIZER;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span>*<span class="title">doit</span><span class="params">(<span class="keyword">void</span> *)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span></span><br><span class="line">main(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">pthread_t</span>tidA, tidB;</span><br><span class="line"></span><br><span class="line">Pthread_create(&amp;tidA, <span class="literal">NULL</span>, &amp;doit, <span class="literal">NULL</span>);</span><br><span class="line">Pthread_create(&amp;tidB, <span class="literal">NULL</span>, &amp;doit, <span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 4wait for both threads to terminate */</span></span><br><span class="line">Pthread_join(tidA, <span class="literal">NULL</span>);</span><br><span class="line">Pthread_join(tidB, <span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line"><span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> *</span><br><span class="line">doit(<span class="keyword">void</span> *vptr)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">int</span>i, val;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * Each thread fetches, prints, and increments the counter NLOOP times.</span></span><br><span class="line"><span class="comment"> * The value of the counter should increase monotonically.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; NLOOP; i++) &#123;</span><br><span class="line"><span class="comment">// 加锁</span></span><br><span class="line">Pthread_mutex_lock(&amp;counter_mutex);</span><br><span class="line"></span><br><span class="line">val = counter;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"%d: %d\n"</span>, pthread_self(), val + <span class="number">1</span>);</span><br><span class="line">counter = val + <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">Pthread_mutex_unlock(&amp;counter_mutex);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span>(<span class="literal">NULL</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>※ 互斥锁的开销并不是很大，但是一旦过于频繁，还是会带来性能问题</p><h1 id="26-8-条件变量"><a href="#26-8-条件变量" class="headerlink" title="26.8 条件变量"></a>26.8 条件变量</h1><p>&emsp;&emsp;互斥锁适用于防止同时访问某个共享变量，但是我们需要另外某种等待某个条件发生期间能让我们进入睡眠的东西。<br>&emsp;&emsp;互斥锁提供互斥机制，条件变量提供信号机制。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_cond_wait</span><span class="params">(<span class="keyword">pthread_cond_t</span>* cptr, <span class="keyword">pthread_mutex_t</span>* mptr)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_cond_signal</span><span class="params">(<span class="keyword">pthraed_cond_t</span>* cptr)</span></span>;</span><br><span class="line"><span class="comment">// 可以在信号发生时将唤醒多个线程</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_cond_broadcast</span><span class="params">(<span class="keyword">pthraed_cond_t</span>* cptr)</span></span>;</span><br><span class="line"><span class="comment">// 线程设置一个阻塞时间，注意这里的时间是一个绝对时间，也就是具体的时间（阻塞解除的时间），而不是阻塞的时间（阻塞的时长）</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_cond_timedwait</span><span class="params">(<span class="keyword">pthraed_cond_t</span>* cptr, <span class="keyword">pthread_mutex_t</span>* mptr, <span class="keyword">const</span> struct timespec* abstime)</span></span>;</span><br><span class="line"><span class="comment">// 返回：成功返回 0，出错返回正值的 Exxx</span></span><br></pre></td></tr></table></figure><p>※ 条件变量一般和互斥锁一起使用</p><h1 id="26-9-web-客户与同时连接（续）"><a href="#26-9-web-客户与同时连接（续）" class="headerlink" title="26.9 web 客户与同时连接（续）"></a>26.9 web 客户与同时连接（续）</h1><p>使用互斥锁和条件变量修改了上面的程序</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">while</span> (nlefttoread &gt; <span class="number">0</span>) &#123;</span><br><span class="line"><span class="keyword">while</span> (nconn &lt; maxnconn &amp;&amp; nlefttoconn &gt; <span class="number">0</span>) &#123;</span><br><span class="line"><span class="comment">/* 4find a file to read */</span></span><br><span class="line"><span class="keyword">for</span> (i = <span class="number">0</span> ; i &lt; nfiles; i++)</span><br><span class="line"><span class="keyword">if</span> (file[i].f_flags == <span class="number">0</span>)</span><br><span class="line"><span class="keyword">break</span>;</span><br><span class="line"><span class="keyword">if</span> (i == nfiles)</span><br><span class="line">err_quit(<span class="string">"nlefttoconn = %d but nothing found"</span>, nlefttoconn);</span><br><span class="line"></span><br><span class="line">file[i].f_flags = F_CONNECTING;</span><br><span class="line">Pthread_create(&amp;tid, <span class="literal">NULL</span>, &amp;do_get_read, &amp;file[i]);</span><br><span class="line">file[i].f_tid = tid;</span><br><span class="line">nconn++;</span><br><span class="line">nlefttoconn--;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 4Wait for thread to terminate */</span></span><br><span class="line">Pthread_mutex_lock(&amp;ndone_mutex);</span><br><span class="line"><span class="comment">// 等待某个线程终止，睡眠由 Pthread_cond_wait 执行</span></span><br><span class="line"><span class="keyword">while</span> (ndone == <span class="number">0</span>)</span><br><span class="line">Pthread_cond_wait(&amp;ndone_cond, &amp;ndone_mutex);</span><br><span class="line"><span class="comment">// 当发现某个线程终止时，遍历 file 找到它，在调用 pthread_join</span></span><br><span class="line"><span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; nfiles; i++) &#123;</span><br><span class="line"><span class="keyword">if</span> (file[i].f_flags &amp; F_DONE) &#123;</span><br><span class="line">Pthread_join(file[i].f_tid, (<span class="keyword">void</span> **) &amp;fptr);</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (&amp;file[i] != fptr)</span><br><span class="line">err_quit(<span class="string">"file[i] != fptr"</span>);</span><br><span class="line">fptr-&gt;f_flags = F_JOINED;<span class="comment">/* clears F_DONE */</span></span><br><span class="line">ndone--;</span><br><span class="line">nconn--;</span><br><span class="line">nlefttoread--;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"thread %d for %s done\n"</span>, fptr-&gt;f_tid, fptr-&gt;f_name);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">Pthread_mutex_unlock(&amp;ndone_mutex);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h1><ul><li>创建一个新的线程比 fork 一个子进程快得多，但是需要注意并发编程。  </li><li>统一进程中的线程共享：全局变量，描述符 等。这也引入了同步问题，可以使用 Pthread 的互斥锁和条件变量。  </li><li>后面会讲到线程池技术。</li></ul>]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;26-1-概念&quot;&gt;&lt;a href=&quot;#26-1-概念&quot; class=&quot;headerlink&quot; title=&quot;26.1 概念&quot;&gt;&lt;/a&gt;26.1 概念&lt;/h1&gt;&lt;p&gt;&amp;emsp;&amp;emsp;在 Unix 上使用 fork 来创建子进程一直以来有一些问题：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;fork 是昂贵的。fork 要把父进程的内存映像复制到子进程，并在子进程中复制所有描述符，等等。现在的实现是 copy-on-write 技术，用以避免在子进程切实需要自己的副本之前把父进程的数据空间复制到子进程，但是就算如此，fork 依旧是昂贵的。&lt;/li&gt;
&lt;li&gt;fork 返回之后父子进程之间信息的传递需要进程间通信 IPC 机制。调用 fork 之前父进程尚未存在的子进程传递信息相当容易，因为子进程将从父进程数据空间及所有描述符的一个副本开始运行。然而从子进程中返回数据较为繁琐。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&amp;emsp;&amp;emsp;线程有助于解决这两个问题。线程有时称为轻权进程，因为线程比进程权重较轻。也就是说线程创建比进程创建快 10 ~ 100 倍。&lt;br&gt;&amp;emsp;&amp;emsp;统一进程内的所有线程共享相同的全局内存。这使得线程之间易于共享信息，然而伴随着这种简易性而来的确是同步问题。  &lt;/p&gt;
&lt;h2 id=&quot;统一进程内的所有线程共享的资源如下：&quot;&gt;&lt;a href=&quot;#统一进程内的所有线程共享的资源如下：&quot; class=&quot;headerlink&quot; title=&quot;统一进程内的所有线程共享的资源如下：&quot;&gt;&lt;/a&gt;统一进程内的所有线程共享的资源如下：&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;全局变量&lt;/li&gt;
&lt;li&gt;进程指令&lt;/li&gt;
&lt;li&gt;大多数数据&lt;/li&gt;
&lt;li&gt;描述符&lt;/li&gt;
&lt;li&gt;信号处理函数和信号处置&lt;/li&gt;
&lt;li&gt;当前的工作目录&lt;/li&gt;
&lt;li&gt;用户 ID 和组 ID&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&quot;每个线程单独使用的有：&quot;&gt;&lt;a href=&quot;#每个线程单独使用的有：&quot; class=&quot;headerlink&quot; title=&quot;每个线程单独使用的有：&quot;&gt;&lt;/a&gt;每个线程单独使用的有：&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;线程 ID&lt;/li&gt;
&lt;li&gt;寄存器集合，包括程序计数器和栈指针&lt;/li&gt;
&lt;li&gt;栈（存放局部变量和返回地址）&lt;/li&gt;
&lt;li&gt;errno&lt;/li&gt;
&lt;li&gt;信号掩码&lt;/li&gt;
&lt;li&gt;优先级&lt;/li&gt;&lt;/ul&gt;
    
    </summary>
    
    
      <category term="UNP" scheme="http://yoursite.com/categories/UNP/"/>
    
    
  </entry>
  
  <entry>
    <title>第17章：ioctl操作</title>
    <link href="http://yoursite.com/2019/09/25/17.ioctl%E6%93%8D%E4%BD%9C/"/>
    <id>http://yoursite.com/2019/09/25/17.ioctl操作/</id>
    <published>2019-09-25T14:54:48.821Z</published>
    <updated>2019-09-28T08:46:38.258Z</updated>
    
    <content type="html"><![CDATA[<h1 id="17-1-概述"><a href="#17-1-概述" class="headerlink" title="17.1 概述"></a>17.1 概述</h1><p>&emsp;&emsp;ioctl 函数传统上一直作为那些不适合归入其他精细定义类别的特性的系统接口。POSIX 创造了一系列函数用来代替 icotl，但是它还是必不可少的。网络程序经常在程序启动后执行使用 ioctl 获取所在主机全部网络接口的信息：接口地址，是否支持广播，是否支持多播等等</p><a id="more"></a><h1 id="17-2-ioctl-函数"><a href="#17-2-ioctl-函数" class="headerlink" title="17.2 ioctl 函数"></a>17.2 ioctl 函数</h1><p>本函数影响由 fd 参数引用的一个打开的文件</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">ioctl</span><span class="params">(<span class="keyword">int</span> fd, <span class="keyword">int</span> request, ...)</span></span></span><br><span class="line"><span class="function"><span class="comment">// 返回：成功返回 0，出错返回 -1</span></span></span><br></pre></td></tr></table></figure><p>第三个参数总是一个指针，但是指针的类型依赖于 request 参数<br>网络相关请求可以分为 6 类：</p><ul><li>套接字操作</li><li>文件操作</li><li>接口操作</li><li>ARP 高速缓存操作</li><li>路由表操作</li><li>流系统</li></ul><p><strong>详细的表见 UNP 图17-1</strong></p><h1 id="17-3-套接字操作"><a href="#17-3-套接字操作" class="headerlink" title="17.3 套接字操作"></a>17.3 套接字操作</h1><p>用于套接字的 ioctl 有三个，都要求 ioctl 的第三个参数指向某一个整数的一个指针</p><ol><li>SIOCATMARK：如果本套接字的都指针当前处于带外标记，那就通过第三个指向的整数返回一个非 0 值；否则返回一个 0 值。POSIX 用函数 sockatmark 替换本请求</li><li>SIOCGPGRP：通过第三个参数指向的整数返回本套接字的进程 ID 或进程组 ID，该 ID 指定本套接字的 SIGIO 或 SIGURG 信号的接收进程。本请求和 fcntl 的 F_GETOWN 命令等效</li><li>SIOCSPGRP：把本套接字的进程 ID 或进程组 ID 设置成由第三个参数指向的整数，该 ID 指定本套接字的 SIGIO 或 SIGURG 信号的接收进程。本请求和 fcntl 的 F_SETOWN 命令等效</li></ol><h1 id="17-4-文件操作"><a href="#17-4-文件操作" class="headerlink" title="17.4 文件操作"></a>17.4 文件操作</h1><p>文件操作的请求以 FIO 开头，它们可能还适用于除套接字外某些特定类型的文件。本节进讨论对于套接字的操作</p><ol><li>FIONBIO：根据 ioctl 的第三个参数指向一个 0 值或非 0 值，可清除或设置本套接字的非阻塞 I/O 标志。本请求和 O_NONBLOCK 文件状态标志等效，可以通过 fcntl 的 F_SETFL 命令清除或设置该标志</li><li>FIOASYNC：根据 ioctl 的第三个参数指向一个 0 值或非 0 值，可清除或设置针对本套接字的信号驱动异步 I/O 标志，它决定是否收取针对本套接字的异步 I/O 信号（SIGIO）。本请求和 O_ASYNC 文件状态标志等效，可以通过 fcntl 的 F_SETFL 命令清除或设置该标志</li><li>FIONREAO：通过由 ioctl 的第三个参数指向的整数返回当前在套接字接收缓冲区中的字节数。本特性同样适用于文件、管道和终端</li><li>FIOSETOWN：对于套接字，和 SIOCSPGRP 等效</li><li>FIOGETOWN：对于套接字，和 SIOCGPGRP 等效</li></ol><h1 id="17-5-接口配置"><a href="#17-5-接口配置" class="headerlink" title="17.5 接口配置"></a>17.5 接口配置</h1><p>&emsp;&emsp;使用 SIOCGIFCONF 请求可以获取所有配置在系统中的接口，它使用 ifconf 结构，ifconf 中又使用 ifreq 结构，内核会返回多个 ifreq 结构，用来返回 ioctl 缓冲区中的信息，我们所需要的信息就在缓冲区中，ifreq 是索引。</p><h1 id="17-6-get-ifi-info-函数"><a href="#17-6-get-ifi-info-函数" class="headerlink" title="17.6 get_ifi_info 函数"></a>17.6 get_ifi_info 函数</h1><p>&emsp;&emsp;UNP 实现了一个 get_ifi_info 函数用来获取系统中的所有接口，返回一个结构链表，每一个结构对应一个打开的接口。使用 SIOCGIFCONF ioctl 实现这个函数。</p><h1 id="17-7-接口操作"><a href="#17-7-接口操作" class="headerlink" title="17.7 接口操作"></a>17.7 接口操作</h1><p>&emsp;&emsp;SIOCGIFCONF 请求给每个已配置的接口返回其名字以及一个套接字地址结构。我们接着可以发出多个接口类其他请求以设置或获取每个接口的其他特征。这些请求的获取版本通常使用 netstat 程序发出，设置版本通常使用 ifconfig 程序发出。任何用户都可以获取，但只有超级用户才可以设置<br>&emsp;&emsp;这些请求接受或返回一个 ifreq 结构中的信息，作为第三个参数。接口总是以其名字标识，在 ifreq 的 ifr_name 中指定，如 le0、lo0、ppp0 等等</p><ol><li>SIOCGIFADDR：在 if_addr 成员中返回单播地址</li><li>SIOCSIFADDR：在 if_addr 成员设置接口地址。这个接口的初始化函数也会被调用</li><li>SIOCGIFFLAGS：用 ifr_flags 成员返回接口标志，标志用 IFF_xx 格式，可以表示接口是否处于工作状态等等</li><li>SIOCSIFFLAGS：用 ifr_flags 成员设置接口标志</li><li>SIOCGIFDSTADDR：用 ifr_dstaddr 成员返回点到点地址</li><li>SIOCSIFDSTADDR：用 ifr_dstaddr 成员设置点到点地址</li><li>SIOCGIFBRDADDR：用 ifr_boardaddr 成员返回广播地址。应用进程需要先获取接口标志，然后发出正确的请求，对于广播接口为 SIOCGIFBRDADDR，对于点到点接口为 SIOCGIFDSTADDR</li><li>SIOCSIFBRDADDR：用 ifr_boardaddr 成员设置广播地址</li><li>SIOCGIFNETMASK：在 if_addr 成员中返回子网掩码</li><li>SIOCSIFNETMASK：在 if_addr 成员中设置子网掩码</li><li>SIOCGIFMETRIC：用 ifr_metric 成员返回接口测度，接口测度由内核为每个接口维护，不过使用它的是路由守护进程 routed。接口测度被 routed 加到跳数上（使得某个接口更不被看好）</li><li>SIOCSIFMETRIC：用 ifr_metric 成员设置路由的接口测度</li></ol><h1 id="17-8-arp-高速缓存操作"><a href="#17-8-arp-高速缓存操作" class="headerlink" title="17.8 arp 高速缓存操作"></a>17.8 arp 高速缓存操作</h1><p>&emsp;&emsp;ARP 高速缓存也通过 ioctl 进行操作，在使用路由域套接字的系统往往改由路由套接字访问 ARP 高速缓存。ioctl 的第三个参数指向 arpreq 结构</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">arpreq</span>&#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr</span> <span class="title">arp_pa</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr</span> <span class="title">arp_ha</span>;</span></span><br><span class="line">    <span class="keyword">int</span> srp_flags;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> ATF_INUSE 0x01</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> ATF_COM   0x02</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> ATF_PERM  0x03</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> ATF_PUBL  0x04</span></span><br></pre></td></tr></table></figure><p>ARP 高速缓存操作有三个请求：</p><ol><li>SIOCSARP：把一个新的表项添加到 ARP 高速缓存中，或修改已经存在的表项。其中 arp_pa 是一个含有 IP 地址和的网际网套接字地址结构，arp_ha 则是一个通用套接字地址结构。</li><li>SIOCDARP：从 ARP 高速缓存中删除一个表项，调用者指定要删除的表项的网际网地址</li><li>SIOCGARP：从 ARP 高速缓存中获取一个表项，调用者指定网际网地址，相应的硬件地址（比如以太网地址）随标志一起返回</li></ol><p><strong>ioctl 没有办法列出 ARP 高速缓存中的所有表项</strong></p><h1 id="17-9-路由表操作"><a href="#17-9-路由表操作" class="headerlink" title="17.9 路由表操作"></a>17.9 路由表操作</h1><p>&emsp;&emsp;某些系统提供两个路由表操作的 ioctl 请求，并要求 ioctl 的第三个参数是指向某个 rtentry 结构的一个指针，该结构定义在 &lt;net/route.h&gt; 中。通常由 route 程序发出。只有超级用户才可以使用。在支持路由域套接字的系统中，这些请求改由路由套接字执行</p><ol><li>SIOCADDRT：往路由表中增加一个表项</li><li>SIOCDELRT：从路由表中删除一个表项</li></ol><p><strong>ioctl 没有办法列出路由表中的所有表项</strong></p><h1 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h1><p>网络编程的相关请求可以分为 6 类：</p><ul><li>套接字操作（是否位于外带标记等）</li><li>文件操作（设置或清除非阻塞标志等）</li><li>接口操作（返回接口列表，获取广播地址等）</li><li>ARP 高速缓存操作（创建，修改，获取，删除）</li><li>路由表操作（增加或删除）</li><li>流系统（31 章笔记）</li></ul><p>&emsp;&emsp;我们将使用其中的套接字操作和文件操作，而接口列表的获取是一个相当常规的操作，我们为此开发了一个完成本操作的函数。只有一些特殊的用途会使用 ioctl 的 ARP 高速缓存操作和路由表操作</p>]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;17-1-概述&quot;&gt;&lt;a href=&quot;#17-1-概述&quot; class=&quot;headerlink&quot; title=&quot;17.1 概述&quot;&gt;&lt;/a&gt;17.1 概述&lt;/h1&gt;&lt;p&gt;&amp;emsp;&amp;emsp;ioctl 函数传统上一直作为那些不适合归入其他精细定义类别的特性的系统接口。POSIX 创造了一系列函数用来代替 icotl，但是它还是必不可少的。网络程序经常在程序启动后执行使用 ioctl 获取所在主机全部网络接口的信息：接口地址，是否支持广播，是否支持多播等等&lt;/p&gt;
    
    </summary>
    
    
      <category term="UNP" scheme="http://yoursite.com/categories/UNP/"/>
    
    
  </entry>
  
  <entry>
    <title>第18章：路由套接字</title>
    <link href="http://yoursite.com/2019/09/25/18.%E8%B7%AF%E7%94%B1%E5%A5%97%E6%8E%A5%E5%AD%97/"/>
    <id>http://yoursite.com/2019/09/25/18.路由套接字/</id>
    <published>2019-09-25T14:54:48.821Z</published>
    <updated>2019-09-28T08:46:38.258Z</updated>
    
    <content type="html"><![CDATA[<h1 id="18-1-概述"><a href="#18-1-概述" class="headerlink" title="18.1 概述"></a>18.1 概述</h1><p>&emsp;&emsp;内核中的 Unix 路由表传统上一直使用 ioctl 命令进行访问，但是没有 ioctl 命令可以获得所有路由表，相反，类似 netstat 等程序通过读取内核的内存获取路由表的内容。</p><ol><li>进程可以通过写出到路由套接字而往内核发送消息。路径的增加和删除采用这种操作实现</li><li>进程可以通过从路由套接字读入而自内核接收消息。内核采用这种操作通知进程已收到并处理一个 ICMP 重定向消息，或者请求外部路由进程解析一个消息</li><li>进程可以使用 sysctl 函数获取所有路由表或所有已配置的接口</li></ol><p><strong>前两种操作需要超级用户权限，最后一种只需要普通用户权限</strong></p><a id="more"></a><h1 id="18-2-数据链路套接字地址结构"><a href="#18-2-数据链路套接字地址结构" class="headerlink" title="18.2 数据链路套接字地址结构"></a>18.2 数据链路套接字地址结构</h1><p>通过路由套接字返回一些消息中含有作为返回值给出的数据链路套接字地址结构，结构如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_dl</span> &#123;</span></span><br><span class="line">  <span class="keyword">uint8_t</span>      sdl_len;</span><br><span class="line">  <span class="keyword">sa_family_t</span>  sdl_family; <span class="comment">/* AF_LINK */</span> </span><br><span class="line">  <span class="keyword">uint16_t</span>     sdl_index; <span class="comment">/* system assigned index, if &gt; 0 */</span> </span><br><span class="line">  <span class="keyword">uint8_t</span>      sdl_type; <span class="comment">/* IFT_ETHER, etc. from  */</span> </span><br><span class="line">  <span class="keyword">uint8_t</span>      sdl_nlen; <span class="comment">/* name length, starting in sdl_data[0] */</span>           </span><br><span class="line">  <span class="keyword">uint8_t</span>      sdl_alen; <span class="comment">/* link-layer address length */</span> </span><br><span class="line">  <span class="keyword">uint8_t</span>      sdl_slen; <span class="comment">/* link-layer selector length */</span> </span><br><span class="line">  <span class="keyword">char</span>         sdl_data[<span class="number">12</span>]; <span class="comment">/* minimum work area, can be larger; </span></span><br><span class="line"><span class="comment">                      contains i/f name and link-layer address */</span> </span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><ul><li>每个接口都有一个唯一的正值索引，返回索引的方法有：if_nametoindex 和 if_nameindex 函数，还有不同协议对应的套接字选项  </li><li>sdl_data 成员含有名字和链路地址。名字从 sdl_data[0] 开始，而且不以空字符结尾。链路层地址从 sdl_data[sdl_nlen] 开始</li><li>数据链路套接字地址结构的长度是可变的。如果链路层地址和名字超过 12 字节，结构将大于 20 字节</li></ul><h1 id="18-3-读和写"><a href="#18-3-读和写" class="headerlink" title="18.3 读和写"></a>18.3 读和写</h1><p>&emsp;&emsp;创建一个路由套接字后，进程可以通过写到该套接字向核和发送命令，通过读自该套接字的内核接受信息。路由域套接字共有 12 个路有消息，其中 5 个可以由进程发出，具体消息和数据结构参照 UNP 图18-2，图18-3<br>&emsp;&emsp;每个结构有相同的前 3 个成员：本消息的长度，版本和类型。</p><h1 id="18-4-sysct-操作"><a href="#18-4-sysct-操作" class="headerlink" title="18.4 sysct 操作"></a>18.4 sysct 操作</h1><p>&emsp;&emsp;我们对路由套接字的主要兴趣在于使用 sysctl 函数检查路由表和接口列表。创建路由套接字（一个 AF_ROUTE 域的原始套接字）需要超级用户权限，然而使用 sysctl 检查路由表和接口列表的进程普通用户也可以操作</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;sys/param.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;sys/sysctl.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sysctl</span><span class="params">(<span class="keyword">int</span>* name, u_int namelen, <span class="keyword">void</span>* oldp, <span class="keyword">size_t</span>* oldlenp, <span class="keyword">void</span> * newp, <span class="keyword">size_t</span> newlen)</span></span>;</span><br><span class="line"><span class="comment">// 返回：若成功返回 0，出错返回 -1</span></span><br></pre></td></tr></table></figure><ul><li>name 参数指定名字的整数数组</li><li>namelen 参数指定数组中的元素数，该数组中的第一个元素指定本请求定向到内核的拿一个子系统，后面的值逐渐细化该子系统的某个部分</li><li>oldp 参数指向一个宫内和存放分层的层级值的缓冲区</li><li>oldlenp 参数指定的值代表该缓冲区的大小</li></ul><p>&emsp;&emsp;sysctl 函数可以获取许多系统信息：文件系统，虚拟内存，内核限制，硬件等等。我们在这里感兴趣的是网络子系统，通过把 name 数组的第一个元素设置为 CTL_NET 来指定。第二个参数可以是下面几种：</p><ul><li>AF_INET：获取或设置影响网际网协议的变量。下一级为使用某个 IPPROTO_xx 常值指定的具体协议</li><li>AF_LINK：获取或设置链路层信息，譬如 PPP 接口的数目</li><li>AF_ROUTE：返回路由表或接口列表的信息</li><li>AF_UNSPEC：获取或设置一些套接字层变量，譬如套接字发送或接收缓冲区的最大大小。</li></ul><p>UNP 小例子：判断 UDP 检验和是否开启</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">"unproute.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;netinet/udp.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;netinet/ip_var.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;netinet/udp_var.h&gt;/* for UDPCTL_xxx constants */</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">int</span>mib[<span class="number">4</span>], val;</span><br><span class="line"><span class="keyword">size_t</span>len;</span><br><span class="line"></span><br><span class="line">mib[<span class="number">0</span>] = CTL_NET;</span><br><span class="line">mib[<span class="number">1</span>] = AF_INET;</span><br><span class="line">mib[<span class="number">2</span>] = IPPROTO_UDP;</span><br><span class="line">mib[<span class="number">3</span>] = UDPCTL_CHECKSUM;</span><br><span class="line"></span><br><span class="line">len = <span class="keyword">sizeof</span>(val);</span><br><span class="line">Sysctl(mib, <span class="number">4</span>, &amp;val, &amp;len, <span class="literal">NULL</span>, <span class="number">0</span>);</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"udp checksum flag: %d\n"</span>, val);</span><br><span class="line"></span><br><span class="line"><span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="18-5-get-ifi-info-函数"><a href="#18-5-get-ifi-info-函数" class="headerlink" title="18.5 get_ifi_info 函数"></a>18.5 get_ifi_info 函数</h1><p>使用路由套接字又把 get_ifi_info 实现了一遍，具体代码见 UNP-18.6</p><h1 id="18-6-接口名字和索引函数"><a href="#18-6-接口名字和索引函数" class="headerlink" title="18.6 接口名字和索引函数"></a>18.6 接口名字和索引函数</h1><p>&emsp;&emsp;有四个函数用于处理接口名字和索引。这四个函数用于需要描述一个接口的地方，适用于 IPv4 和 IPv6。需要注意一个基本概念：每个接口都有唯一的名字和唯一的正值索引（0 从不用做索引）</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;net/if.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">unsigned</span> <span class="keyword">int</span> <span class="title">if_nametoindex</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> * ifname)</span></span>;</span><br><span class="line"><span class="comment">// 返回：成功返回为正值的接口索引，出错返回 0</span></span><br><span class="line"><span class="function"><span class="keyword">char</span>* <span class="title">if_indextoname</span><span class="params">(unisgned <span class="keyword">int</span> ifindex, <span class="keyword">char</span>* ifname)</span></span>;</span><br><span class="line"><span class="comment">// 返回：成功返回指向接口名字的指针，出错返回 NULL</span></span><br><span class="line"><span class="function">struct if_nameindex* <span class="title">if_nameindex</span><span class="params">(<span class="keyword">void</span>)</span></span>;</span><br><span class="line"><span class="comment">// 返回：成功为非空指针，出错返回 NULL</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">if_freenameindex</span><span class="params">(struct if_nameindex* ptr)</span></span></span><br></pre></td></tr></table></figure><ul><li>if_nametoindex 返回名字为 ifname 的接口的索引</li><li>if_indextoname 返回索引为 ifindex 的接口名字</li><li>if_nameindex 返回一个指向 if_nameindex 结构数组的指针</li></ul><p>下面使用路由套接字实现这四个函数：</p><h2 id="if-nametoindex-函数"><a href="#if-nametoindex-函数" class="headerlink" title="if_nametoindex 函数"></a>if_nametoindex 函数</h2><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">unsigned</span> <span class="keyword">int</span> <span class="title">if_nametoindex</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *name)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">int</span>idx, namelen;</span><br><span class="line"><span class="keyword">char</span>*buf, *next, *lim;</span><br><span class="line"><span class="keyword">size_t</span>len;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">if_msghdr</span>*<span class="title">ifm</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr</span>*<span class="title">sa</span>, *<span class="title">rti_info</span>[<span class="title">RTAX_MAX</span>];</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_dl</span>*<span class="title">sdl</span>;</span></span><br><span class="line">  <span class="comment">// net_rt_iflist 函数用来返回接口列表</span></span><br><span class="line"><span class="keyword">if</span> ( (buf = net_rt_iflist(<span class="number">0</span>, <span class="number">0</span>, &amp;len)) == <span class="literal">NULL</span>)</span><br><span class="line"><span class="keyword">return</span>(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">namelen = <span class="built_in">strlen</span>(name);</span><br><span class="line">lim = buf + len;</span><br><span class="line"><span class="keyword">for</span> (next = buf; next &lt; lim; next += ifm-&gt;ifm_msglen) &#123;</span><br><span class="line">    <span class="comment">// 处理缓冲区中的消息，仅仅查找 RTM_IFINFO 消息</span></span><br><span class="line">    <span class="comment">// 找到后调用 get_rtaddrs 函数设置指向各个套接字地址结构的指针</span></span><br><span class="line">    <span class="comment">// 如果存在一个接口名字，那就比较其中的接口名字（rti_info[RTAX_IFP]）和调用者参数中要查找的接口名字</span></span><br><span class="line">ifm = (struct if_msghdr *) next;</span><br><span class="line"><span class="keyword">if</span> (ifm-&gt;ifm_type == RTM_IFINFO) &#123;</span><br><span class="line">sa = (struct sockaddr *) (ifm + <span class="number">1</span>);</span><br><span class="line">get_rtaddrs(ifm-&gt;ifm_addrs, sa, rti_info);</span><br><span class="line"><span class="keyword">if</span> ( (sa = rti_info[RTAX_IFP]) != <span class="literal">NULL</span>) &#123;</span><br><span class="line"><span class="keyword">if</span> (sa-&gt;sa_family == AF_LINK) &#123;</span><br><span class="line">sdl = (struct sockaddr_dl *) sa;</span><br><span class="line"><span class="keyword">if</span> (sdl-&gt;sdl_nlen == namelen &amp;&amp; <span class="built_in">strncmp</span>(&amp;sdl-&gt;sdl_data[<span class="number">0</span>], name, sdl-&gt;sdl_nlen) == <span class="number">0</span>) &#123;</span><br><span class="line">idx = sdl-&gt;sdl_index;<span class="comment">/* save before free() */</span></span><br><span class="line"><span class="built_in">free</span>(buf);</span><br><span class="line"><span class="keyword">return</span>(idx);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">free</span>(buf);</span><br><span class="line"><span class="keyword">return</span>(<span class="number">0</span>);<span class="comment">/* no match for name */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="if-indextoname-函数"><a href="#if-indextoname-函数" class="headerlink" title="if_indextoname 函数"></a>if_indextoname 函数</h2><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 该函数和上一个函数类似，仅仅是比较的类型不一样而已</span></span><br><span class="line"><span class="function"><span class="keyword">char</span> * <span class="title">if_indextoname</span><span class="params">(<span class="keyword">unsigned</span> <span class="keyword">int</span> idx, <span class="keyword">char</span> *name)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">char</span>*buf, *next, *lim;</span><br><span class="line"><span class="keyword">size_t</span>len;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">if_msghdr</span>*<span class="title">ifm</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr</span>*<span class="title">sa</span>, *<span class="title">rti_info</span>[<span class="title">RTAX_MAX</span>];</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_dl</span>*<span class="title">sdl</span>;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> ( (buf = net_rt_iflist(<span class="number">0</span>, idx, &amp;len)) == <span class="literal">NULL</span>)</span><br><span class="line"><span class="keyword">return</span>(<span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line">lim = buf + len;</span><br><span class="line"><span class="keyword">for</span> (next = buf; next &lt; lim; next += ifm-&gt;ifm_msglen) &#123;</span><br><span class="line">ifm = (struct if_msghdr *) next;</span><br><span class="line"><span class="keyword">if</span> (ifm-&gt;ifm_type == RTM_IFINFO) &#123;</span><br><span class="line">sa = (struct sockaddr *) (ifm + <span class="number">1</span>);</span><br><span class="line">get_rtaddrs(ifm-&gt;ifm_addrs, sa, rti_info);</span><br><span class="line"><span class="keyword">if</span> ( (sa = rti_info[RTAX_IFP]) != <span class="literal">NULL</span>) &#123;</span><br><span class="line"><span class="keyword">if</span> (sa-&gt;sa_family == AF_LINK) &#123;</span><br><span class="line">sdl = (struct sockaddr_dl *) sa;</span><br><span class="line"><span class="keyword">if</span> (sdl-&gt;sdl_index == idx) &#123;</span><br><span class="line"><span class="keyword">int</span> slen = min(IFNAMSIZ - <span class="number">1</span>, sdl-&gt;sdl_nlen);</span><br><span class="line"><span class="built_in">strncpy</span>(name, sdl-&gt;sdl_data, slen);</span><br><span class="line">name[slen] = <span class="number">0</span>;<span class="comment">/* null terminate */</span></span><br><span class="line"><span class="built_in">free</span>(buf);</span><br><span class="line"><span class="keyword">return</span>(name);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">free</span>(buf);</span><br><span class="line"><span class="keyword">return</span>(<span class="literal">NULL</span>);<span class="comment">/* no match for index */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="if-nameindex-函数"><a href="#if-nameindex-函数" class="headerlink" title="if_nameindex 函数"></a>if_nameindex 函数</h2><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 返回一个 if_nameindex 结构数组，包含所有接口名字和索引对</span></span><br><span class="line"><span class="function">struct if_nameindex * <span class="title">if_nameindex</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">char</span>*buf, *next, *lim;</span><br><span class="line"><span class="keyword">size_t</span>len;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">if_msghdr</span>*<span class="title">ifm</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr</span>*<span class="title">sa</span>, *<span class="title">rti_info</span>[<span class="title">RTAX_MAX</span>];</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_dl</span>*<span class="title">sdl</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">if_nameindex</span>*<span class="title">result</span>, *<span class="title">ifptr</span>;</span></span><br><span class="line"><span class="keyword">char</span>*namptr;</span><br><span class="line">  <span class="comment">// 构建返回数组，确定大小</span></span><br><span class="line"><span class="keyword">if</span> ( (buf = net_rt_iflist(<span class="number">0</span>, <span class="number">0</span>, &amp;len)) == <span class="literal">NULL</span>)</span><br><span class="line"><span class="keyword">return</span>(<span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> ( (result = <span class="built_in">malloc</span>(len)) == <span class="literal">NULL</span>)<span class="comment">/* overestimate */</span></span><br><span class="line"><span class="keyword">return</span>(<span class="literal">NULL</span>);</span><br><span class="line">ifptr = result;</span><br><span class="line">namptr = (<span class="keyword">char</span> *) result + len;<span class="comment">/* names start at end of buffer */</span></span><br><span class="line"></span><br><span class="line">lim = buf + len;</span><br><span class="line"><span class="keyword">for</span> (next = buf; next &lt; lim; next += ifm-&gt;ifm_msglen) &#123;</span><br><span class="line">    <span class="comment">// 遍历所有消息，从中查找 RTM_IFINFO 消息以及后面的数据链路套接字地址结构。把接口名字和索引放到返回数组中</span></span><br><span class="line">ifm = (struct if_msghdr *) next;</span><br><span class="line"><span class="keyword">if</span> (ifm-&gt;ifm_type == RTM_IFINFO) &#123;</span><br><span class="line">sa = (struct sockaddr *) (ifm + <span class="number">1</span>);</span><br><span class="line">get_rtaddrs(ifm-&gt;ifm_addrs, sa, rti_info);</span><br><span class="line"><span class="keyword">if</span> ( (sa = rti_info[RTAX_IFP]) != <span class="literal">NULL</span>) &#123;</span><br><span class="line"><span class="keyword">if</span> (sa-&gt;sa_family == AF_LINK) &#123;</span><br><span class="line">sdl = (struct sockaddr_dl *) sa;</span><br><span class="line">namptr -= sdl-&gt;sdl_nlen + <span class="number">1</span>;</span><br><span class="line"><span class="built_in">strncpy</span>(namptr, &amp;sdl-&gt;sdl_data[<span class="number">0</span>], sdl-&gt;sdl_nlen);</span><br><span class="line">namptr[sdl-&gt;sdl_nlen] = <span class="number">0</span>;<span class="comment">/* null terminate */</span></span><br><span class="line">ifptr-&gt;if_name = namptr;</span><br><span class="line">ifptr-&gt;if_index = sdl-&gt;sdl_index;</span><br><span class="line">ifptr++;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">  <span class="comment">// 把返回数组的最后一个元素置空</span></span><br><span class="line">ifptr-&gt;if_name = <span class="literal">NULL</span>;<span class="comment">/* mark end of array of structs */</span></span><br><span class="line">ifptr-&gt;if_index = <span class="number">0</span>;</span><br><span class="line"><span class="built_in">free</span>(buf);</span><br><span class="line"><span class="keyword">return</span>(result);<span class="comment">/* caller must free() this when done */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="if-freenameindex-函数"><a href="#if-freenameindex-函数" class="headerlink" title="if_freenameindex 函数"></a>if_freenameindex 函数</h2><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 释放 if_nameindex 结构内存即可，因为是一整块内存，不需要遍历</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">if_freenameindex</span><span class="params">(struct if_nameindex *ptr)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="built_in">free</span>(ptr);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h1><p>&emsp;&emsp;本节所说的 sockaddr_dl 结构，是一种可变长度的数据链路套接字地址结构。以便返回接口索引、名字和硬件地址等不同信息。<br>&emsp;&emsp;进程可以写到路由套接字的消息有 5 个类型，内核通过路由套接字异步返回的消息有 15 个类型。<br>&emsp;&emsp;sysctl 函数是获取和设置操作系统参数的通用方法。我们所关注的 sysctl 操作包括：</p><ul><li>获取所有接口列表</li><li>获取到路由表</li><li>获取 ARP 高速缓存</li></ul><p>&emsp;&emsp;IPv6 要求套接字 API 实施的相关改动包括在接口名字和接口索引之间进行映射的 4 个函数。每个接口都有一个正值索引。系统已经把接口名字和索引值关联起来，我们可以很容易的使用 sysctl 实现接口信息相关函数</p>]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;18-1-概述&quot;&gt;&lt;a href=&quot;#18-1-概述&quot; class=&quot;headerlink&quot; title=&quot;18.1 概述&quot;&gt;&lt;/a&gt;18.1 概述&lt;/h1&gt;&lt;p&gt;&amp;emsp;&amp;emsp;内核中的 Unix 路由表传统上一直使用 ioctl 命令进行访问，但是没有 ioctl 命令可以获得所有路由表，相反，类似 netstat 等程序通过读取内核的内存获取路由表的内容。&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;进程可以通过写出到路由套接字而往内核发送消息。路径的增加和删除采用这种操作实现&lt;/li&gt;
&lt;li&gt;进程可以通过从路由套接字读入而自内核接收消息。内核采用这种操作通知进程已收到并处理一个 ICMP 重定向消息，或者请求外部路由进程解析一个消息&lt;/li&gt;
&lt;li&gt;进程可以使用 sysctl 函数获取所有路由表或所有已配置的接口&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;前两种操作需要超级用户权限，最后一种只需要普通用户权限&lt;/strong&gt;&lt;/p&gt;
    
    </summary>
    
    
      <category term="UNP" scheme="http://yoursite.com/categories/UNP/"/>
    
    
  </entry>
  
  <entry>
    <title>第19章：密钥管理套接字</title>
    <link href="http://yoursite.com/2019/09/25/19.%E5%AF%86%E9%92%A5%E7%AE%A1%E7%90%86%E5%A5%97%E6%8E%A5%E5%AD%97/"/>
    <id>http://yoursite.com/2019/09/25/19.密钥管理套接字/</id>
    <published>2019-09-25T14:54:48.821Z</published>
    <updated>2019-09-28T08:46:38.258Z</updated>
    
    <content type="html"><![CDATA[<h1 id="19-1-概述"><a href="#19-1-概述" class="headerlink" title="19.1 概述"></a>19.1 概述</h1><p>&emsp;&emsp;随着 IP 安全体系结构的引入，私钥体系加密和认证密钥的管理越来越需要一套标准的机制。新创建了一套协议族 PF_KEY 域，在这个密钥管理域中，唯一支持的一种套接字是原始套接字。</p><p>&emsp;&emsp;在普通的 Unix 系统上，密钥管理套接字仅限超级用户有打开的权限。</p><a id="more"></a><h1 id="19-2-读和写"><a href="#19-2-读和写" class="headerlink" title="19.2 读和写"></a>19.2 读和写</h1><h1 id="19-3-倾泻安全关联数据库"><a href="#19-3-倾泻安全关联数据库" class="headerlink" title="19.3 倾泻安全关联数据库"></a>19.3 倾泻安全关联数据库</h1><h1 id="19-4-创建静态安全关联"><a href="#19-4-创建静态安全关联" class="headerlink" title="19.4 创建静态安全关联"></a>19.4 创建静态安全关联</h1><h1 id="19-5-动态维护安全关联"><a href="#19-5-动态维护安全关联" class="headerlink" title="19.5 动态维护安全关联"></a>19.5 动态维护安全关联</h1><h1 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h1>]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;19-1-概述&quot;&gt;&lt;a href=&quot;#19-1-概述&quot; class=&quot;headerlink&quot; title=&quot;19.1 概述&quot;&gt;&lt;/a&gt;19.1 概述&lt;/h1&gt;&lt;p&gt;&amp;emsp;&amp;emsp;随着 IP 安全体系结构的引入，私钥体系加密和认证密钥的管理越来越需要一套标准的机制。新创建了一套协议族 PF_KEY 域，在这个密钥管理域中，唯一支持的一种套接字是原始套接字。&lt;/p&gt;
&lt;p&gt;&amp;emsp;&amp;emsp;在普通的 Unix 系统上，密钥管理套接字仅限超级用户有打开的权限。&lt;/p&gt;
    
    </summary>
    
    
      <category term="UNP" scheme="http://yoursite.com/categories/UNP/"/>
    
    
  </entry>
  
  <entry>
    <title>第20章：广播</title>
    <link href="http://yoursite.com/2019/09/25/20.%E5%B9%BF%E6%92%AD/"/>
    <id>http://yoursite.com/2019/09/25/20.广播/</id>
    <published>2019-09-25T14:54:48.821Z</published>
    <updated>2019-09-28T08:46:38.258Z</updated>
    
    <content type="html"><![CDATA[<h1 id="20-1-概述"><a href="#20-1-概述" class="headerlink" title="20.1 概述"></a>20.1 概述</h1><h3 id="emsp-emsp-emsp-emsp-emsp-emsp-emsp-emsp-emsp-不同类型的寻址方式"><a href="#emsp-emsp-emsp-emsp-emsp-emsp-emsp-emsp-emsp-不同类型的寻址方式" class="headerlink" title="&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;不同类型的寻址方式"></a>&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;不同类型的寻址方式</h3><table><thead><tr><th>类型</th><th>IPv4</th><th>IPv6</th><th>TCP</th><th>UDP</th><th>所标识接口数</th><th>递送到接口数</th></tr></thead><tbody><tr><td>单播</td><td>√</td><td>√</td><td>√</td><td>√</td><td>一个</td><td>一个</td></tr><tr><td>任播</td><td></td><td>√</td><td>尚没有</td><td>√</td><td>一组</td><td>一组中的一个</td></tr><tr><td>组播</td><td>可选</td><td>√</td><td></td><td>√</td><td>全体</td><td>一组中的全体</td></tr><tr><td>广播</td><td>√</td><td></td><td></td><td>√</td><td>全体</td><td>全体</td></tr></tbody></table><ul><li>组播支持在 IPv4 中是可选的，在 IPv6 中却是必须的</li><li>IPv6 不支持广播。任何使用 IPv4 编写的广播程序，在移植到 IPV6 上要换成组播</li><li>广播和组播要求用于 UDP 或原始 IP，他们不能用于 TCP</li></ul><p>&emsp;&emsp;IPv6 往寻址体系结构中增加了任播方式。任播允许从一组通常提供相同服务的主机中选择一个（一般是最近的一个）通过适当的配置路由，并在多个位置往路由协议中注入同一个地址，多个 IPv4 或 IPv6 主机可以提供该地址的任播服务，任播还在逐步地完善中。<br>&emsp;&emsp;广播的用途之一是在本地子网定位一个服务器主机，前提是已经知道这台服务器主机位于本地子网，但并不知道具体的单播地址。这种操作也称为 <strong>资源发现</strong> 。另一个用途是在有多个客户端与单个服务器主机通信的局域网环境中尽量减少分组流通。处于这个目的使用广播的因特网应用有多个例子：</p><ul><li>ARP：ARP 并不是一个用户应用，而是 IPv4 的基本组成之一。ARP 在本地子网上广播一个请获取想要的硬件地址。ARP 使用在链路层不是 IP 层</li><li>DHCP：在认定本地子网存在一个 DHCP 服务器主机的前提下，DCHP 客户主机向广播地址（通常是 255.255.255.255）发送自己的请求</li><li>NTP：NTR 的一种常见使用情形是客户主机配置上待使用的一个或多个服务器主机的 IP 地址，然后以某个频度轮询这些服务器主机。根据由服务器返回的当前时间和到达服务器的 RTT，客户使用更精妙的算法更新本地时钟</li><li>路由守护进程：route 是最早实现且常用的路由守护进程之一，它在一个局域网上广播自己的路由表。这样一来该局域网所有路由器都可以接受这些路由通告，而无须事先为每个路由器配置其邻居路由的 IP 地址</li></ul><p><strong>组播可以代替广播的这两个用途（资源发现、减少网络分组流通）广播有一定的问题</strong></p><a id="more"></a><h1 id="20-2-广播地址"><a href="#20-2-广播地址" class="headerlink" title="20.2 广播地址"></a>20.2 广播地址</h1><p>广播地址例子：</p><ul><li>子网 192.168.42/24 的所有接口的子网定向广播地址是 192.168.42.255 {子网 ID，-1}  </li><li>受限广播地址 {-1， -1} 或 255.255.255.255   </li></ul><p><strong>路由器从不转发目的地址为 255.255.255.255 的 IP 数据报</strong></p><h1 id="20-3-单播和广播的比较"><a href="#20-3-单播和广播的比较" class="headerlink" title="20.3 单播和广播的比较"></a>20.3 单播和广播的比较</h1><p>&emsp;&emsp;单播局域网中其余的主机可以在比较了自己的以太网地址和数据报的目标以太网地址，发现不同就会忽略，只有相同才会沿着协议栈对其进行处理。所以忽略它的是接口而不是主机。但是对于广播，局域网中的所有主机都会对其沿着协议栈进行处理，所以忽略它的是主机<br>&emsp;&emsp;广播存在的问题是：子网上未参与相应广播的所有主机也不得不沿协议栈一路向上完整的处理收取的 UDP 广播数据报，直到该数据报在 UDP 层时被丢弃。另外，子网所有非 IP 主机也不得不在数据链路层接受完整的帧，然后再丢弃它。如果又较高速率产生 IP 广播数据报，可能会严重影响子网其余主机的性能，使用组播来解决这个问题。</p><h1 id="20-4-使用广播的-dg-cli-函数"><a href="#20-4-使用广播的-dg-cli-函数" class="headerlink" title="20.4 使用广播的 dg_cli 函数"></a>20.4 使用广播的 dg_cli 函数</h1><p>修改 dg_cli 函数，使得可以向 UDP 标准 daytime 服务器广播发送请求，然后显示所有应答</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">dg_cli</span><span class="params">(FILE *fp, <span class="keyword">int</span> sockfd, <span class="keyword">const</span> SA *pservaddr, <span class="keyword">socklen_t</span> servlen)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">int</span>n;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span>on = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">char</span>sendline[MAXLINE], recvline[MAXLINE + <span class="number">1</span>];</span><br><span class="line"><span class="keyword">socklen_t</span>len;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr</span>*<span class="title">preply_addr</span>;</span></span><br><span class="line">    <span class="comment">// 给 recvfrom 收到的服务器地址分配空间</span></span><br><span class="line">preply_addr = Malloc(servlen);</span><br><span class="line">    <span class="comment">// 设置 SO_BROADCAST 套接字选项</span></span><br><span class="line">Setsockopt(sockfd, SOL_SOCKET, SO_BROADCAST, &amp;on, <span class="keyword">sizeof</span>(on));</span><br><span class="line">    <span class="comment">// 设置 SIGALRM 的信号处理函数 recvfrom_alarm</span></span><br><span class="line">Signal(SIGALRM, recvfrom_alarm);</span><br><span class="line">    <span class="comment">// 下面发送的是广播数据报，可能会获得多个应答，我们在一个循环中调用 recvfrom，并显示在 5 秒钟内收到应答</span></span><br><span class="line">    <span class="comment">// 5 秒后系统会产生 SIGLARM 信号，导致 recvfrom 返回 EINTR 错误</span></span><br><span class="line"><span class="keyword">while</span> (Fgets(sendline, MAXLINE, fp) != <span class="literal">NULL</span>) &#123;</span><br><span class="line"></span><br><span class="line">Sendto(sockfd, sendline, <span class="built_in">strlen</span>(sendline), <span class="number">0</span>, pservaddr, servlen);</span><br><span class="line"></span><br><span class="line">alarm(<span class="number">5</span>);</span><br><span class="line"><span class="keyword">for</span> ( ; ; ) &#123;</span><br><span class="line">len = servlen;</span><br><span class="line">n = recvfrom(sockfd, recvline, MAXLINE, <span class="number">0</span>, preply_addr, &amp;len);</span><br><span class="line"><span class="keyword">if</span> (n &lt; <span class="number">0</span>) &#123;</span><br><span class="line"><span class="keyword">if</span> (errno == EINTR)</span><br><span class="line"><span class="keyword">break</span>;<span class="comment">/* waited long enough for replies */</span></span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">err_sys(<span class="string">"recvfrom error"</span>);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">// 打印收到的回答</span></span><br><span class="line">recvline[n] = <span class="number">0</span>;<span class="comment">/* null terminate */</span></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"from %s: %s"</span>,</span><br><span class="line">Sock_ntop_host(preply_addr, len), recvline);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">free</span>(preply_addr);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">recvfrom_alarm</span><span class="params">(<span class="keyword">int</span> signo)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">return</span>;<span class="comment">/* just interrupt the recvfrom() */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>&emsp;&emsp;内核不允许对广播数据报进行分片，对于目的地址是广播地址的 IP 数据报，如果其大小超过外出接口 MTU 会返回 EMSGSIZE 错误。原因可能是广播已经给网络造成很大压力，再进行分片，局域网的压力会大大增加。</p><h1 id="20-5-竞争状态"><a href="#20-5-竞争状态" class="headerlink" title="20.5 竞争状态"></a>20.5 竞争状态</h1><p>&emsp;&emsp;对于上一小节的代码，其中存在着一个问题：我们在一个无限 for 循环处理打印程序，通过定时闹钟来 break。但是问题是在 for 循环的任何时候都可能执行信号处理，并不仅仅是我们期望的在 recvfrom 阻塞中进行。所以当正在打印时执行信号处理操作，我们就一直会阻塞在 recvfrom 上了，因为我们唯一可以通过信号处理在 recvfrom 上跳出循环的机会让我们错失在了打印时。  </p><p>下面有 4 种解决办法，第一种是错误的：</p><h2 id="1-阻塞和解阻塞信号（错误版本）："><a href="#1-阻塞和解阻塞信号（错误版本）：" class="headerlink" title="1.阻塞和解阻塞信号（错误版本）："></a>1.阻塞和解阻塞信号（错误版本）：</h2><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">dg_cli</span><span class="params">(FILE *fp, <span class="keyword">int</span> sockfd, <span class="keyword">const</span> SA *pservaddr, <span class="keyword">socklen_t</span> servlen)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">int</span>n;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span>on = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">char</span>sendline[MAXLINE], recvline[MAXLINE + <span class="number">1</span>];</span><br><span class="line"><span class="keyword">sigset_t</span>sigset_alrm;</span><br><span class="line"><span class="keyword">socklen_t</span>len;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr</span>*<span class="title">preply_addr</span>;</span></span><br><span class="line"> </span><br><span class="line">preply_addr = Malloc(servlen);</span><br><span class="line"></span><br><span class="line">Setsockopt(sockfd, SOL_SOCKET, SO_BROADCAST, &amp;on, <span class="keyword">sizeof</span>(on));</span><br><span class="line">    <span class="comment">// 声明一个信号集，把它初始化为空集，再打开 SIGALRM 对应的位</span></span><br><span class="line">Sigemptyset(&amp;sigset_alrm);</span><br><span class="line">Sigaddset(&amp;sigset_alrm, SIGALRM);</span><br><span class="line"></span><br><span class="line">Signal(SIGALRM, recvfrom_alarm);</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> (Fgets(sendline, MAXLINE, fp) != <span class="literal">NULL</span>) &#123;</span><br><span class="line"></span><br><span class="line">Sendto(sockfd, sendline, <span class="built_in">strlen</span>(sendline), <span class="number">0</span>, pservaddr, servlen);</span><br><span class="line"></span><br><span class="line">alarm(<span class="number">5</span>);</span><br><span class="line"><span class="keyword">for</span> ( ; ; ) &#123;</span><br><span class="line">            <span class="comment">// 调用 recvfrom 前，解阻塞 SIGALRM 信号</span></span><br><span class="line">len = servlen;</span><br><span class="line">Sigprocmask(SIG_UNBLOCK, &amp;sigset_alrm, <span class="literal">NULL</span>);</span><br><span class="line">n = recvfrom(sockfd, recvline, MAXLINE, <span class="number">0</span>, preply_addr, &amp;len);</span><br><span class="line">            <span class="comment">// recvfrom 函数结束后，立即阻塞 SIGALRM 信号，直到等到下一次解阻塞，也就是下一次循环开始</span></span><br><span class="line">Sigprocmask(SIG_BLOCK, &amp;sigset_alrm, <span class="literal">NULL</span>);</span><br><span class="line"><span class="keyword">if</span> (n &lt; <span class="number">0</span>) &#123;</span><br><span class="line"><span class="keyword">if</span> (errno == EINTR)</span><br><span class="line"><span class="keyword">break</span>;<span class="comment">/* waited long enough for replies */</span></span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">err_sys(<span class="string">"recvfrom error"</span>);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">recvline[n] = <span class="number">0</span>;<span class="comment">/* null terminate */</span></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"from %s: %s"</span>,</span><br><span class="line">Sock_ntop_host(preply_addr, len), recvline);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">free</span>(preply_addr);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> </span><br><span class="line">recvfrom_alarm(<span class="keyword">int</span> signo)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">return</span>;<span class="comment">/* just interrupt the recvfrom() */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>&emsp;&emsp;这种方式仍然存在问题，就是 recvfrom 和 阻塞 SIGALRM 信号操作都是相互独立的操作，万一，信号在 recvfrom 调用完但是信号还没有阻塞之前进行递交，那么内核就不能阻塞该信号，虽然出错拆给你扣已经很小了，但是问题依然是存在的。</p><h2 id="2-用-pselect-阻塞和解阻塞信号："><a href="#2-用-pselect-阻塞和解阻塞信号：" class="headerlink" title="2.用 pselect 阻塞和解阻塞信号："></a>2.用 pselect 阻塞和解阻塞信号：</h2><p>使用 pselect 来解决这个问题：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">dg_cli</span><span class="params">(FILE *fp, <span class="keyword">int</span> sockfd, <span class="keyword">const</span> SA *pservaddr, <span class="keyword">socklen_t</span> servlen)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">int</span>n;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span>on = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">char</span>sendline[MAXLINE], recvline[MAXLINE + <span class="number">1</span>];</span><br><span class="line">fd_setrset;</span><br><span class="line"><span class="keyword">sigset_t</span>sigset_alrm, sigset_empty;</span><br><span class="line"><span class="keyword">socklen_t</span>len;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr</span>*<span class="title">preply_addr</span>;</span></span><br><span class="line"> </span><br><span class="line">preply_addr = Malloc(servlen);</span><br><span class="line"></span><br><span class="line">Setsockopt(sockfd, SOL_SOCKET, SO_BROADCAST, &amp;on, <span class="keyword">sizeof</span>(on));</span><br><span class="line"></span><br><span class="line">FD_ZERO(&amp;rset);</span><br><span class="line"></span><br><span class="line">Sigemptyset(&amp;sigset_empty);</span><br><span class="line">Sigemptyset(&amp;sigset_alrm);</span><br><span class="line">Sigaddset(&amp;sigset_alrm, SIGALRM);</span><br><span class="line"></span><br><span class="line">Signal(SIGALRM, recvfrom_alarm);</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> (Fgets(sendline, MAXLINE, fp) != <span class="literal">NULL</span>) &#123;</span><br><span class="line">Sendto(sockfd, sendline, <span class="built_in">strlen</span>(sendline), <span class="number">0</span>, pservaddr, servlen);</span><br><span class="line">        <span class="comment">// 阻塞 SIGALRM 并调用 pselect</span></span><br><span class="line">Sigprocmask(SIG_BLOCK, &amp;sigset_alrm, <span class="literal">NULL</span>);</span><br><span class="line">alarm(<span class="number">5</span>);</span><br><span class="line"><span class="keyword">for</span> ( ; ; ) &#123;</span><br><span class="line">FD_SET(sockfd, &amp;rset);</span><br><span class="line">            <span class="comment">// 调用 pselect 时，信号都是非阻塞的。调用完成，再恢复成被调用时的状态</span></span><br><span class="line">n = pselect(sockfd+<span class="number">1</span>, &amp;rset, <span class="literal">NULL</span>, <span class="literal">NULL</span>, <span class="literal">NULL</span>, &amp;sigset_empty);</span><br><span class="line"><span class="keyword">if</span> (n &lt; <span class="number">0</span>) &#123;</span><br><span class="line"><span class="keyword">if</span> (errno == EINTR)</span><br><span class="line"><span class="keyword">break</span>;</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">err_sys(<span class="string">"pselect error"</span>);</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (n != <span class="number">1</span>)</span><br><span class="line">err_sys(<span class="string">"pselect error: returned %d"</span>, n);</span><br><span class="line">            <span class="comment">// 套接字变为可读，直接调用 recvfrom</span></span><br><span class="line">len = servlen;</span><br><span class="line">n = Recvfrom(sockfd, recvline, MAXLINE, <span class="number">0</span>, preply_addr, &amp;len);</span><br><span class="line">recvline[n] = <span class="number">0</span>;<span class="comment">/* null terminate */</span></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"from %s: %s"</span>,</span><br><span class="line">Sock_ntop_host(preply_addr, len), recvline);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">free</span>(preply_addr);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span></span><br><span class="line">recvfrom_alarm(<span class="keyword">int</span> signo)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">return</span>;<span class="comment">/* just interrupt the recvfrom() */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="3-使用-sigsetjmp-和-siglongjmp："><a href="#3-使用-sigsetjmp-和-siglongjmp：" class="headerlink" title="3.使用 sigsetjmp 和 siglongjmp："></a>3.使用 sigsetjmp 和 siglongjmp：</h2><p>&emsp;&emsp;解决静态问题还可以不使用信号来进行中断被阻塞系统调用并跳出循环。在是在信号处理函数中调用 siglongjmp 函数，siglongjmp 是非局部跳转，使用它可以从一个函数跳转到另一个函数</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span><span class="title">recvfrom_alarm</span><span class="params">(<span class="keyword">int</span>)</span></span>;</span><br><span class="line"><span class="comment">// 本函数和信号处理函数使用的跳转缓冲区</span></span><br><span class="line"><span class="keyword">static</span> sigjmp_bufjmpbuf;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">dg_cli</span><span class="params">(FILE *fp, <span class="keyword">int</span> sockfd, <span class="keyword">const</span> SA *pservaddr, <span class="keyword">socklen_t</span> servlen)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">int</span>n;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span>on = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">char</span>sendline[MAXLINE], recvline[MAXLINE + <span class="number">1</span>];</span><br><span class="line"><span class="keyword">socklen_t</span>len;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr</span>*<span class="title">preply_addr</span>;</span></span><br><span class="line"> </span><br><span class="line">preply_addr = Malloc(servlen);</span><br><span class="line"></span><br><span class="line">Setsockopt(sockfd, SOL_SOCKET, SO_BROADCAST, &amp;on, <span class="keyword">sizeof</span>(on));</span><br><span class="line"></span><br><span class="line">Signal(SIGALRM, recvfrom_alarm);</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> (Fgets(sendline, MAXLINE, fp) != <span class="literal">NULL</span>) &#123;</span><br><span class="line"></span><br><span class="line">Sendto(sockfd, sendline, <span class="built_in">strlen</span>(sendline), <span class="number">0</span>, pservaddr, servlen);</span><br><span class="line"></span><br><span class="line">alarm(<span class="number">5</span>);</span><br><span class="line"><span class="keyword">for</span> ( ; ; ) &#123;</span><br><span class="line">            <span class="comment">// 从 dg_cli 函数中直接调用 sigsetjmp 时，它在建立跳转缓冲区后返回 0，接着调用 recvfrom。</span></span><br><span class="line"><span class="keyword">if</span> (sigsetjmp(jmpbuf, <span class="number">1</span>) != <span class="number">0</span>)</span><br><span class="line"><span class="keyword">break</span>;</span><br><span class="line">len = servlen;</span><br><span class="line">n = Recvfrom(sockfd, recvline, MAXLINE, <span class="number">0</span>, preply_addr, &amp;len);</span><br><span class="line">recvline[n] = <span class="number">0</span>;<span class="comment">/* null terminate */</span></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"from %s: %s"</span>,</span><br><span class="line">Sock_ntop_host(preply_addr, len), recvline);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">free</span>(preply_addr);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span></span><br><span class="line">recvfrom_alarm(<span class="keyword">int</span> signo)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 调用 siglongjmp 使 dg_cli 进行跳转</span></span><br><span class="line">siglongjmp(jmpbuf, <span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="4-使用从信号处理函数到主控函数的-IPC："><a href="#4-使用从信号处理函数到主控函数的-IPC：" class="headerlink" title="4.使用从信号处理函数到主控函数的 IPC："></a>4.使用从信号处理函数到主控函数的 IPC：</h2><p>&emsp;&emsp;通过信号通知系统 IPC 再通过 IPC 管道通知 dg_cli 中循环跳出，这里不贴代码了</p><h1 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h1><p>&emsp;&emsp;广播发送的数据报由子网上所有终端主机接收。广播的劣势在于同一个子网上所有主机都必须处理数据报，若是 UDP 数据报则需要沿着协议栈一直处理到 UDP 层，即使不参与广播也需要处理，运行大量广播的应用，系统负担较大。使用组播解决这个问题，因为组播发送的数据只会对相应组播应用感兴趣的主机接收处理。  </p><p>&emsp;解决竞争状态的几种方式：</p><ul><li>使用 pselect</li><li>使用 sigsetjmp 和 siglongjmp</li><li>使用从信号处理函数到主循环的 IPC（典型为管道）</li></ul>]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;20-1-概述&quot;&gt;&lt;a href=&quot;#20-1-概述&quot; class=&quot;headerlink&quot; title=&quot;20.1 概述&quot;&gt;&lt;/a&gt;20.1 概述&lt;/h1&gt;&lt;h3 id=&quot;emsp-emsp-emsp-emsp-emsp-emsp-emsp-emsp-emsp-不同类型的寻址方式&quot;&gt;&lt;a href=&quot;#emsp-emsp-emsp-emsp-emsp-emsp-emsp-emsp-emsp-不同类型的寻址方式&quot; class=&quot;headerlink&quot; title=&quot;&amp;emsp;&amp;emsp;&amp;emsp;&amp;emsp;&amp;emsp;&amp;emsp;&amp;emsp;&amp;emsp;&amp;emsp;不同类型的寻址方式&quot;&gt;&lt;/a&gt;&amp;emsp;&amp;emsp;&amp;emsp;&amp;emsp;&amp;emsp;&amp;emsp;&amp;emsp;&amp;emsp;&amp;emsp;不同类型的寻址方式&lt;/h3&gt;&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;类型&lt;/th&gt;
&lt;th&gt;IPv4&lt;/th&gt;
&lt;th&gt;IPv6&lt;/th&gt;
&lt;th&gt;TCP&lt;/th&gt;
&lt;th&gt;UDP&lt;/th&gt;
&lt;th&gt;所标识接口数&lt;/th&gt;
&lt;th&gt;递送到接口数&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;&lt;tr&gt;
&lt;td&gt;单播&lt;/td&gt;
&lt;td&gt;√&lt;/td&gt;
&lt;td&gt;√&lt;/td&gt;
&lt;td&gt;√&lt;/td&gt;
&lt;td&gt;√&lt;/td&gt;
&lt;td&gt;一个&lt;/td&gt;
&lt;td&gt;一个&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;任播&lt;/td&gt;
&lt;td&gt;&lt;/td&gt;
&lt;td&gt;√&lt;/td&gt;
&lt;td&gt;尚没有&lt;/td&gt;
&lt;td&gt;√&lt;/td&gt;
&lt;td&gt;一组&lt;/td&gt;
&lt;td&gt;一组中的一个&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;组播&lt;/td&gt;
&lt;td&gt;可选&lt;/td&gt;
&lt;td&gt;√&lt;/td&gt;
&lt;td&gt;&lt;/td&gt;
&lt;td&gt;√&lt;/td&gt;
&lt;td&gt;全体&lt;/td&gt;
&lt;td&gt;一组中的全体&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;广播&lt;/td&gt;
&lt;td&gt;√&lt;/td&gt;
&lt;td&gt;&lt;/td&gt;
&lt;td&gt;&lt;/td&gt;
&lt;td&gt;√&lt;/td&gt;
&lt;td&gt;全体&lt;/td&gt;
&lt;td&gt;全体&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;&lt;/table&gt;
&lt;ul&gt;
&lt;li&gt;组播支持在 IPv4 中是可选的，在 IPv6 中却是必须的&lt;/li&gt;
&lt;li&gt;IPv6 不支持广播。任何使用 IPv4 编写的广播程序，在移植到 IPV6 上要换成组播&lt;/li&gt;
&lt;li&gt;广播和组播要求用于 UDP 或原始 IP，他们不能用于 TCP&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&amp;emsp;&amp;emsp;IPv6 往寻址体系结构中增加了任播方式。任播允许从一组通常提供相同服务的主机中选择一个（一般是最近的一个）通过适当的配置路由，并在多个位置往路由协议中注入同一个地址，多个 IPv4 或 IPv6 主机可以提供该地址的任播服务，任播还在逐步地完善中。&lt;br&gt;&amp;emsp;&amp;emsp;广播的用途之一是在本地子网定位一个服务器主机，前提是已经知道这台服务器主机位于本地子网，但并不知道具体的单播地址。这种操作也称为 &lt;strong&gt;资源发现&lt;/strong&gt; 。另一个用途是在有多个客户端与单个服务器主机通信的局域网环境中尽量减少分组流通。处于这个目的使用广播的因特网应用有多个例子：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;ARP：ARP 并不是一个用户应用，而是 IPv4 的基本组成之一。ARP 在本地子网上广播一个请获取想要的硬件地址。ARP 使用在链路层不是 IP 层&lt;/li&gt;
&lt;li&gt;DHCP：在认定本地子网存在一个 DHCP 服务器主机的前提下，DCHP 客户主机向广播地址（通常是 255.255.255.255）发送自己的请求&lt;/li&gt;
&lt;li&gt;NTP：NTR 的一种常见使用情形是客户主机配置上待使用的一个或多个服务器主机的 IP 地址，然后以某个频度轮询这些服务器主机。根据由服务器返回的当前时间和到达服务器的 RTT，客户使用更精妙的算法更新本地时钟&lt;/li&gt;
&lt;li&gt;路由守护进程：route 是最早实现且常用的路由守护进程之一，它在一个局域网上广播自己的路由表。这样一来该局域网所有路由器都可以接受这些路由通告，而无须事先为每个路由器配置其邻居路由的 IP 地址&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;组播可以代替广播的这两个用途（资源发现、减少网络分组流通）广播有一定的问题&lt;/strong&gt;&lt;/p&gt;
    
    </summary>
    
    
      <category term="UNP" scheme="http://yoursite.com/categories/UNP/"/>
    
    
  </entry>
  
  <entry>
    <title>第21章：组播</title>
    <link href="http://yoursite.com/2019/09/25/21.%E7%BB%84%E6%92%AD/"/>
    <id>http://yoursite.com/2019/09/25/21.组播/</id>
    <published>2019-09-25T14:54:48.821Z</published>
    <updated>2019-09-28T08:46:38.258Z</updated>
    
    <content type="html"><![CDATA[<h1 id="21-1-概述"><a href="#21-1-概述" class="headerlink" title="21.1 概述"></a>21.1 概述</h1><p>单播地址：单个 IP 接口<br>组播地址：一组 IP 接口<br>广播地址：所有 IP 接口（当前局域网中）  </p><p>&emsp;&emsp;组播是单播和广播的一种折中方案。组播数据报仅由对它感兴趣的接口才会接收，由运行相应组播会话应用系统的主机上的接口接收。广播一般仅在局域网中进行使用，组播即可以支持局域网又支持广域网。<br>&emsp;&emsp;套接字 API 对组播的支持增加了 9 个套接字选项，其中 3 个影响目的地址为组播地址的 UDP 数据报的发送，另外 6 个影响主机对于组播数据报的接收。<br>&emsp;&emsp;为了接收目的地址为某个组地址且目的端口为某个端口的多播数据组，进程必须加入该多播组，并捆绑该端口到某个UDP套接字。此两操作是截然不同的，不过都是必需的。多播组加入操作告知所在主机的IP层和数据链路层接收发往该组的多播数据报。端口捆绑操作则是应用进程向UDP指示它想接收发往该端口的数据报的手段。</p><a id="more"></a><h1 id="21-2-组播地址"><a href="#21-2-组播地址" class="headerlink" title="21.2 组播地址"></a>21.2 组播地址</h1><p>我们需要了解 IPv4 的组播和 IPv6 的组播的不同</p><h2 id="IPv4-的-D-类地址"><a href="#IPv4-的-D-类地址" class="headerlink" title="IPv4 的 D 类地址"></a>IPv4 的 D 类地址</h2><p>IPv4 的 D 类地址（从 224.0.0.0 到 239.255.255.255）是 IPv4 组播地址。D 类地址的低序 28 位构成组播组 ID，整个 32 位地址称为组地址。<br>IPv4 的以太网多播地址的高 24 位总是 01:00:5e.下一位总是 0，底序的 23 位复制自多播组 ID 的低序 23 位。多播组 ID 的高序 5 位在映射过程中被忽略。这意谓着 32 个多播地址映射成单个以太网地址，因此这个映射关系不是一对一的。<br>以太网地址首字节的低序 2 位标明地址是一个统一管理的组地址。<strong>统一管理</strong>属性位意味着以太网地址的高序 24 位由 IEEE 分配，组地址属性位由接收接口识别并进行特殊处理。  </p><h3 id="几个特殊的-IPv4-多播地址："><a href="#几个特殊的-IPv4-多播地址：" class="headerlink" title="几个特殊的 IPv4 多播地址："></a>几个特殊的 IPv4 多播地址：</h3><ul><li>224.0.0.1 是所有主机组。子网上所有具有多播能力的节点（主机，路由器或打印机等）必须在所有具有多播能力的接口上加入该组。</li><li>224.0.0.2 是所有路由器组。子网上所有多播路由器必须在所有具有多播能力的接口上加上该组  </li></ul><p><strong>介于 224.0.0.0 - 224.0.0.255 之间的地址（224.0.0.0/24）称为链路局部的组播地址。这些地址式为低级拓扑发现和维护协议保留的。多播路由器从不转发以些地址为目的的地址的数据报。</strong>  </p><h2 id="IPv6-多播地址"><a href="#IPv6-多播地址" class="headerlink" title="IPv6 多播地址"></a>IPv6 多播地址</h2><p>IPv6 组播地址的高序字节值为 ff。其中 112 位组 ID 的低序 32 位复制到以太网地址的低序 32 位。以太网地址的高序 2 字节为 33:33。<br>以太网地址首字节低序 2 位标明该地址是一个局部管理的组地址。<strong>局部管理</strong>意味着不能保证该地址对于 IPv6 的唯一性。可能有 IPv6 以外的其它的协议族共享同一网络使用同样的以太网高位 2 字节值。  </p><h2 id="多播地址的范围："><a href="#多播地址的范围：" class="headerlink" title="多播地址的范围："></a>多播地址的范围：</h2><p>IPv6 多播地址显示的存在一个 4 位的范围字段，用于指定多播数据报能够游走的范围。IPv6 分组还有一个跳限字段，用于限制分组被路由器转发的次数</p><h2 id="多播会话："><a href="#多播会话：" class="headerlink" title="多播会话："></a>多播会话：</h2><p>一个多播地址（IPv4 or IPv6 地址）和一个传输层端口（通常是 UDP 端口）组合为一个会话。举例来说，一个音频/视频电话可能使用两个会话，一个用于音频，一个用于视频，这些会话一般使用相同的端口</p><h1 id="21-3-局域网上组播和广播的比较"><a href="#21-3-局域网上组播和广播的比较" class="headerlink" title="21.3 局域网上组播和广播的比较"></a>21.3 局域网上组播和广播的比较</h1><p>接收端需要加入对应的组播，但是发送端并不需要。</p><h1 id="21-4-广域网上的组播"><a href="#21-4-广域网上的组播" class="headerlink" title="21.4 广域网上的组播"></a>21.4 广域网上的组播</h1><h1 id="21-5-原特定组播"><a href="#21-5-原特定组播" class="headerlink" title="21.5 原特定组播"></a>21.5 原特定组播</h1><h1 id="21-6-组播套接字选项"><a href="#21-6-组播套接字选项" class="headerlink" title="21.6 组播套接字选项"></a>21.6 组播套接字选项</h1><p>关于组播的套接字选项这里不做展示，参看 UNP-21.6节</p><h1 id="21-7-mcast-join-和相关函数"><a href="#21-7-mcast-join-和相关函数" class="headerlink" title="21.7 mcast_join 和相关函数"></a>21.7 mcast_join 和相关函数</h1><h1 id="21-8-使用组播的-dg-cli-函数"><a href="#21-8-使用组播的-dg-cli-函数" class="headerlink" title="21.8 使用组播的 dg_cli 函数"></a>21.8 使用组播的 dg_cli 函数</h1><h1 id="21-9-接收-ip-组播基础设施会话声明"><a href="#21-9-接收-ip-组播基础设施会话声明" class="headerlink" title="21.9 接收 ip 组播基础设施会话声明"></a>21.9 接收 ip 组播基础设施会话声明</h1><h1 id="21-10-发送和接受"><a href="#21-10-发送和接受" class="headerlink" title="21.10 发送和接受"></a>21.10 发送和接受</h1><h1 id="21-11-sntp：简单网络时间协议"><a href="#21-11-sntp：简单网络时间协议" class="headerlink" title="21.11 sntp：简单网络时间协议"></a>21.11 sntp：简单网络时间协议</h1><h1 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h1>]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;21-1-概述&quot;&gt;&lt;a href=&quot;#21-1-概述&quot; class=&quot;headerlink&quot; title=&quot;21.1 概述&quot;&gt;&lt;/a&gt;21.1 概述&lt;/h1&gt;&lt;p&gt;单播地址：单个 IP 接口&lt;br&gt;组播地址：一组 IP 接口&lt;br&gt;广播地址：所有 IP 接口（当前局域网中）  &lt;/p&gt;
&lt;p&gt;&amp;emsp;&amp;emsp;组播是单播和广播的一种折中方案。组播数据报仅由对它感兴趣的接口才会接收，由运行相应组播会话应用系统的主机上的接口接收。广播一般仅在局域网中进行使用，组播即可以支持局域网又支持广域网。&lt;br&gt;&amp;emsp;&amp;emsp;套接字 API 对组播的支持增加了 9 个套接字选项，其中 3 个影响目的地址为组播地址的 UDP 数据报的发送，另外 6 个影响主机对于组播数据报的接收。&lt;br&gt;&amp;emsp;&amp;emsp;为了接收目的地址为某个组地址且目的端口为某个端口的多播数据组，进程必须加入该多播组，并捆绑该端口到某个UDP套接字。此两操作是截然不同的，不过都是必需的。多播组加入操作告知所在主机的IP层和数据链路层接收发往该组的多播数据报。端口捆绑操作则是应用进程向UDP指示它想接收发往该端口的数据报的手段。&lt;/p&gt;
    
    </summary>
    
    
      <category term="UNP" scheme="http://yoursite.com/categories/UNP/"/>
    
    
  </entry>
  
  <entry>
    <title>第22章：高级UDP套接字编程</title>
    <link href="http://yoursite.com/2019/09/25/22.%E9%AB%98%E7%BA%A7UDP%E5%A5%97%E6%8E%A5%E5%AD%97%E7%BC%96%E7%A8%8B/"/>
    <id>http://yoursite.com/2019/09/25/22.高级UDP套接字编程/</id>
    <published>2019-09-25T14:54:48.821Z</published>
    <updated>2019-09-28T08:46:38.258Z</updated>
    
    <content type="html"><![CDATA[<h1 id="22-1-概述"><a href="#22-1-概述" class="headerlink" title="22.1 概述"></a>22.1 概述</h1><p>&emsp;&emsp;首先确定某个外来 UDP 数据报的目的地址和接收端口，因为绑定某个 UDP 端口和通配地址的一个套接字能够在任何接口上接受单播，广播和组播数据报。<br>&emsp;&emsp;TCP 是一个字节流协议，又使用滑动窗口，因此没有记录边界和接收端速度慢于发送端的情况。但是 UDP 却需要程序对这些进行处理。<br>&emsp;&emsp;如果实现不支持 IP_RECVDSTADDR 套接字选项，那么确定外来 UDP 数据报目的地址的方法之一是捆绑所有接口地址并使用 select。</p><a id="more"></a><h1 id="22-2-接收标志、目的-ip-地址和接口索引"><a href="#22-2-接收标志、目的-ip-地址和接口索引" class="headerlink" title="22.2 接收标志、目的 ip 地址和接口索引"></a>22.2 接收标志、目的 ip 地址和接口索引</h1><p>过去 sendmsg 和 recvmsg 一直用于通过 Unix 域套接字传递描述符。但随着改变，使用方式也在变化：</p><ul><li>在 msghdr 结构加入 msg_flags 用来返回标志该进程</li><li>辅助数据越来越多的被使用</li></ul><p>我们会实现一个类似 recvmsg 的 recvfrom_flags 函数，区别是新的函数还返回：</p><ul><li>所返回的 msg_flags 值</li><li>所收取数据报的目的地址（通过 IP_RECVDSTADDR 套接字选项获取）</li><li>所收取数据报接受接口的索引（通过 IP_RECVIF 套接字选项获取）<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 该结构体用来返回最后两项</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">unp_in_pktinfo</span>&#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">in_addr</span> <span class="title">ipi_addr</span>;</span></span><br><span class="line">    <span class="keyword">int</span>            ipi_ifindex;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// 前三个参数类似于 recvfrom，后面的参数用来返回上面提到的项</span></span><br><span class="line"><span class="keyword">ssize_t</span> recvfrom_flags(<span class="keyword">int</span> fd, <span class="keyword">void</span> *ptr, <span class="keyword">size_t</span> nbytes, <span class="keyword">int</span> *flagsp,</span><br><span class="line">   SA *sa, <span class="keyword">socklen_t</span> *salenptr, struct unp_in_pktinfo *pktp)</span><br><span class="line">&#123;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">msghdr</span><span class="title">msg</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">iovec</span><span class="title">iov</span>[1];</span></span><br><span class="line"><span class="keyword">ssize_t</span>n;</span><br><span class="line"><span class="comment">// 不同的实现差异</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span>HAVE_MSGHDR_MSG_CONTROL</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">cmsghdr</span>*<span class="title">cmptr</span>;</span></span><br><span class="line"><span class="keyword">union</span> &#123;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">cmsghdr</span><span class="title">cm</span>;</span></span><br><span class="line">  <span class="keyword">char</span>control[CMSG_SPACE(<span class="keyword">sizeof</span>(struct in_addr)) +</span><br><span class="line">CMSG_SPACE(<span class="keyword">sizeof</span>(struct unp_in_pktinfo))];</span><br><span class="line">&#125; control_un;</span><br><span class="line"></span><br><span class="line">msg.msg_control = control_un.control;</span><br><span class="line">msg.msg_controllen = <span class="keyword">sizeof</span>(control_un.control);</span><br><span class="line">msg.msg_flags = <span class="number">0</span>;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">else</span></span></span><br><span class="line">bzero(&amp;msg, <span class="keyword">sizeof</span>(msg));<span class="comment">/* make certain msg_accrightslen = 0 */</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"><span class="comment">// 填写一个 msghdr 结构并用来调用 recvmsg</span></span><br><span class="line">msg.msg_name = sa;</span><br><span class="line">msg.msg_namelen = *salenptr;</span><br><span class="line">iov[<span class="number">0</span>].iov_base = ptr;</span><br><span class="line">iov[<span class="number">0</span>].iov_len = nbytes;</span><br><span class="line">msg.msg_iov = iov;</span><br><span class="line">msg.msg_iovlen = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> ( (n = recvmsg(fd, &amp;msg, *flagsp)) &lt; <span class="number">0</span>)</span><br><span class="line"><span class="keyword">return</span>(n);</span><br><span class="line"></span><br><span class="line">*salenptr = msg.msg_namelen;<span class="comment">/* pass back results */</span></span><br><span class="line"><span class="keyword">if</span> (pktp)</span><br><span class="line">bzero(pktp, <span class="keyword">sizeof</span>(struct unp_in_pktinfo));<span class="comment">/* 0.0.0.0, i/f = 0 */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifndef</span>HAVE_MSGHDR_MSG_CONTROL</span></span><br><span class="line">*flagsp = <span class="number">0</span>;<span class="comment">/* pass back results */</span></span><br><span class="line"><span class="keyword">return</span>(n);</span><br><span class="line"><span class="meta">#<span class="meta-keyword">else</span></span></span><br><span class="line"><span class="comment">// 返回 msg_flags 的成员值</span></span><br><span class="line">*flagsp = msg.msg_flags;</span><br><span class="line"><span class="keyword">if</span> (msg.msg_controllen &lt; <span class="keyword">sizeof</span>(struct cmsghdr) ||</span><br><span class="line">(msg.msg_flags &amp; MSG_CTRUNC) || pktp == <span class="literal">NULL</span>)</span><br><span class="line"><span class="keyword">return</span>(n);</span><br><span class="line"><span class="comment">// 处理辅助数据</span></span><br><span class="line"><span class="keyword">for</span> (cmptr = CMSG_FIRSTHDR(&amp;msg); cmptr != <span class="literal">NULL</span>;</span><br><span class="line"> cmptr = CMSG_NXTHDR(&amp;msg, cmptr)) &#123;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span>IP_RECVDSTADDR</span></span><br><span class="line"><span class="comment">// 如果目的 IP 地址作为控制信息返回，就返回给调用者</span></span><br><span class="line"><span class="keyword">if</span> (cmptr-&gt;cmsg_level == IPPROTO_IP &amp;&amp;</span><br><span class="line">cmptr-&gt;cmsg_type == IP_RECVDSTADDR) &#123;</span><br><span class="line"></span><br><span class="line"><span class="built_in">memcpy</span>(&amp;pktp-&gt;ipi_addr, CMSG_DATA(cmptr),</span><br><span class="line">   <span class="keyword">sizeof</span>(struct in_addr));</span><br><span class="line"><span class="keyword">continue</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span>IP_RECVIF</span></span><br><span class="line"><span class="keyword">if</span> (cmptr-&gt;cmsg_level == IPPROTO_IP &amp;&amp;</span><br><span class="line">cmptr-&gt;cmsg_type == IP_RECVIF) &#123;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_dl</span>*<span class="title">sdl</span>;</span></span><br><span class="line"></span><br><span class="line">sdl = (struct sockaddr_dl *) CMSG_DATA(cmptr);</span><br><span class="line">pktp-&gt;ipi_ifindex = sdl-&gt;sdl_index;</span><br><span class="line"><span class="keyword">continue</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line">err_quit(<span class="string">"unknown ancillary data, len = %d, level = %d, type = %d"</span>,</span><br><span class="line"> cmptr-&gt;cmsg_len, cmptr-&gt;cmsg_level, cmptr-&gt;cmsg_type);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span>(n);</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span><span class="comment">/* HAVE_MSGHDR_MSG_CONTROL */</span></span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">ssize_t</span> Recvfrom_flags(<span class="keyword">int</span> fd, <span class="keyword">void</span> *ptr, <span class="keyword">size_t</span> nbytes, <span class="keyword">int</span> *flagsp,</span><br><span class="line">   SA *sa, <span class="keyword">socklen_t</span> *salenptr, struct unp_in_pktinfo *pktp)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">ssize_t</span>n;</span><br><span class="line"></span><br><span class="line">n = recvfrom_flags(fd, ptr, nbytes, flagsp, sa, salenptr, pktp);</span><br><span class="line"><span class="keyword">if</span> (n &lt; <span class="number">0</span>)</span><br><span class="line">err_quit(<span class="string">"recvfrom_flags error"</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span>(n);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h3 id="修改-dg-echo-来输出目的-IP-地址和数据报截断标志"><a href="#修改-dg-echo-来输出目的-IP-地址和数据报截断标志" class="headerlink" title="修改 dg_echo 来输出目的 IP 地址和数据报截断标志"></a>修改 dg_echo 来输出目的 IP 地址和数据报截断标志</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">undef</span>MAXLINE</span></span><br><span class="line"><span class="comment">// 测试更大的数据报，用来测试截断标志</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>MAXLINE20<span class="comment">/* to see datagram truncation */</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span></span><br><span class="line">dg_echo(<span class="keyword">int</span> sockfd, SA *pcliaddr, <span class="keyword">socklen_t</span> clilen)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">int</span>flags;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span>on = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">socklen_t</span>len;</span><br><span class="line"><span class="keyword">ssize_t</span>n;</span><br><span class="line"><span class="keyword">char</span>mesg[MAXLINE], str[INET6_ADDRSTRLEN],</span><br><span class="line">ifname[IFNAMSIZ];</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">in_addr</span><span class="title">in_zero</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">unp_in_pktinfo</span><span class="title">pktinfo</span>;</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span>IP_RECVDSTADDR</span></span><br><span class="line"><span class="comment">// 开启 IP_RECVDSTADDR 和 IP_RECVIF 套接字选项</span></span><br><span class="line"><span class="keyword">if</span> (setsockopt(sockfd, IPPROTO_IP, IP_RECVDSTADDR, &amp;on, <span class="keyword">sizeof</span>(on)) &lt; <span class="number">0</span>)</span><br><span class="line">err_ret(<span class="string">"setsockopt of IP_RECVDSTADDR"</span>);</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span>IP_RECVIF</span></span><br><span class="line"><span class="keyword">if</span> (setsockopt(sockfd, IPPROTO_IP, IP_RECVIF, &amp;on, <span class="keyword">sizeof</span>(on)) &lt; <span class="number">0</span>)</span><br><span class="line">err_ret(<span class="string">"setsockopt of IP_RECVIF"</span>);</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line">bzero(&amp;in_zero, <span class="keyword">sizeof</span>(struct in_addr));<span class="comment">/* all 0 IPv4 address */</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> ( ; ; ) &#123;</span><br><span class="line">len = clilen;</span><br><span class="line">flags = <span class="number">0</span>;</span><br><span class="line">n = Recvfrom_flags(sockfd, mesg, MAXLINE, &amp;flags,</span><br><span class="line">   pcliaddr, &amp;len, &amp;pktinfo);</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"%d-byte datagram from %s"</span>, n, Sock_ntop(pcliaddr, len));</span><br><span class="line"><span class="comment">// 返回的 IP 地址不为 0，调用 Inet_ntop 转换表达形式</span></span><br><span class="line"><span class="keyword">if</span> (<span class="built_in">memcmp</span>(&amp;pktinfo.ipi_addr, &amp;in_zero, <span class="keyword">sizeof</span>(in_zero)) != <span class="number">0</span>)</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">", to %s"</span>, Inet_ntop(AF_INET, &amp;pktinfo.ipi_addr,</span><br><span class="line">str, <span class="keyword">sizeof</span>(str)));</span><br><span class="line"><span class="comment">// 返回接口索引不为 0，调用 If_indextoname 获取接口名字并打印</span></span><br><span class="line"><span class="keyword">if</span> (pktinfo.ipi_ifindex &gt; <span class="number">0</span>)</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">", recv i/f = %s"</span>,</span><br><span class="line">   If_indextoname(pktinfo.ipi_ifindex, ifname));</span><br><span class="line"><span class="comment">// 这四个标志位，分别测试是否打开，打开则打印</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span>MSG_TRUNC</span></span><br><span class="line"><span class="keyword">if</span> (flags &amp; MSG_TRUNC)<span class="built_in">printf</span>(<span class="string">" (datagram truncated)"</span>);</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span>MSG_CTRUNC</span></span><br><span class="line"><span class="keyword">if</span> (flags &amp; MSG_CTRUNC)<span class="built_in">printf</span>(<span class="string">" (control info truncated)"</span>);</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span>MSG_BCAST</span></span><br><span class="line"><span class="keyword">if</span> (flags &amp; MSG_BCAST)<span class="built_in">printf</span>(<span class="string">" (broadcast)"</span>);</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span>MSG_MCAST</span></span><br><span class="line"><span class="keyword">if</span> (flags &amp; MSG_MCAST)<span class="built_in">printf</span>(<span class="string">" (multicast)"</span>);</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"\n"</span>);</span><br><span class="line"></span><br><span class="line">Sendto(sockfd, mesg, n, <span class="number">0</span>, pcliaddr, len);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="22-3-数据报截断"><a href="#22-3-数据报截断" class="headerlink" title="22.3 数据报截断"></a>22.3 数据报截断</h1><p>&emsp;&emsp;当到达的一个 UDP 数据报超过应用进程提供的缓冲区容量时，recvmsg 在其 msghdr 结构的 msg_flags 成员上设置 MSG_TRUNC 标志。不幸的是不是所有实现都支持这种方式来处理过长的 UDP 数据报，有三种情况：</p><ol><li>丢弃超出部分的字节，并返回 MSG_TRUNC 标志</li><li>丢弃超出部分字节，但不告知应用进程</li><li>保留套接字进程并在同一套接字的读操作中进行返回  </li></ol><p>&emsp;&emsp;解决判断困难的一个方法就是，设置接收缓冲区大小为数据包大小多一个字节，如果收到等于该缓冲区长度的数据报，就说明是一个过长的数据报。</p><h1 id="22-4-何时用-UDP-代替-TCP"><a href="#22-4-何时用-UDP-代替-TCP" class="headerlink" title="22.4 何时用 UDP 代替 TCP"></a>22.4 何时用 UDP 代替 TCP</h1><ol><li>UDP 支持广播和组播。应用进程使用广播和组播，就必须使用 UDP</li><li>UDP 没有建立连接和断开。交换一个请求和一个应答，UDP 只需要 2 个分组，TCP 大约需要 20 个分组，并且最小事务处理时间 UDP 需要 RTT+SPT，TCP 为 2*RTT+SPT。</li></ol><p>&emsp;&emsp;但是并不是所有来连接都用来交换一个请求，在一个连接上处理多个请求，多出的一个 RTT 时间就显得不太关键了。尽管如此还是有这种情况会发生的。  </p><h2 id="并不是所有连接都需要-TCP-的所有特性："><a href="#并不是所有连接都需要-TCP-的所有特性：" class="headerlink" title="并不是所有连接都需要 TCP 的所有特性："></a>并不是所有连接都需要 TCP 的所有特性：</h2><ul><li>正面确认，丢失分组重传，重复分组检测，对接受的分组排序。</li><li>窗口式流量控制。用来控制发送端的发送速度不会超过接收端的接受速度，速度实时控制并判断。</li><li>慢启动和拥塞避免。</li></ul><h2 id="我们有如下建议："><a href="#我们有如下建议：" class="headerlink" title="我们有如下建议："></a>我们有如下建议：</h2><ul><li>对于广播和多播必须使用 UDP。任何形式的错误控制必须加到客户和服务器程序之中，不过应用程序需要允许一定的错误，例如音频中几帧的丢失等。如果对于错误过于严格，就需要考虑两者的收益是否大于所带来的复杂性</li><li>对于简单的请求-应答使用 UDP。一些控制手段需要加到应用程序中。但是如果交换的数据量较大，使用 TCP 也未尝不可</li><li>对于海量数据传输，不应该使用 UDP，会大大增加程序的复杂性</li></ul><p>但也存在例外：</p><ul><li>TFTP 使用 UDP 传输海量数据，因为编码更加简单（使用 UDP 约 800 行，TCP 为 4500 行）</li><li>NFS 使用 UDP 传输海量数据，历史原因</li></ul><p>※虽然 UDP 的使用频率在递减，但是预期接下来会增加，因为组播的使用会更加频繁</p><h1 id="22-5-给-UDP-应用增加可靠性"><a href="#22-5-给-UDP-应用增加可靠性" class="headerlink" title="22.5 给 UDP 应用增加可靠性"></a>22.5 给 UDP 应用增加可靠性</h1><p>&emsp;&emsp;如果想让请求-应答式应用程序使用 UDP，必须在客户中增加以下两个特性。</p><ol><li>超时和重传：用于处理丢失的数据报。</li><li>序列号：供客户验证一个应答是否匹配相应的请求</li></ol><p>&emsp;&emsp;增加序列号较为简单，只需要服务器进行记录并回射即可。<br>&emsp;&emsp;处理超时重传的老方法是发送一个请求等待 N 秒。没有收到应答，就重新发送同一个请求并等待 N 秒，一定次数后放弃。这个方法存在的问题是不同的网络 RTT 差距较大，并会随时间变化。但可以节键 TCP 中相关的算法。</p><p><strong>之后实现了一个例子用来实现上面的内容,参考书上相应的章节</strong></p><h1 id="22-6-捆绑接口地址"><a href="#22-6-捆绑接口地址" class="headerlink" title="22.6 捆绑接口地址"></a>22.6 捆绑接口地址</h1><p>&emsp;&emsp;get_ifi_info 函数常见用途之一是用于需要监视本地主机所有接口以便获悉某个数据报何时及那个接口上到达的 UDP 应用程序。这种用途允许接收程序获悉该 UDP 数据报的目的地址，因此决定一个数据报的传递套接字的正是它的目的地址。  </p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span><span class="title">mydg_echo</span><span class="params">(<span class="keyword">int</span>, SA *, <span class="keyword">socklen_t</span>, SA *)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span> </span>&#123;</span><br><span class="line"><span class="keyword">int</span>sockfd;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span>on = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">pid_t</span>pid;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ifi_info</span>*<span class="title">ifi</span>, *<span class="title">ifihead</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span>*<span class="title">sa</span>, <span class="title">cliaddr</span>, <span class="title">wildaddr</span>;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (ifihead = ifi = Get_ifi_info(AF_INET, <span class="number">1</span>);</span><br><span class="line"> ifi != <span class="literal">NULL</span>; ifi = ifi-&gt;ifi_next) &#123;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*4bind unicast address */</span></span><br><span class="line">sockfd = Socket(AF_INET, SOCK_DGRAM, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">Setsockopt(sockfd, SOL_SOCKET, SO_REUSEADDR, &amp;on, <span class="keyword">sizeof</span>(on));</span><br><span class="line"></span><br><span class="line">sa = (struct sockaddr_in *) ifi-&gt;ifi_addr;</span><br><span class="line">sa-&gt;sin_family = AF_INET;</span><br><span class="line">sa-&gt;sin_port = htons(SERV_PORT);</span><br><span class="line">Bind(sockfd, (SA *) sa, <span class="keyword">sizeof</span>(*sa));</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"bound %s\n"</span>, Sock_ntop((SA *) sa, <span class="keyword">sizeof</span>(*sa)));</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> ( (pid = Fork()) == <span class="number">0</span>) &#123;<span class="comment">/* child */</span></span><br><span class="line">mydg_echo(sockfd, (SA *) &amp;cliaddr, <span class="keyword">sizeof</span>(cliaddr), (SA *) sa);</span><br><span class="line"><span class="built_in">exit</span>(<span class="number">0</span>);<span class="comment">/* never executed */</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (ifi-&gt;ifi_flags &amp; IFF_BROADCAST) &#123;</span><br><span class="line"><span class="comment">/* 4try to bind broadcast address */</span></span><br><span class="line">sockfd = Socket(AF_INET, SOCK_DGRAM, <span class="number">0</span>);</span><br><span class="line">Setsockopt(sockfd, SOL_SOCKET, SO_REUSEADDR, &amp;on, <span class="keyword">sizeof</span>(on));</span><br><span class="line"></span><br><span class="line">sa = (struct sockaddr_in *) ifi-&gt;ifi_brdaddr;</span><br><span class="line">sa-&gt;sin_family = AF_INET;</span><br><span class="line">sa-&gt;sin_port = htons(SERV_PORT);</span><br><span class="line"><span class="keyword">if</span> (bind(sockfd, (SA *) sa, <span class="keyword">sizeof</span>(*sa)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line"><span class="keyword">if</span> (errno == EADDRINUSE) &#123;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"EADDRINUSE: %s\n"</span>,</span><br><span class="line">   Sock_ntop((SA *) sa, <span class="keyword">sizeof</span>(*sa)));</span><br><span class="line">Close(sockfd);</span><br><span class="line"><span class="keyword">continue</span>;</span><br><span class="line">&#125; <span class="keyword">else</span></span><br><span class="line">err_sys(<span class="string">"bind error for %s"</span>,</span><br><span class="line">Sock_ntop((SA *) sa, <span class="keyword">sizeof</span>(*sa)));</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"bound %s\n"</span>, Sock_ntop((SA *) sa, <span class="keyword">sizeof</span>(*sa)));</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> ( (pid = Fork()) == <span class="number">0</span>) &#123;<span class="comment">/* child */</span></span><br><span class="line">mydg_echo(sockfd, (SA *) &amp;cliaddr, <span class="keyword">sizeof</span>(cliaddr),</span><br><span class="line">  (SA *) sa);</span><br><span class="line"><span class="built_in">exit</span>(<span class="number">0</span>);<span class="comment">/* never executed */</span></span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 4bind wildcard address */</span></span><br><span class="line">sockfd = Socket(AF_INET, SOCK_DGRAM, <span class="number">0</span>);</span><br><span class="line">Setsockopt(sockfd, SOL_SOCKET, SO_REUSEADDR, &amp;on, <span class="keyword">sizeof</span>(on));</span><br><span class="line"></span><br><span class="line">bzero(&amp;wildaddr, <span class="keyword">sizeof</span>(wildaddr));</span><br><span class="line">wildaddr.sin_family = AF_INET;</span><br><span class="line">wildaddr.sin_addr.s_addr = htonl(INADDR_ANY);</span><br><span class="line">wildaddr.sin_port = htons(SERV_PORT);</span><br><span class="line">Bind(sockfd, (SA *) &amp;wildaddr, <span class="keyword">sizeof</span>(wildaddr));</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"bound %s\n"</span>, Sock_ntop((SA *) &amp;wildaddr, <span class="keyword">sizeof</span>(wildaddr)));</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> ( (pid = Fork()) == <span class="number">0</span>) &#123;<span class="comment">/* child */</span></span><br><span class="line">mydg_echo(sockfd, (SA *) &amp;cliaddr, <span class="keyword">sizeof</span>(cliaddr), (SA *) sa);</span><br><span class="line"><span class="built_in">exit</span>(<span class="number">0</span>);<span class="comment">/* never executed */</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span></span><br><span class="line">mydg_echo(<span class="keyword">int</span> sockfd, SA *pcliaddr, <span class="keyword">socklen_t</span> clilen, SA *myaddr)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">int</span>n;</span><br><span class="line"><span class="keyword">char</span>mesg[MAXLINE];</span><br><span class="line"><span class="keyword">socklen_t</span>len;</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> ( ; ; ) &#123;</span><br><span class="line">len = clilen;</span><br><span class="line">n = Recvfrom(sockfd, mesg, MAXLINE, <span class="number">0</span>, pcliaddr, &amp;len);</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"child %d, datagram from %s"</span>, getpid(),</span><br><span class="line">   Sock_ntop(pcliaddr, len));</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">", to %s\n"</span>, Sock_ntop(myaddr, clilen));</span><br><span class="line"></span><br><span class="line">Sendto(sockfd, mesg, n, <span class="number">0</span>, pcliaddr, len);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="22-7-并发-UDP-服务器"><a href="#22-7-并发-UDP-服务器" class="headerlink" title="22.7 并发 UDP 服务器"></a>22.7 并发 UDP 服务器</h1><p>&emsp;&emsp;大多数 UDP 服务器是迭代的，但是如果单个请求处理时间太长可能会造成后面的请求应答时间过长。当 UDP 服务器实现并发时并不能向 TCP 一样，因为 TCP 拥有唯一的链接可以采用简单的 fork 即可。UDP 有两类服务器，分别进行处：</p><ol><li>读入一个客户请求发送一个应答，就不再相关，直接 fork 一个子进程进行处理，子进程直接回应客户。</li><li>第二种 UDP 与客户交换多个数据报，但是客户仅仅知道服务器一个总所周知的端口，解决办法是，给每个客户创建一个新的套接字，调用 bind 一个临时端口，使用该套接字发送这个客户的所有应答，这种操作需要客户端判断第一个应答中的就 port，并将之后的数据报都发送到这个 port。</li></ol><p>※ TFTP 是第二种方式的一个例子</p><h1 id="22-8-ipv6-分组信息"><a href="#22-8-ipv6-分组信息" class="headerlink" title="22.8 ipv6 分组信息"></a>22.8 ipv6 分组信息</h1><p>IPv6 允许应用进程为每个外出数据报指定最多 5 条信息：</p><ol><li>源 IPv6 地址</li><li>外出接口索引</li><li>外出跳限</li><li>下一跳地址</li><li>外出流通类别</li></ol><p>这些信息会作为辅助信息使用 sendmsg 进行发送</p><h3 id="外出和到达接口"><a href="#外出和到达接口" class="headerlink" title="外出和到达接口"></a>外出和到达接口</h3><p>&emsp;&emsp;IPv6 节点的接口由正值整数标识。任何接口都不会被赋予 0 值索引。</p><h3 id="源和目的-IPv6-地址"><a href="#源和目的-IPv6-地址" class="headerlink" title="源和目的 IPv6 地址"></a>源和目的 IPv6 地址</h3><p>&emsp;&emsp;源 IPv6 地址通常通过调用 bind 指定。不过连同数据一起指定源地址可能并不需要多少开销。后者允许服务器确保所发送应答的源地址等于响应客户请求的目的地址，这是一个某些客户需要且 IPv4 难以提供的特性</p><h3 id="指定和接收跳限"><a href="#指定和接收跳限" class="headerlink" title="指定和接收跳限"></a>指定和接收跳限</h3><p>&emsp;&emsp;外出跳限指定：<br>&emsp;&emsp;单播数据报：通过 IPV6_UNICAST_HOPS 套接字进行指定<br>&emsp;&emsp;组播数据报：通过 IPV6_MULTICAST_HOPS 套接字进行指定  </p><p>&emsp;&emsp;接收跳限指定：由 recvmsg 作为辅助数据返回的前提是进程开启 IPV6_RECVHOPLIMIT 套接字选项。包含本辅助数据的 cmsghdr 结构中，cmsg_level 成员将是 IPPROTO_IPV6，cmsg_type 成员将是 IPV6_HOPLIMIT，数据的第一个字节将是 4 字节整数跳限的第一个字节。<br>※ 跳限的正常值在 0 ~ 255 之间</p><h3 id="指定下一跳地址"><a href="#指定下一跳地址" class="headerlink" title="指定下一跳地址"></a>指定下一跳地址</h3><p>IPV6_NEXTHOP 辅助数据对象将数据报的下一跳指定为一个套接字地址结构。在包含本辅助数据的 cmsghdr 结构中，cmsg_level 成员是 IPPROTO_IPV6，cmsg_type 成员是 IPV6_NEXTHOP，数据的第一字节是套接字地址结构的第一个字节。</p><h3 id="指定和接收流通类别"><a href="#指定和接收流通类别" class="headerlink" title="指定和接收流通类别"></a>指定和接收流通类别</h3><p>&emsp;&emsp;IPV6_TCLASS 辅助数据对象指定数据包的流通类别。</p><h1 id="22-9-ipv6-路径-mtu-控制"><a href="#22-9-ipv6-路径-mtu-控制" class="headerlink" title="22.9 ipv6 路径 mtu 控制"></a>22.9 ipv6 路径 mtu 控制</h1><p>&emsp;&emsp;IPv6 为应用程序提供若干路径 MTU 发现控制手段。默认设置对于绝大多数是正确的，不过特殊的程序想要更改 MTU 发现行为，IPv6 有 4 个套接字选项。</p><h3 id="以最小-MTU-发送"><a href="#以最小-MTU-发送" class="headerlink" title="以最小 MTU 发送"></a>以最小 MTU 发送</h3><p>&emsp;&emsp;执行路径 MTU 发现时，一般按照外出接口的 MTU 或路径 MTU 二者中的较小者尽心分片。IPv6 定义了值为 1280 字节的最小 MTU，所有链路必须支持。使用这个方法可能会丢失较大 MTU 的发送机会，但是可以快速发送，避免了路径 MTU 发现的缺点</p><h3 id="接收路径-MTU-变动提示"><a href="#接收路径-MTU-变动提示" class="headerlink" title="接收路径 MTU 变动提示"></a>接收路径 MTU 变动提示</h3><p>&emsp;&emsp;应用程序可以开启 IPV6_RECVPATHMTU 套接字选项已接受路径 MTU 变动通知。本标志值使得任何时候路径 MTU 发生变动时作为互助数据由 recvmsg 返回变动后的 MTU</p><h3 id="确定当前路径-MTU"><a href="#确定当前路径-MTU" class="headerlink" title="确定当前路径 MTU"></a>确定当前路径 MTU</h3><p>&emsp;&emsp;如果应用程序没有开启 IPV6_RECVPATHMTU 套接字选项，那么可以使用 IPV6_PATHMTU 确定一个已连接套接字路径的当前 MTU，没有的话返回外出接口的 MTU</p><h3 id="避免分片"><a href="#避免分片" class="headerlink" title="避免分片"></a>避免分片</h3><p>&emsp;&emsp;默认情况下会对数据报按照路径 MTU 执行分片，但有些程序不希望执行分片。而是自行发现路径 MTU。IPV6_DONTFRAG 套接字选项用于关闭分片特性：其值为 0 表示允许自动分片，为 1 关闭自动分片。</p><h1 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h1><p>&emsp;&emsp;有些程序想要知道某个 UDP 数据报的目的 IPv4 地址和接收接口。开启 IP_RECVDSTADDR 和 IP_RECVIF 套接字选项可以作为辅助数据随每个数据报返回这些信息。<br>&emsp;&emsp;尽管 UDP 无法提供类似于 TCP 的众多特性，但是 UDP 使用的地方还是很多。广播和组播只能使用 UDP。简单的请求—应答也可以使用 UDP，不过必须增加程序级别的可靠性手段。UDP 不应用于海量数据的传输。  </p>]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;22-1-概述&quot;&gt;&lt;a href=&quot;#22-1-概述&quot; class=&quot;headerlink&quot; title=&quot;22.1 概述&quot;&gt;&lt;/a&gt;22.1 概述&lt;/h1&gt;&lt;p&gt;&amp;emsp;&amp;emsp;首先确定某个外来 UDP 数据报的目的地址和接收端口，因为绑定某个 UDP 端口和通配地址的一个套接字能够在任何接口上接受单播，广播和组播数据报。&lt;br&gt;&amp;emsp;&amp;emsp;TCP 是一个字节流协议，又使用滑动窗口，因此没有记录边界和接收端速度慢于发送端的情况。但是 UDP 却需要程序对这些进行处理。&lt;br&gt;&amp;emsp;&amp;emsp;如果实现不支持 IP_RECVDSTADDR 套接字选项，那么确定外来 UDP 数据报目的地址的方法之一是捆绑所有接口地址并使用 select。&lt;/p&gt;
    
    </summary>
    
    
      <category term="UNP" scheme="http://yoursite.com/categories/UNP/"/>
    
    
  </entry>
  
  <entry>
    <title>第12章：IPv4和IPv6的互操作性</title>
    <link href="http://yoursite.com/2019/09/25/12.IPv4%E5%92%8CIPv6%E7%9A%84%E4%BA%92%E6%93%8D%E4%BD%9C%E6%80%A7/"/>
    <id>http://yoursite.com/2019/09/25/12.IPv4和IPv6的互操作性/</id>
    <published>2019-09-25T14:54:48.820Z</published>
    <updated>2019-09-28T08:46:38.257Z</updated>
    
    <content type="html"><![CDATA[<h1 id="12-1-概述"><a href="#12-1-概述" class="headerlink" title="12.1 概述"></a>12.1 概述</h1><p>&emsp;&emsp;未来，因特网会逐渐从 IPv4 转换到 IPv6，所以引用程序能够对两种协议协同工作是很重要的，我们将讨论 IPv4 应用如何与 IPv6 进行通信</p><!--main--><h1 id="12-2-IPv4-客户与-IPv6-服务器"><a href="#12-2-IPv4-客户与-IPv6-服务器" class="headerlink" title="12.2 IPv4 客户与 IPv6 服务器"></a>12.2 IPv4 客户与 IPv6 服务器</h1><p>&emsp;&emsp;双栈主机一个很重要的特性就是技能处理 IPv4 又能处理 IPv6 客户。这是通过使用 IPv4 映射的 IPv6 地址来实现的<br>主机使用 IPv6 服务器，收到 IPv4 的帧时，会将源 IPv4 地址转换为一个映射的 IPv6 地址<br>一个 IPv4 客户端和 IPv6 服务端通信地步骤如下：</p><ol><li>IPv6 服务器启动后创建一个 IPv6 监听套接字</li><li>IPv4 客户调用 getsockname 找到服务器地 A 记录。服务器既有 A 也有 AAAA 记录，因为它是双栈的</li><li>客户调用 connect，发送一个 IPv4 的 SYN 给服务器</li><li>服务器收到这个 SYN，把它标志为 IPv4 映射为 IPv6，响应一个 IPv4 SYN/ACK。连接建立后，由 accept 返回给服务器的地址就是这个 IPv4 映射的 IPv6 地址</li><li>当服务器向这个客户端发送数据时，会使用客户端的 IPv4 地址，所以通信使用的全部都是 IPv4 连接</li><li>如果服务器不检查这个地址时 IPv6 还是 IPv4 映射过来，它永远不会知道客户端的 IP 类型，客户端也不需要知道服务器的类型</li></ol><p>大多数双栈主机遵循以下规则：</p><ul><li>IPv4 监听套接字只能接受来自 IPv4 客户的外来连接</li><li>如果服务器有一个绑定了 IPv6 的监听套接字，该套接字没设置 IPV6_V6ONLY 套接字选项，它可以接收 IPv4 连接</li><li>如果服务器有一个 IPv6 监听套接字，绑定了通配地址，该套接字设置了 IPV6_V6ONLY 套接字选项，它只能接收 IPv6 连接<h1 id="12-3-IPv6-客户与-IPv4-服务器"><a href="#12-3-IPv6-客户与-IPv4-服务器" class="headerlink" title="12.3 IPv6 客户与 IPv4 服务器"></a>12.3 IPv6 客户与 IPv4 服务器</h1>&emsp;&emsp;客户机运行在双栈主机上并使用 IPv6 套接字描述符</li></ul><ol><li>一个 IPv4 服务器在只支持 IPv4 的主机上启动一个 IPv4 监听套接字</li><li>IPv6 客户启动后调用 getaddrinfo 单纯查找 IPv6 的地址，因为服务器只支持 IPv4，所以返回给客户端的是一个 IPv4 映射的 IPv6 地址</li><li>IPv6 客户在作为函数参数的 IPv6 套接字地址结构中设置这个 IPv4 映射的 IPv6 地址然后调用 connect。内核检测到这个映射地址后自动发送一个 IPv4 SYN 到服务器</li><li>服务器响应一个 IPv4 SYN/ACK，连接于是通过使用 IPv4 数据报建立<h1 id="12-4-IPv6-地址测试宏"><a href="#12-4-IPv6-地址测试宏" class="headerlink" title="12.4 IPv6 地址测试宏"></a>12.4 IPv6 地址测试宏</h1></li></ol><p>有一些 IPv6 应用必须知道和它通信的是 IPv6 还是 IPv4 协议，使用 &lt;netinet/in.h&gt; 中的函数可以进行测试</p><h1 id="12-5-源代码可移植性"><a href="#12-5-源代码可移植性" class="headerlink" title="12.5 源代码可移植性"></a>12.5 源代码可移植性</h1><p>考虑到源码的可移植性，编写代码时应尽量避免 gethostbyname gethostbyaddr 等套接字函数，使用 getaddrinfo getnameinfo 等函数，使得代码变得与协议无关。</p><h1 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h1><ul><li>双栈主机的 IPv6 服务器可以和两种客户端进行通讯，网络中仅使用 IPv4 进行传输</li><li>双栈主机的 IPv6 客户端也可以和两种服务器进行通讯</li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;12-1-概述&quot;&gt;&lt;a href=&quot;#12-1-概述&quot; class=&quot;headerlink&quot; title=&quot;12.1 概述&quot;&gt;&lt;/a&gt;12.1 概述&lt;/h1&gt;&lt;p&gt;&amp;emsp;&amp;emsp;未来，因特网会逐渐从 IPv4 转换到 IPv6，所以引用程序能够对两种协议
      
    
    </summary>
    
    
      <category term="UNP" scheme="http://yoursite.com/categories/UNP/"/>
    
    
  </entry>
  
  <entry>
    <title>第13章：守护进程和inetd超级服务器</title>
    <link href="http://yoursite.com/2019/09/25/13.%E5%AE%88%E6%8A%A4%E8%BF%9B%E7%A8%8B%E5%92%8Cinetd%E8%B6%85%E7%BA%A7%E6%9C%8D%E5%8A%A1%E5%99%A8/"/>
    <id>http://yoursite.com/2019/09/25/13.守护进程和inetd超级服务器/</id>
    <published>2019-09-25T14:54:48.820Z</published>
    <updated>2019-09-28T08:46:38.257Z</updated>
    
    <content type="html"><![CDATA[<h1 id="13-1-概述"><a href="#13-1-概述" class="headerlink" title="13.1 概述"></a>13.1 概述</h1><p>&emsp;&emsp;<strong>守护进程</strong>：是在后台运行且不与任何控制终端关联的进程。Unix 系统通常有很多守护进程在后台运行（20~50）执行不同的管理任务<br>&emsp;&emsp;守护进程没有终端，通常是因为他们由开机时的脚本进行启动。但是守护进程也可能从某个终端由用户在 shell 提示符下键入命令行进行启动，这样的守护进程必须亲自脱离与控制终端的关联，从而避免与作业控制，终端会话管理，终端产生信号等发生不希望的交互,也防止后台的守护进程输出到终端  </p><a id="more"></a><h3 id="守护进程的启动方式："><a href="#守护进程的启动方式：" class="headerlink" title="守护进程的启动方式："></a>守护进程的启动方式：</h3><ul><li>在系统阶段进行启动，许多守护进程由系统初始化脚本进行启动，脚本通常位于 /etc 等目录，这些脚本启动的守护进程开始就拥有超级用户权限（inetd，Web，sendmail，syslogd 等等）</li><li>许多网络服务器由 inetd 超级服务器进行启动。Inetd 监听网络请求，每当有一个请求到达，启动相应的实际服务器（Telnet，FTP…）</li><li>cron 守护进程按规则定期执行一些程序。这些程序的时刻到来时，corn 执行的程序通常也是守护进行的方式运行</li><li>at 命令用于指定将来某个时刻的程序执行，时间到达时，通常使用 corn 来进行执行</li><li>守护进程还可以从用户的终端在前台或者后台进行启动。这么做往往是测试守护进程或者重启关闭的守护进程。  </li></ul><p><strong>因为守护进程没有终端，所以他们的消息使用 syslog 进行处理，即使用 syslog 函数，将消息发送给 syslogd 进程</strong></p><h1 id="13-2-syslodg-守护进程"><a href="#13-2-syslodg-守护进程" class="headerlink" title="13.2 syslodg 守护进程"></a>13.2 syslodg 守护进程</h1><p>syslogd 守护进程通常由系统初始化脚本进行启动，并在系统工作时间一直运行，启动步骤如下：</p><ol><li>读取配置文件，在 /etc/syslog.conf 配置文件指定守护进程收取的各种日志消息应如何处理。可能添加到一个文件中，或被写到用户的登录窗口，或被转发给另一个主机上的 syslogd 进程</li><li>创建 Unix 域数据报套接字，给它捆绑路径名 /var/run/log</li><li>创建 UDP 套接字，捆绑 514 端口，接收别的主机发送过来的日志</li><li>打开路径名 /dev/klog。来自内核的任何出错消息从这个设备输入<br>&emsp;&emsp;syslog 使用 select 来监听上面 2，3，4 步的描述符来接受日志，并按照配置文件进行处理。如果守护进程读取 SIGHUP 信号，就重新读取配置文件<br>&emsp;&emsp;<strong>最新的系统不建议开启 514 端口，会遭到攻击</strong></li></ol><h1 id="13-3-syslog-函数"><a href="#13-3-syslog-函数" class="headerlink" title="13.3 syslog 函数"></a>13.3 syslog 函数</h1><p>守护进程没有终端，所以不能把消息 fprintf 到 stderr 上。从守护进程中登记消息的常用技巧是调用 syslog 函数</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;syslog.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">syslog</span><span class="params">(<span class="keyword">int</span> priority, <span class="keyword">const</span> <span class="keyword">char</span> * message, ...)</span></span>;</span><br></pre></td></tr></table></figure><p>参数解析：</p><ul><li>priority：级别和设施两者的组合体</li><li>message：类似 printf 格式串，增加了 %m 规范代表当前 errno 值</li></ul><p>&emsp;&emsp;当 syslog 被应用进程首次调用时，它创建一个 Unix 域数据报套接字，然后调用 connect 连接到由 syslogd 守护进程创建的 Unix 域数据报套接字的众所周知的路径名。这个套接字一直打开，直到进程终止关闭  </p><h4 id="logger-命令在-shell-脚本中以向-syslogd-发送消息"><a href="#logger-命令在-shell-脚本中以向-syslogd-发送消息" class="headerlink" title="logger 命令在 shell 脚本中以向 syslogd 发送消息"></a>logger 命令在 shell 脚本中以向 syslogd 发送消息</h4><h1 id="13-4-daemon-init-函数"><a href="#13-4-daemon-init-函数" class="headerlink" title="13.4 daemon_init 函数"></a>13.4 daemon_init 函数</h1><p>编写一个守护进程的创建函数，有些系统提供 daemon 函数用来创建守护进程，和本程序类似</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">"unp.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;syslog.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>MAXFD64</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">extern</span> <span class="keyword">int</span>daemon_proc;<span class="comment">/* defined in error.c */</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span></span><br><span class="line">daemon_init(<span class="keyword">const</span> <span class="keyword">char</span> *pname, <span class="keyword">int</span> facility)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">int</span>i;</span><br><span class="line"><span class="keyword">pid_t</span>pid;</span><br><span class="line">    <span class="comment">// 调用 fork 创建子进程，然后直接终止父进程，留下子进程继续执行。如果是在 shell 中执行的程序，父进程终止，shell 会认为程序已经结束了，子进程就可以在后台执行了</span></span><br><span class="line">    <span class="comment">// 子进程继承父进程的进程组 ID，但它有自己的进程 ID，这就保证了子进程不是一个进程组的头进程，这是接下来调用 setsid 的必要条件</span></span><br><span class="line"><span class="keyword">if</span> ( (pid = Fork()) &lt; <span class="number">0</span>)</span><br><span class="line"><span class="keyword">return</span> (<span class="number">-1</span>);</span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span> (pid)</span><br><span class="line">_exit(<span class="number">0</span>);<span class="comment">/* parent terminates */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* child 1 continues... */</span></span><br><span class="line">    <span class="comment">// setsid 用来创建一个新的会话。当前进程变为新会话的会话头进程以及新进程组的进程组头进程，从而不再有控制终端</span></span><br><span class="line"><span class="keyword">if</span> (setsid() &lt; <span class="number">0</span>)<span class="comment">/* become session leader */</span></span><br><span class="line"><span class="keyword">return</span> (<span class="number">-1</span>);</span><br><span class="line">    <span class="comment">// 忽略 SIGHUP 信号，并再次调用 fork。该函数返回时，同样只使用子进程，父进程返回</span></span><br><span class="line">    <span class="comment">// 再次 fork 是为了确保本守护进程将来即使打开一个新的终端，也不会自动获得控制终端。当没有终端的一个会话头进程打开终端时，该终端自动成为这个头进程的控制终端。再次调用 fork，产生的子进程不是会话头进程，就不会自动获得一个控制终端。这里必须忽略 SIGHUP 信号，当会话头进程终止时，所有会话子进程都会收到 SIGHUP 信号</span></span><br><span class="line">Signal(SIGHUP, SIG_IGN);</span><br><span class="line"><span class="keyword">if</span> ( (pid = Fork()) &lt; <span class="number">0</span>)</span><br><span class="line"><span class="keyword">return</span> (<span class="number">-1</span>);</span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span> (pid)</span><br><span class="line">_exit(<span class="number">0</span>);<span class="comment">/* child 1 terminates */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* child 2 continues... */</span></span><br><span class="line">    <span class="comment">// 把全局变量 daemon_proc 设置为非 0 值，这个变量由 err_XXX 函数使用，不为 0 是为了告诉他们将 fprintf 输出替换为调用 syslog 函数</span></span><br><span class="line">daemon_proc = <span class="number">1</span>;<span class="comment">/* for err_XXX() functions */</span></span><br><span class="line">    <span class="comment">// 改变工作目录到根目录</span></span><br><span class="line">chdir(<span class="string">"/"</span>);<span class="comment">/* change working directory */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* close off file descriptors */</span></span><br><span class="line">    <span class="comment">// 关闭所有打开的描述符，直接关闭前 64 个，这里不考虑太多</span></span><br><span class="line"><span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; MAXFD; i++)</span><br><span class="line">close(i);</span><br><span class="line"></span><br><span class="line"><span class="comment">/* redirect stdin, stdout, and stderr to /dev/null */</span></span><br><span class="line">    <span class="comment">// 将 stdin stdout stderr 重定向到 /dev/null</span></span><br><span class="line">open(<span class="string">"/dev/null"</span>, O_RDONLY);</span><br><span class="line">open(<span class="string">"/dev/null"</span>, O_RDWR);</span><br><span class="line">open(<span class="string">"/dev/null"</span>, O_RDWR);</span><br><span class="line">    <span class="comment">// 使用 syslogd 处理错误</span></span><br><span class="line">openlog(pname, LOG_PID, facility);</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> (<span class="number">0</span>);<span class="comment">/* success */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>&emsp;&emsp;守护进程在没有终端的环境下运行，不会接收 SIGHUP 信号。许多守护进程把这个信号可以当作系统发送的通知，表示配置文件发送了变化，应重新读取配置文件，类似的还有 SIGINT SINGWINCH 信号</p><h1 id="13-5-inetd-守护进程"><a href="#13-5-inetd-守护进程" class="headerlink" title="13.5 inetd 守护进程"></a>13.5 inetd 守护进程</h1><p>&emsp;&emsp;通过 inetd 简化一系列服务器进程的启动流程，这些服务器程序流程相似，并且大部分时间处于睡眠，交给 inetd 守护进程来进行处理，既可以简化编写的代码，又可以对每次客户端的请求单独响应，并不需要每个服务器一直等待客户端，因为他们大多数时间都处于休眠状态。只需要 inetd 循环等待客户端的请求即可，来了请求，为对应的客户创建需要的服务器子进程即可。  </p><h4 id="当使用-inetd-调用一个程序时，程序名作为第一个参数进行传递。"><a href="#当使用-inetd-调用一个程序时，程序名作为第一个参数进行传递。" class="headerlink" title="当使用 inetd 调用一个程序时，程序名作为第一个参数进行传递。"></a>当使用 inetd 调用一个程序时，程序名作为第一个参数进行传递。</h4><h3 id="inetd-工作流程"><a href="#inetd-工作流程" class="headerlink" title="inetd 工作流程"></a>inetd 工作流程</h3><ol><li>启动阶段，读取配置文件，并给文件中每个类型服务器创建一个适当的类型（TCP or UDP…）的套接字。inetd 能够处理的服务器最大个数取决于 inetd 能够创建的描述符最大个数，使用 select 对所有描述符进行集中</li><li>为每个套接字调用 bind，指定 IP + port。端口通过 getservbyname 获取</li><li>对于 TCP 套接字，调用 listen 来进行监听，UDP 不用执行</li><li>使用 select 对所有套接字描述符进行监听，inetd 大部分时间都花在这里</li><li>如果可读的是 TCP 套接字描述符，调用 accept 来进行连接</li><li>调用 fork 创建子进程来处理不同的请求，类似于并发服务器</li><li>如果第 5 步返回字节流套接字，父进程要关闭已连接套接字，就是 accept 的套接字，类似于 TCP 并发服务器</li></ol><h1 id="13-6-daemon-inetd-函数"><a href="#13-6-daemon-inetd-函数" class="headerlink" title="13.6 daemon_inetd 函数"></a>13.6 daemon_inetd 函数</h1><p>该函数可以用在 inetd 启动的服务器程序中</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">"unp.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;syslog.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">extern</span> <span class="keyword">int</span>daemon_proc;<span class="comment">/* defined in error.c */</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span></span><br><span class="line">daemon_inetd(<span class="keyword">const</span> <span class="keyword">char</span> *pname, <span class="keyword">int</span> facility)</span><br><span class="line">&#123;</span><br><span class="line">daemon_proc = <span class="number">1</span>;<span class="comment">/* for our err_XXX() functions */</span></span><br><span class="line">openlog(pname, LOG_PID, facility);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>所有的步骤已经由 inetd 在启动时执行完毕，本函数仅仅处理错误函数设置 daemon_proc 标志，并调用 openlog 函数</p><h4 id="由-inetd-作为守护进程启动时间获取服务器程序"><a href="#由-inetd-作为守护进程启动时间获取服务器程序" class="headerlink" title="由 inetd 作为守护进程启动时间获取服务器程序"></a>由 inetd 作为守护进程启动时间获取服务器程序</h4><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">"unp.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;time.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span></span><br><span class="line">main(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">socklen_t</span>len;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr</span>*<span class="title">cliaddr</span>;</span></span><br><span class="line"><span class="keyword">char</span>buff[MAXLINE];</span><br><span class="line"><span class="keyword">time_t</span>ticks;</span><br><span class="line"></span><br><span class="line">daemon_inetd(argv[<span class="number">0</span>], <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">cliaddr = Malloc(<span class="keyword">sizeof</span>(struct sockaddr_storage));</span><br><span class="line">len = <span class="keyword">sizeof</span>(struct sockaddr_storage);</span><br><span class="line">Getpeername(<span class="number">0</span>, cliaddr, &amp;len);</span><br><span class="line">err_msg(<span class="string">"connection from %s"</span>, Sock_ntop(cliaddr, len));</span><br><span class="line"></span><br><span class="line">    ticks = time(<span class="literal">NULL</span>);</span><br><span class="line">    <span class="built_in">snprintf</span>(buff, <span class="keyword">sizeof</span>(buff), <span class="string">"%.24s\r\n"</span>, ctime(&amp;ticks));</span><br><span class="line">    Write(<span class="number">0</span>, buff, <span class="built_in">strlen</span>(buff));</span><br><span class="line"></span><br><span class="line">Close(<span class="number">0</span>);<span class="comment">/* close TCP connection */</span></span><br><span class="line"><span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h1><p>&emsp;&emsp;守护进程是在后台运行并独立与所有终端的进程，许多网络服务器作为守护进程运行。守护进程所产生的输出调用 syslog 函数交给 syslogd 守护进程处理<br>启动任意一个程序并将其变为守护进程步骤如下：</p><ol><li>调用 fork 到后台运行</li><li>调用 setsid 创建一个新会话，并让前一步的子进程成为会话头进程</li><li>再次 fork 防止会话头进程自动获取控制终端</li><li>改变工作目录</li><li>创建模式掩码</li><li>关闭所有非必要描述符<br>许多 Unix 服务器由 inetd 守护进程启动。它处理所有守护进程需要的步骤，当启动真正的服务器时，套接字已在标准输入，标准输出，标准错误上打开。这样就不用调用 socket，bind，accept，这些步骤已经由 inetd 完成。</li></ol>]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;13-1-概述&quot;&gt;&lt;a href=&quot;#13-1-概述&quot; class=&quot;headerlink&quot; title=&quot;13.1 概述&quot;&gt;&lt;/a&gt;13.1 概述&lt;/h1&gt;&lt;p&gt;&amp;emsp;&amp;emsp;&lt;strong&gt;守护进程&lt;/strong&gt;：是在后台运行且不与任何控制终端关联的进程。Unix 系统通常有很多守护进程在后台运行（20~50）执行不同的管理任务&lt;br&gt;&amp;emsp;&amp;emsp;守护进程没有终端，通常是因为他们由开机时的脚本进行启动。但是守护进程也可能从某个终端由用户在 shell 提示符下键入命令行进行启动，这样的守护进程必须亲自脱离与控制终端的关联，从而避免与作业控制，终端会话管理，终端产生信号等发生不希望的交互,也防止后台的守护进程输出到终端  &lt;/p&gt;
    
    </summary>
    
    
      <category term="UNP" scheme="http://yoursite.com/categories/UNP/"/>
    
    
  </entry>
  
  <entry>
    <title>第14章：高级IO函数</title>
    <link href="http://yoursite.com/2019/09/25/14.%E9%AB%98%E7%BA%A7IO%E5%87%BD%E6%95%B0/"/>
    <id>http://yoursite.com/2019/09/25/14.高级IO函数/</id>
    <published>2019-09-25T14:54:48.820Z</published>
    <updated>2019-09-28T08:46:38.257Z</updated>
    
    <content type="html"><![CDATA[<h1 id="14-1-概述"><a href="#14-1-概述" class="headerlink" title="14.1 概述"></a>14.1 概述</h1><p>&emsp;&emsp;本章讨论 I/O 的高级操作，首先是在 I/O 上设置超时，这里有三种方法。然后是 read 和 write 的三个变体：</p><ol><li>recv 和 send 允许通过第四个参数从进程到内核传递标志</li><li>readv 和 writev 允许指定往其中输入数据或从其中输出数据的缓冲区向量</li><li>recvmsg 和 sendmsg 结合了其他 I/O 函数的所有特性，并具备接收和发送辅助数据的新能力<a id="more"></a><h1 id="14-2-套接字超时"><a href="#14-2-套接字超时" class="headerlink" title="14.2 套接字超时"></a>14.2 套接字超时</h1></li></ol><p>设置超时的方法有 3 种：</p><ol><li>调用 alarm，它在指定超时期满时产生 SIGALRM 信号。这个方法涉及信号处理，而信号处理在不同的实现上存在差异，而且可能干扰进程中现有的 alarm 调用</li><li>在 select 中阻塞等待 I/O（select 有内置时间限制）以此代替直接阻塞在 read 或 write 调用上</li><li>使用较新的 SO_RCVTIMEO 和 SO_SNDTIMEO 套接字选项。这个方法的问题在于并非所有实现都支持这两个套接字选项。</li></ol><p>&emsp;&emsp;上述三个技术都适用于输入和输出操作（例如 read，write 以及诸如 recvfrom，sendto 之类），不过我们依然期待可用于 connect 的技术，因为 TCP 内置的 connect 超时相当长。select 可用来在 connect 上设置超时的先决条件是相应套接字处于非阻塞模式，而那两个套接字选项对 connect 并不适用。我们还指出，前两个技术使用于任何描述符，而第三个技术仅仅适用于套接字描述符（因为是套接字描述符选项）</p><h3 id="1-使用-SIGALRM-为-connect-设置超时"><a href="#1-使用-SIGALRM-为-connect-设置超时" class="headerlink" title="1. 使用 SIGALRM 为 connect 设置超时"></a>1. 使用 SIGALRM 为 connect 设置超时</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span><span class="title">connect_alarm</span><span class="params">(<span class="keyword">int</span>)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">connect_timeo</span><span class="params">(<span class="keyword">int</span> sockfd, <span class="keyword">const</span> SA *saptr, <span class="keyword">socklen_t</span> salen, <span class="keyword">int</span> nsec)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">Sigfunc*sigfunc;</span><br><span class="line"><span class="keyword">int</span>n;</span><br><span class="line">    <span class="comment">// 设置 SIGALRM 信号的处理函数，并保存原有的处理函数到 sigfunc</span></span><br><span class="line">sigfunc = Signal(SIGALRM, connect_alarm);</span><br><span class="line">    <span class="comment">// 设置报警时钟的秒数，返回值是上一次设置的剩余秒数，没有就返回 0</span></span><br><span class="line"><span class="keyword">if</span> (alarm(nsec) != <span class="number">0</span>)</span><br><span class="line">err_msg(<span class="string">"connect_timeo: alarm was already set"</span>);</span><br><span class="line">    <span class="comment">// 调用 connect，调用中断就设置 error 设置为 TIMEOUT，并关闭套接字，防止三路握手继续进行</span></span><br><span class="line"><span class="keyword">if</span> ( (n = connect(sockfd, saptr, salen)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">close(sockfd);</span><br><span class="line"><span class="keyword">if</span> (errno == EINTR)</span><br><span class="line">errno = ETIMEDOUT;</span><br><span class="line">&#125;</span><br><span class="line">    <span class="comment">// 关闭报警时钟，并恢复原处理函数</span></span><br><span class="line">alarm(<span class="number">0</span>);<span class="comment">/* turn off the alarm */</span></span><br><span class="line">Signal(SIGALRM, sigfunc);<span class="comment">/* restore previous signal handler */</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span>(n);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 信号函数仅仅返回</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">connect_alarm</span><span class="params">(<span class="keyword">int</span> signo)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">return</span>;<span class="comment">/* just interrupt the connect() */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>※ 本方法仅能减少 connect 的超时，但不能增加 connect 的超时设置，因为 connect 有自己的超时设置<br>※ 需要注意的是在多线程中使用信号非常困难，建议仅仅在未线程化或仅在单线程中使用本技术  </p><h3 id="2-使用-SIGALRM-为-recvfrom-设置超时"><a href="#2-使用-SIGALRM-为-recvfrom-设置超时" class="headerlink" title="2. 使用 SIGALRM 为 recvfrom 设置超时"></a>2. 使用 SIGALRM 为 recvfrom 设置超时</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span><span class="title">sig_alrm</span><span class="params">(<span class="keyword">int</span>)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">dg_cli</span><span class="params">(FILE *fp, <span class="keyword">int</span> sockfd, <span class="keyword">const</span> SA *pservaddr, <span class="keyword">socklen_t</span> servlen)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">int</span>n;</span><br><span class="line"><span class="keyword">char</span>sendline[MAXLINE], recvline[MAXLINE + <span class="number">1</span>];</span><br><span class="line"></span><br><span class="line">Signal(SIGALRM, sig_alrm);</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> (Fgets(sendline, MAXLINE, fp) != <span class="literal">NULL</span>) &#123;</span><br><span class="line"></span><br><span class="line">Sendto(sockfd, sendline, <span class="built_in">strlen</span>(sendline), <span class="number">0</span>, pservaddr, servlen);</span><br><span class="line">        <span class="comment">// 调用 recvfrom 函数前设置了 5 秒的超时设置</span></span><br><span class="line">alarm(<span class="number">5</span>);</span><br><span class="line"><span class="keyword">if</span> ( (n = recvfrom(sockfd, recvline, MAXLINE, <span class="number">0</span>, <span class="literal">NULL</span>, <span class="literal">NULL</span>)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line"><span class="keyword">if</span> (errno == EINTR)</span><br><span class="line"><span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"socket timeout\n"</span>);</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">err_sys(<span class="string">"recvfrom error"</span>);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// 读取数据，关闭超时处理</span></span><br><span class="line">alarm(<span class="number">0</span>);</span><br><span class="line">recvline[n] = <span class="number">0</span>;<span class="comment">/* null terminate */</span></span><br><span class="line">Fputs(recvline, <span class="built_in">stdout</span>);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 简单返回，用来中断阻塞的 ercvfrom 调用</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">sig_alrm</span><span class="params">(<span class="keyword">int</span> signo)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">return</span>;<span class="comment">/* just interrupt the recvfrom() */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="3-使用-select-为-recvfrom-设置超时"><a href="#3-使用-select-为-recvfrom-设置超时" class="headerlink" title="3. 使用 select 为 recvfrom 设置超时"></a>3. 使用 select 为 recvfrom 设置超时</h3><p>该函数中 select 指定等待描述符的最长时间</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">readable_timeo</span><span class="params">(<span class="keyword">int</span> fd, <span class="keyword">int</span> sec)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">fd_setrset;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">timeval</span><span class="title">tv</span>;</span></span><br><span class="line">    <span class="comment">// 准备 select 参数</span></span><br><span class="line">FD_ZERO(&amp;rset);</span><br><span class="line">FD_SET(fd, &amp;rset);</span><br><span class="line"></span><br><span class="line">tv.tv_sec = sec;</span><br><span class="line">tv.tv_usec = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">// 调用有超时的 select 函数，出错返回 -1，超时返回 0</span></span><br><span class="line"><span class="keyword">return</span>(select(fd+<span class="number">1</span>, &amp;rset, <span class="literal">NULL</span>, <span class="literal">NULL</span>, &amp;tv));</span><br><span class="line"><span class="comment">/* 4&gt; 0 if descriptor is readable */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="4-使用-SO-RCVTIMEO-套接字选项为-recvfrom-设置超时"><a href="#4-使用-SO-RCVTIMEO-套接字选项为-recvfrom-设置超时" class="headerlink" title="4. 使用 SO_RCVTIMEO 套接字选项为 recvfrom 设置超时"></a>4. 使用 SO_RCVTIMEO 套接字选项为 recvfrom 设置超时</h3><p>&emsp;&emsp;该操作设置一次即可，与套接字的读操作绑定，前面的方法都需要循环重新设置，本套接字选项仅适用于读操作，类似的 SO_SNDTIMEO 选项对应于写操作，两者均不能用于 connect 设置超时  </p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">dg_cli</span><span class="params">(FILE *fp, <span class="keyword">int</span> sockfd, <span class="keyword">const</span> SA *pservaddr, <span class="keyword">socklen_t</span> servlen)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">int</span>n;</span><br><span class="line"><span class="keyword">char</span>sendline[MAXLINE], recvline[MAXLINE + <span class="number">1</span>];</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">timeval</span><span class="title">tv</span>;</span></span><br><span class="line">    <span class="comment">// 指向 timeval 结构体的指针，保存的是超时的值</span></span><br><span class="line">tv.tv_sec = <span class="number">5</span>;</span><br><span class="line">tv.tv_usec = <span class="number">0</span>;</span><br><span class="line">Setsockopt(sockfd, SOL_SOCKET, SO_RCVTIMEO, &amp;tv, <span class="keyword">sizeof</span>(tv));</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> (Fgets(sendline, MAXLINE, fp) != <span class="literal">NULL</span>) &#123;</span><br><span class="line"></span><br><span class="line">Sendto(sockfd, sendline, <span class="built_in">strlen</span>(sendline), <span class="number">0</span>, pservaddr, servlen);</span><br><span class="line"></span><br><span class="line">n = recvfrom(sockfd, recvline, MAXLINE, <span class="number">0</span>, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span><br><span class="line"><span class="keyword">if</span> (n &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="comment">// I/O 超时操作，recvfrom 函数返回一个 EWOULDBLOCK 错误</span></span><br><span class="line"><span class="keyword">if</span> (errno == EWOULDBLOCK) &#123;</span><br><span class="line"><span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"socket timeout\n"</span>);</span><br><span class="line"><span class="keyword">continue</span>;</span><br><span class="line">&#125; <span class="keyword">else</span></span><br><span class="line">err_sys(<span class="string">"recvfrom error"</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">recvline[n] = <span class="number">0</span>;<span class="comment">/* null terminate */</span></span><br><span class="line">Fputs(recvline, <span class="built_in">stdout</span>);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="14-3-recv-和-send-函数"><a href="#14-3-recv-和-send-函数" class="headerlink" title="14.3 recv 和 send 函数"></a>14.3 recv 和 send 函数</h1><p>类似于 read 和 write 函数，不过多一个参数 flags</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="keyword">ssize_t</span> recv(<span class="keyword">int</span> sockfd, <span class="keyword">void</span>* buff, <span class="keyword">size_t</span> nbytes, <span class="keyword">int</span> flags);</span><br><span class="line"><span class="keyword">ssize_t</span> send(<span class="keyword">int</span> sockfd, <span class="keyword">const</span> <span class="keyword">void</span>* buff, <span class="keyword">size_t</span> nbytes, <span class="keyword">int</span> flags);</span><br><span class="line"><span class="comment">// 返回：成功返回读入写出的字节数，出错返回 -1</span></span><br></pre></td></tr></table></figure><p>&emsp;&emsp;flag 可以用来标识一些操作，绕过路由表查找、仅本操作非阻塞、发送或接受外带数据、窥看外来消息、等待所有数据。具体用法参看 UNP-14.3<br>&emsp;&emsp;flag 是值传递，并不是值-结果参数。所以它只能从进程向内核传递标志，内核不能返回标志。随着协议的增加，需要实现这个操作使用 recvmsg 和 sendmsg 中用 msghdr 来进行引用传递  </p><h1 id="14-4-readv-和-writev-函数"><a href="#14-4-readv-和-writev-函数" class="headerlink" title="14.4 readv 和 writev 函数"></a>14.4 readv 和 writev 函数</h1><p>&emsp;&emsp;这两个函数类似 read 和 write，不过 readv 和 writev 允许单个系统调用读入或写出自一个或者多个缓冲区。这些操作被称为分散读和集中写，因为来自读操作的输入数据被分散到多个应用缓冲区中，而来自多个应用缓冲区的输出数据被集中提供给单个写操作。  </p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;sys/uio.h&gt;</span></span></span><br><span class="line"><span class="keyword">ssize_t</span> readv(<span class="keyword">int</span> fileds, <span class="keyword">const</span> struct iovec* iov, <span class="keyword">int</span> iovcnt);</span><br><span class="line"><span class="keyword">ssize_t</span> writev(<span class="keyword">int</span> fields, <span class="keyword">const</span> struct iovec* iov, <span class="keyword">int</span> iovcnt);</span><br><span class="line"><span class="comment">// 返回：成功返回读入或写出的字节数，出错返回 -1</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">iovec</span>&#123;</span></span><br><span class="line">    <span class="keyword">void</span> *iov_base;      <span class="comment">// buf 的开始地址</span></span><br><span class="line">    <span class="keyword">size_t</span> iov_len;      <span class="comment">// buf 的大小</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>&emsp;&emsp;struct iovec 结构体定义如上，函数第二个参数指向的是一个 iovec 的数组，一般系统中定义数组长度的常值为 16，最大值一般在 1024~2100<br>&emsp;&emsp;readv 和 writev 函数可用于任何描述符，不仅仅局限于套接字描述符。writev 是一个原子操作，所以对于 UDP 来说，一次 writev 仅产生一个 UDP 数据报  </p><h4 id="当一个-4-字节的-write-和-396-字节的-write-调用时可能会触发-naggle-算法合并它们，解决这个问题的首选方法就是使用-writev-函数"><a href="#当一个-4-字节的-write-和-396-字节的-write-调用时可能会触发-naggle-算法合并它们，解决这个问题的首选方法就是使用-writev-函数" class="headerlink" title="当一个 4 字节的 write 和 396 字节的 write 调用时可能会触发 naggle 算法合并它们，解决这个问题的首选方法就是使用 writev 函数"></a>当一个 4 字节的 write 和 396 字节的 write 调用时可能会触发 naggle 算法合并它们，解决这个问题的首选方法就是使用 writev 函数</h4><h1 id="14-5-recvmsg-和-sendmsg-函数"><a href="#14-5-recvmsg-和-sendmsg-函数" class="headerlink" title="14.5 recvmsg 和 sendmsg 函数"></a>14.5 recvmsg 和 sendmsg 函数</h1><p>这两个函数是最通用的 I/O 函数，可以替换上面所有的读写函数  </p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="keyword">ssize_t</span> recvmsg(<span class="keyword">int</span> sockfd, struct msghdr * msg, <span class="keyword">int</span> flags);</span><br><span class="line"><span class="keyword">ssize_t</span> sendmsg(<span class="keyword">int</span> sockfd, struct msghdr * msg, <span class="keyword">int</span> flags);</span><br><span class="line"><span class="comment">// 返回：成功读入或者写出的字节数，出错则为 -1</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">msghdr</span>&#123;</span>   <span class="comment">// 用来保存大部分参数</span></span><br><span class="line">    <span class="keyword">void</span> *mag_name;</span><br><span class="line">    <span class="keyword">socklen_t</span> msg_namelen;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">iovec</span> *<span class="title">msg_iov</span>;</span></span><br><span class="line">    <span class="keyword">int</span> msg_iovlen;</span><br><span class="line">    <span class="keyword">void</span> *msg_control;</span><br><span class="line">    <span class="keyword">socklen_t</span> msg_controllen;</span><br><span class="line">    <span class="keyword">int</span> msg_flags;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ol><li>mag_name 和 msg_namelen 这两个成员用于套接字未连接的场合（UDP 等），msg_name 指向一个套接字地址结构，低走用着在其中存放接受者（sendmsg）或者发送者（recvmsg）的协议地址。如果无需指明协议地址，msg_name 是空指针。msg_namelen 对于 sendmsg 是值参数，对于 recvmsg 是值-结果参数。  </li><li>msg_iov 和 msg_iovlen 这两个成员指定输入或输出缓冲区数组（就是 iovec 数组）  </li><li>msg_control 和 msg_controllen 这两个成员指定可选的辅助数据的位置和大小 msg_controllen 对于 recvmsg 来说是一个值-结果参数</li><li>对于 recvmsg 和 sendmsg 需要区别他们的两个标志变量，一个是传递值的 flags 参数，另一个是所传递 msghdr 结构的 msg_flags 成员，传递的是引用，因为传递给函数的是地址</li></ol><ul><li>只有 recvmsg 使用 msg_flags 参数。sendmsg 忽略该参数，相关参数设置参考 UNP-14.5  </li></ul><h3 id="五组-I-O-函数之间的差异"><a href="#五组-I-O-函数之间的差异" class="headerlink" title="五组 I/O 函数之间的差异"></a>五组 I/O 函数之间的差异</h3><table><thead><tr><th>函数</th><th>任何描述符</th><th>仅套接字描述符</th><th>单个读/写缓冲区</th><th>分散/集中读/写</th><th>可选标志</th><th>可选对端地址</th><th>可选控制信息</th></tr></thead><tbody><tr><td>read, write</td><td>√</td><td></td><td>√</td><td></td><td></td><td></td><td></td></tr><tr><td>readv, writev</td><td>√</td><td></td><td></td><td>√</td><td></td><td></td><td></td></tr><tr><td>recv, send</td><td></td><td>√</td><td>√</td><td></td><td>√</td><td></td><td></td></tr><tr><td>recvfrom, sendto</td><td></td><td>√</td><td>√</td><td></td><td>√</td><td>√</td><td></td></tr><tr><td>recvmsg, sendmsg</td><td></td><td>√</td><td></td><td>√</td><td>√</td><td>√</td><td>√</td></tr></tbody></table><h1 id="14-6-辅助数据"><a href="#14-6-辅助数据" class="headerlink" title="14.6 辅助数据"></a>14.6 辅助数据</h1><p>&emsp;&emsp;辅助数据可通过调用 sendmsg 和 recvmsg 这两个函数，使用 msghdr 结构中的 msg_control 和 msg_controllen 这两个成员来发送和接收，也叫做控制信息。<br>&emsp;&emsp;辅助数据由一个或多个辅助数据对象构成，每个对象以一个定义在头文件&lt;sys/socket.h&gt;中的 cmsghdr 结构体</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">cmsghdr</span>&#123;</span></span><br><span class="line"><span class="keyword">socklen_t</span> cmsg_len;</span><br><span class="line"><span class="keyword">int</span> cmsg_level;</span><br><span class="line"><span class="keyword">int</span> cmsg_type;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="14-7-排队的数据量"><a href="#14-7-排队的数据量" class="headerlink" title="14.7 排队的数据量"></a>14.7 排队的数据量</h1><p>&emsp;&emsp;有时候我们想要在不真正读取数据的前提下直到一个套接字上已有多少数据排队等待着读取。有三种技术可以获得一排队数据量。  </p><ol><li>如果获取排队数据量的目的在于避免读操作内核阻塞，呢么可以使用非阻塞式 I/O，但是不能获得数据量啊，只能直到是否有数据。</li><li>如果我们既想查看数据又想数据留在接收队列中等待其余部分的读取，使用 MSG_PEEK 标志。可以使用非阻塞套接字来实现既能判断是否有数据可读，然后又可以查看数据但不读取。需要注意的是对于 TCP 连接，两次获取量的值大小可能不同，如果在两次获取之间收到了流数据。但是 UDP 仅返回第一个数据报的大小，所以即使两次之间有新的数据报，也不影响</li><li>一些实现支持 ioctl 的 FIONREAD 命令。该命令的第三个 ioctl 参数是指向某个整数的一个指针，内核通过该整数返回的值就是套接字接收队列的当前字节数。该值是已排队字节的总和，对于 UDP 包括所有已排队的数据报。某些实现中，队 UDP 套接字返回的值还包括一个套接字地址结构的空间，其中含有发送者的 IP 地址和端口号</li></ol><h1 id="14-8-套接字和标准-I-O"><a href="#14-8-套接字和标准-I-O" class="headerlink" title="14.8 套接字和标准 I/O"></a>14.8 套接字和标准 I/O</h1><p>执行 I/O 还可以使用标准 I/O 函数库，使用标准 I/O 对套接字进行读取一般可以打开两个流，一个用来读，一个用来写。  </p><h4 id="不建议在套接字上使用标准-I-O"><a href="#不建议在套接字上使用标准-I-O" class="headerlink" title="不建议在套接字上使用标准 I/O"></a>不建议在套接字上使用标准 I/O</h4><h1 id="14-9-高级轮询技术"><a href="#14-9-高级轮询技术" class="headerlink" title="14.9 高级轮询技术"></a>14.9 高级轮询技术</h1><h3 id="kqueue-接口"><a href="#kqueue-接口" class="headerlink" title="kqueue 接口"></a>kqueue 接口</h3><p>&emsp;&emsp;本接口允许进程向内核注册描述所关注的 kqueue 事件的事件过滤器。事件除了与 select 所关注类似的文件 IO 超时外，还有异步 IO、文件修改通知、进程跟踪、信号处理</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;sys/event.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;sys/time.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">kqueue</span><span class="params">(<span class="keyword">void</span>)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">kevent</span><span class="params">(<span class="keyword">int</span> kq, <span class="keyword">const</span> struct kevent * changelist, <span class="keyword">int</span> nchanges,</span></span></span><br><span class="line"><span class="function"><span class="params">struct kevent * eventlist, <span class="keyword">int</span> nevents,</span></span></span><br><span class="line"><span class="function"><span class="params"><span class="keyword">const</span> struct timespec * timeout)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">EV_SET</span><span class="params">(struct kevent *kev, <span class="keyword">uintptr_t</span> ident, <span class="keyword">short</span> filter, </span></span></span><br><span class="line"><span class="function"><span class="params">u_short flags, u_int fflags, <span class="keyword">intptr_t</span> data, <span class="keyword">void</span> *udata)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">kevent</span>&#123;</span></span><br><span class="line"><span class="keyword">uintptr_t</span> ident;</span><br><span class="line"><span class="keyword">short</span> filter;</span><br><span class="line">u_short flags;</span><br><span class="line">u_int fflags;</span><br><span class="line"><span class="keyword">intptr_t</span> data;</span><br><span class="line"><span class="keyword">void</span> *udata;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>&emsp;&emsp;kqueue 函数返回一个新的 kqueue 描述符，用于后面的 kevent 调用。kevent 函数即用于注册所关注的事件，也用于确定是否有所关注事件发生。changelist 和 nchanges 这两个参数给出对所关注事件做出更改，没有的话设置为 NULL，0。关于 timeout 结构体的区别，select 是纳秒，而 kqueue 是微秒。</p><h1 id="14-10-t-tcp：事务目的-tcp"><a href="#14-10-t-tcp：事务目的-tcp" class="headerlink" title="14.10 t/tcp：事务目的 tcp"></a>14.10 t/tcp：事务目的 tcp</h1><p>&emsp;&emsp;T/TCP 是对 TCP 的略微修改，避免最近通信过的主机之间再次三次握手。它能把 SYN，FIN 和数据组合到单个分节中，前提是一个分节可以存储这些数据。<br>&emsp;&emsp;T/TCP 包含所有 TCP 的特性，使得基于 TCP 的连接有了类似于 UDP 的效果，即两个主机之间频繁连接断开，但是使用 T/TCP 可以使得三次握手的消耗几乎为 0  </p><h1 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h1><ol><li>在套接字操作上设置时间限制的方法有三个：</li></ol><ul><li>使用 alarm 函数和 SIGALRM 信号。</li><li>使用由 select 提供的时间限制</li><li>使用较新的 SO_RCVTIMEO 和 SO_SNDTIMEO 套接字选项  </li></ul><ol start="2"><li>第一种方法简单易用，但是涉及信号处理，可能引发竞争条件。使用 select 会阻塞在 select 上，而不是阻塞在 read，write，connect 调用上。第三种方法不是所有系统都提供。  </li><li>recvmsg 和 sendmsg 是 5 组读写函数中最通用的。它有其余读写函数的所有特性：指定 MSG_xxx，返回或指定对端的协议地址，使用多个缓冲区，还增加了两个新特性：给应用进程返回标志，接收或者发送辅助数据。  </li><li>C 标准 I/O 可以用在套接字上，但是并不推荐使用。  </li><li>T/TCP 是对 TCP 的简单增强版本，能够在两台主机最近通信的前提下避免三路握手，使得对端更快的做出应答。从编程角度看，客户端通过调用 sendto 而不是通常使用的 connect write shutdown 调用序列发挥 T/TCP 的优势</li></ol>]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;14-1-概述&quot;&gt;&lt;a href=&quot;#14-1-概述&quot; class=&quot;headerlink&quot; title=&quot;14.1 概述&quot;&gt;&lt;/a&gt;14.1 概述&lt;/h1&gt;&lt;p&gt;&amp;emsp;&amp;emsp;本章讨论 I/O 的高级操作，首先是在 I/O 上设置超时，这里有三种方法。然后是 read 和 write 的三个变体：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;recv 和 send 允许通过第四个参数从进程到内核传递标志&lt;/li&gt;
&lt;li&gt;readv 和 writev 允许指定往其中输入数据或从其中输出数据的缓冲区向量&lt;/li&gt;
&lt;li&gt;recvmsg 和 sendmsg 结合了其他 I/O 函数的所有特性，并具备接收和发送辅助数据的新能力&lt;/li&gt;&lt;/ol&gt;
    
    </summary>
    
    
      <category term="UNP" scheme="http://yoursite.com/categories/UNP/"/>
    
    
  </entry>
  
  <entry>
    <title>第15章：Unix域协议</title>
    <link href="http://yoursite.com/2019/09/25/15.Unix%E5%9F%9F%E5%8D%8F%E8%AE%AE/"/>
    <id>http://yoursite.com/2019/09/25/15.Unix域协议/</id>
    <published>2019-09-25T14:54:48.820Z</published>
    <updated>2019-09-28T08:46:38.257Z</updated>
    
    <content type="html"><![CDATA[<h1 id="15-1-概述"><a href="#15-1-概述" class="headerlink" title="15.1 概述"></a>15.1 概述</h1><p>&emsp;&emsp;Unix 域协议并不是一个实际的协议族，而是在单个主机上执行客户/服务器通信的一种方法。所用 API 就是不同主机通信的 API，Unix 域协议也可以看作 IPC 方法之一。<br>&emsp;&emsp;Unix 域协议提供两类套接字：字节流套接字（类似 TCP）和数据报套接字（类似 UDP）<br>使用 Unix 域协议的原因：</p><ol><li>同主机使用 Unix 域协议性能比普通 TCP 套接字快出一倍</li><li>Unix 域套接字可用于同一主机上的不同进程之间传递描述符</li><li>Unix 域套接字新的实现把客户的凭证提供给服务器，这样可以提供额外的安全措施检查<a id="more"></a><h1 id="15-2-unix-域套接字地址结构"><a href="#15-2-unix-域套接字地址结构" class="headerlink" title="15.2 unix 域套接字地址结构"></a>15.2 unix 域套接字地址结构</h1></li></ol><p>Unix 域套接字地址结构如下</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_un</span>&#123;</span></span><br><span class="line">    <span class="keyword">sa_family_t</span> sun_family;  <span class="comment">// AF_LOCAL</span></span><br><span class="line">    <span class="keyword">char</span> sun_path[<span class="number">104</span>];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>sun_path 必须使用空字符结尾，为指定地址，就是 sun_path[0] = 0 的地址，等价于 IPv4 的 INADDR_ANY 常值  </p><h3 id="Unix-域套接字的-bind-调用"><a href="#Unix-域套接字的-bind-调用" class="headerlink" title="Unix 域套接字的 bind 调用"></a>Unix 域套接字的 bind 调用</h3><p>下面的程序创建了一个 Unix 域套接字，并 bind 一个路径名，在调用 getsockname 输出绑定的路径名</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">int</span>sockfd;</span><br><span class="line"><span class="keyword">socklen_t</span>len;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_un</span><span class="title">addr1</span>, <span class="title">addr2</span>;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (argc != <span class="number">2</span>)</span><br><span class="line">err_quit(<span class="string">"usage: unixbind &lt;pathname&gt;"</span>);</span><br><span class="line"></span><br><span class="line">sockfd = Socket(AF_LOCAL, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    <span class="comment">// 如果文件系统中已经存在这个路径名，bind 会调用失败，所以先使用 unlink 删除这个路径名</span></span><br><span class="line">unlink(argv[<span class="number">1</span>]);<span class="comment">/* OK if this fails */</span></span><br><span class="line">    <span class="comment">// 设置绑定的地址</span></span><br><span class="line">bzero(&amp;addr1, <span class="keyword">sizeof</span>(addr1));</span><br><span class="line">addr1.sun_family = AF_LOCAL;   <span class="comment">// AF_LOCAL</span></span><br><span class="line"><span class="built_in">strncpy</span>(addr1.sun_path, argv[<span class="number">1</span>], <span class="keyword">sizeof</span>(addr1.sun_path)<span class="number">-1</span>);</span><br><span class="line">Bind(sockfd, (SA *) &amp;addr1, SUN_LEN(&amp;addr1));</span><br><span class="line"></span><br><span class="line">len = <span class="keyword">sizeof</span>(addr2);</span><br><span class="line">Getsockname(sockfd, (SA *) &amp;addr2, &amp;len);</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"bound name = %s, returned len = %d\n"</span>, addr2.sun_path, len);</span><br><span class="line"></span><br><span class="line"><span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="15-3-socketpair-函数"><a href="#15-3-socketpair-函数" class="headerlink" title="15.3 socketpair 函数"></a>15.3 socketpair 函数</h1><p>socketpair 函数创建两个随后连接起来的套接字，本函数仅仅适用于 Unix 域套接字</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">socketpair</span><span class="params">(<span class="keyword">int</span> family, <span class="keyword">int</span> type, <span class="keyword">int</span> protocol, <span class="keyword">int</span> sockfd[<span class="number">2</span>])</span></span>;</span><br><span class="line"><span class="comment">// 返回：成功返回非 0，出错返回 -1</span></span><br></pre></td></tr></table></figure><ul><li>family 必须是 AF_LOCAL</li><li>protocol 必须是 0</li><li>type 可以是 SOCK_STREAM 或者 SOCK_DGRAM</li><li>新创建的两个描述符用 sockfd 存储返回</li></ul><p>&emsp;&emsp;使用 SOCK_STREAM 创建的结果称为 stream pipe，它和调用 pipe 创建的普通 Unix pipe 类似，差别在于 stream pipe 是全双工的，即两个描述符都是即可读又可写。</p><h1 id="15-4-套接字函数"><a href="#15-4-套接字函数" class="headerlink" title="15.4 套接字函数"></a>15.4 套接字函数</h1><ol><li>由 bind 创建的路径名默认访问权限应为 0777 并根据当前 umask 值进行修正</li><li>与 Unix 域套接字关联的路径名应该是一个绝对路径名，因为运行时的目录是从当前目录开始的，会造成不可预测的结果</li><li>在 connect 调用中指定的路径必须是一个绑定在某个打开的 Unix 域套接字上的路径名，而且它们的套接字类型（数据报或字节流）也必须一致</li><li>调用 connect 连接一个 Unix 域套接字涉及的权限测试等同于调用 open 以只写方式访问相应的路径名</li><li>unix 域字节流套接字类似于 TCP 套接字：他们都为进程提供一个无记录边界的字节流接口</li><li>对于某个 Unix 域字节流套接字的 connect 发现监听队列满了，会直接返回错误。但是通常的 TCP 服务器在监视队列满了会忽略这个 SYN，会导致客户端重发消息</li><li>unix 域数据报套接字类似于 UDP 套接字：他们都为进程提供一个保留记录边界的不可靠的数据报服务</li><li>在一个未绑定的 Unix 域套接字上发送数据报不会自动给这个套接字捆绑一个路径名，这一点不同于 UDP 套接字。类似的，对于某个 Unix 域数据报套接字的 connect 调用不会给本套接字捆绑一个路径名，这一点不同于 TCP 和 UDP</li></ol><h1 id="15-5-unix-域字节流客户-服务器程序"><a href="#15-5-unix-域字节流客户-服务器程序" class="headerlink" title="15.5 unix 域字节流客户/服务器程序"></a>15.5 unix 域字节流客户/服务器程序</h1><p>使用 Unix 域字节流套接字重写回射服务器</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">int</span>listenfd, connfd;</span><br><span class="line"><span class="keyword">pid_t</span>childpid;</span><br><span class="line"><span class="keyword">socklen_t</span>clilen;</span><br><span class="line">    <span class="comment">// 两个套接字的地址结构均是 sockaddr_un 类型</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_un</span><span class="title">cliaddr</span>, <span class="title">servaddr</span>;</span></span><br><span class="line"><span class="function"><span class="keyword">void</span><span class="title">sig_chld</span><span class="params">(<span class="keyword">int</span>)</span></span>;</span><br><span class="line">    <span class="comment">// 创建 Unix 域字节流套接字描述符</span></span><br><span class="line">listenfd = Socket(AF_LOCAL, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    <span class="comment">// 关联本机一个路径，并使用 unlink 先删除这个路径</span></span><br><span class="line">unlink(UNIXSTR_PATH);</span><br><span class="line">bzero(&amp;servaddr, <span class="keyword">sizeof</span>(servaddr));</span><br><span class="line">servaddr.sun_family = AF_LOCAL;</span><br><span class="line"><span class="built_in">strcpy</span>(servaddr.sun_path, UNIXSTR_PATH);</span><br><span class="line"></span><br><span class="line">Bind(listenfd, (SA *) &amp;servaddr, <span class="keyword">sizeof</span>(servaddr));</span><br><span class="line"></span><br><span class="line">Listen(listenfd, LISTENQ);</span><br><span class="line"></span><br><span class="line">Signal(SIGCHLD, sig_chld);</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> ( ; ; ) &#123;</span><br><span class="line">clilen = <span class="keyword">sizeof</span>(cliaddr);</span><br><span class="line"><span class="keyword">if</span> ( (connfd = accept(listenfd, (SA *) &amp;cliaddr, &amp;clilen)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line"><span class="keyword">if</span> (errno == EINTR)</span><br><span class="line"><span class="keyword">continue</span>;<span class="comment">/* back to for() */</span></span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">err_sys(<span class="string">"accept error"</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> ( (childpid = Fork()) == <span class="number">0</span>) &#123;<span class="comment">/* child process */</span></span><br><span class="line">Close(listenfd);<span class="comment">/* close listening socket */</span></span><br><span class="line">str_echo(connfd);<span class="comment">/* process request */</span></span><br><span class="line"><span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line">Close(connfd);<span class="comment">/* parent closes connected socket */</span></span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>使用 Unix 域字节流套接字重写回射客户端</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">int</span>sockfd;</span><br><span class="line">    <span class="comment">// Unix 域套接字地址结构</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_un</span><span class="title">servaddr</span>;</span></span><br><span class="line">    <span class="comment">// 参数必须是 AF_LOCAL</span></span><br><span class="line">sockfd = Socket(AF_LOCAL, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">bzero(&amp;servaddr, <span class="keyword">sizeof</span>(servaddr));</span><br><span class="line">servaddr.sun_family = AF_LOCAL;</span><br><span class="line">    <span class="comment">// 复制路径名，这里不需要使用 unlink</span></span><br><span class="line"><span class="built_in">strcpy</span>(servaddr.sun_path, UNIXSTR_PATH);</span><br><span class="line"></span><br><span class="line">Connect(sockfd, (SA *) &amp;servaddr, <span class="keyword">sizeof</span>(servaddr));</span><br><span class="line"></span><br><span class="line">str_cli(<span class="built_in">stdin</span>, sockfd);<span class="comment">/* do it all */</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="15-6-unix-域数据报客户-服务器程序"><a href="#15-6-unix-域数据报客户-服务器程序" class="headerlink" title="15.6 unix 域数据报客户/服务器程序"></a>15.6 unix 域数据报客户/服务器程序</h1><p>使用 Unix 域数据报套接字重写回射服务器</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">int</span>sockfd;</span><br><span class="line">    <span class="comment">// 两个套接字的地址结构均是 sockaddr_un 类型</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_un</span><span class="title">servaddr</span>, <span class="title">cliaddr</span>;</span></span><br><span class="line">    <span class="comment">// 参数必须是 AF_LOCAL</span></span><br><span class="line">sockfd = Socket(AF_LOCAL, SOCK_DGRAM, <span class="number">0</span>);</span><br><span class="line">    <span class="comment">// 关联本机一个路径，并使用 unlink 先删除这个路径</span></span><br><span class="line">unlink(UNIXDG_PATH);</span><br><span class="line">bzero(&amp;servaddr, <span class="keyword">sizeof</span>(servaddr));</span><br><span class="line">servaddr.sun_family = AF_LOCAL;</span><br><span class="line"><span class="built_in">strcpy</span>(servaddr.sun_path, UNIXDG_PATH);</span><br><span class="line"></span><br><span class="line">Bind(sockfd, (SA *) &amp;servaddr, <span class="keyword">sizeof</span>(servaddr));</span><br><span class="line"></span><br><span class="line">dg_echo(sockfd, (SA *) &amp;cliaddr, <span class="keyword">sizeof</span>(cliaddr));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>使用 Unix 域数据报套接字重写回射客户端</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">int</span>sockfd;</span><br><span class="line">    <span class="comment">// Unix 域套接字地址结构</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_un</span><span class="title">cliaddr</span>, <span class="title">servaddr</span>;</span></span><br><span class="line">    <span class="comment">// 参数必须是 AF_LOCAL</span></span><br><span class="line">sockfd = Socket(AF_LOCAL, SOCK_DGRAM, <span class="number">0</span>);</span><br><span class="line">    <span class="comment">// 和 UDP 不同的是，使用 Unix 域套接字，必须使用 bind 绑定路径名到套接字，这样服务器才有回射的路径名</span></span><br><span class="line">    <span class="comment">// 否则服务器会回射到路径名为空的数据，这样服务器调用 sendto 会报错</span></span><br><span class="line">bzero(&amp;cliaddr, <span class="keyword">sizeof</span>(cliaddr));<span class="comment">/* bind an address for us */</span></span><br><span class="line">cliaddr.sun_family = AF_LOCAL;</span><br><span class="line"><span class="built_in">strcpy</span>(cliaddr.sun_path, tmpnam(<span class="literal">NULL</span>));</span><br><span class="line"></span><br><span class="line">Bind(sockfd, (SA *) &amp;cliaddr, <span class="keyword">sizeof</span>(cliaddr));</span><br><span class="line"></span><br><span class="line">bzero(&amp;servaddr, <span class="keyword">sizeof</span>(servaddr));<span class="comment">/* fill in server's address */</span></span><br><span class="line">servaddr.sun_family = AF_LOCAL;</span><br><span class="line"><span class="built_in">strcpy</span>(servaddr.sun_path, UNIXDG_PATH);</span><br><span class="line"></span><br><span class="line">dg_cli(<span class="built_in">stdin</span>, sockfd, (SA *) &amp;servaddr, <span class="keyword">sizeof</span>(servaddr));</span><br><span class="line"></span><br><span class="line"><span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="15-7-描述符传递"><a href="#15-7-描述符传递" class="headerlink" title="15.7 描述符传递"></a>15.7 描述符传递</h1><p>&emsp;&emsp;可以在两个进程之间创建一个 Unix 域套接字，使用 sendmsg 跨这个套接字发送一个特殊的消息。这个消息交给内核处理，会把打开的描述符从发送进程传递到接收进程，步骤如下：</p><ol><li>创建一个 Unix 域套接字</li><li>发送进程通过调用返回描述符的任一 Unix 函数打开一个描述符，例如 open、pipe、mkfifo、socket、accept。可以在进程之间传递的描述符类型不限，包括各种描述符，不仅仅局限于文件描述符</li><li>发送进程创建一个 msghdr 结构，描述符使用辅助数据进行传递，所以只能使用 recvmsg 和 sendmsg 来处理。发送一个描述符会让它的引用计数加一</li><li>接收方调用 recvmsg 来接受这个描述符。会在进程中创建一个新的描述符，和发送端的指向相同的文件表项</li></ol><p><strong>UNP-15.7 小节提供了一个很好的描述符传递的例子，推荐仔细看看</strong></p><h1 id="15-8-接收发送者的凭证"><a href="#15-8-接收发送者的凭证" class="headerlink" title="15.8 接收发送者的凭证"></a>15.8 接收发送者的凭证</h1><p>&emsp;&emsp;本章开头说过的用户凭证通过辅助数据进行传递，其结构一般根据不同系统有所不同，当客户端和服务器进行通讯时，服务器通常需要根据一定手段获取客户的身份，用来验证客户是否有权限请求相应的服务。</p><h1 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h1><ul><li>Unix 域套接字是客户和服务器在同一主机上的 IPC 方法之一。和其余 IPC 方式相比，Unix 域套接字的优势在于其 API 和网络传递数据的 API 几乎相同。并且在同一主机的情况下，性能优于 TCP 类连接性能  </li><li>编写 Unix 连接时，应注意的点是必须 bind 一个路径名到 UDP 套接字的客户，以使 UDP 服务器有发送应答的目的地  </li><li>同一个主机上客户和服务器之间的描述符传递是一个非常有用的技术，它通过 Unix 域套接字发生。</li></ul>]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;15-1-概述&quot;&gt;&lt;a href=&quot;#15-1-概述&quot; class=&quot;headerlink&quot; title=&quot;15.1 概述&quot;&gt;&lt;/a&gt;15.1 概述&lt;/h1&gt;&lt;p&gt;&amp;emsp;&amp;emsp;Unix 域协议并不是一个实际的协议族，而是在单个主机上执行客户/服务器通信的一种方法。所用 API 就是不同主机通信的 API，Unix 域协议也可以看作 IPC 方法之一。&lt;br&gt;&amp;emsp;&amp;emsp;Unix 域协议提供两类套接字：字节流套接字（类似 TCP）和数据报套接字（类似 UDP）&lt;br&gt;使用 Unix 域协议的原因：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;同主机使用 Unix 域协议性能比普通 TCP 套接字快出一倍&lt;/li&gt;
&lt;li&gt;Unix 域套接字可用于同一主机上的不同进程之间传递描述符&lt;/li&gt;
&lt;li&gt;Unix 域套接字新的实现把客户的凭证提供给服务器，这样可以提供额外的安全措施检查&lt;/li&gt;&lt;/ol&gt;
    
    </summary>
    
    
      <category term="UNP" scheme="http://yoursite.com/categories/UNP/"/>
    
    
  </entry>
  
  <entry>
    <title>第16章：非阻塞式IO</title>
    <link href="http://yoursite.com/2019/09/25/16.%E9%9D%9E%E9%98%BB%E5%A1%9E%E5%BC%8FIO/"/>
    <id>http://yoursite.com/2019/09/25/16.非阻塞式IO/</id>
    <published>2019-09-25T14:54:48.820Z</published>
    <updated>2019-09-28T08:46:38.257Z</updated>
    
    <content type="html"><![CDATA[<h1 id="16-1-概述"><a href="#16-1-概述" class="headerlink" title="16.1 概述"></a>16.1 概述</h1><p>&emsp;&emsp;套接字的默认状态是阻塞的。这表示发出一个不能立即完成的套接字调用时，其进程会被投入睡眠，等待相应操作完成。可能阻塞的套接字调用可分为下面 4 种：</p><ol><li>输入操作，包括 read，readv，recv，recvfrom 和 recvmsg 共 5 个函数。如果某个进程对一个阻塞的 TCP 套接字调用这些输入函数之一，并且缓冲区中没有数据可以读取，进程会进入休眠，知道数据到达。TCP 是字节流协议，该进程的唤醒只要一些数据，如果要设置固定的量，可以使用 readn 或者设置 MSG_WAITALL 标志。对于非阻塞的套接字，如果输入操作不被满足，调用会立即返回一个 EWOULDBLOCK 错误</li><li>输出操作，包括 write，writev，send，sendto 和 sendmsg 共 5 个函数，和输入类似。对于一个非阻塞的 TCP 套接字，如果发送缓冲区没有空间，输出函数调用将立即返回一个 EWOULDBLOCK 错误。</li><li>接受外来连接，即 accept 函数。如果对一个阻塞的套接字调用该 accept 函数，并且没有新的连接到达，调用进程将被投入睡眠。如果一个非阻塞的套接字调用 accept 函数，并且尚无新的连接到达，accept 调用将立即返回一个 EWOLULDBLOCK 错误。</li><li>发起外出连接，即用于 TCP 的 connect 函数。TCP 连接的建立涉及一个三路握手过程，而且 connect 函数一直要等到客户收到对于自己的 SYN 的 ACK 才会返回。这意味着 TCP 的每个 connect 总会阻塞其调用进程至少一个 RTT 时间。如果对一个非阻塞的 TCP 套接字调用 connect，并且连接不能立即建立，那么连接的建立能照样发起，不过会返回一个 EINPROGRESS 错误，注意这个错误和上面的错误并不相同。还需要注意同一主机上的连接会立即建立完成，通常发生在同一主机的情况下。因此对于非阻塞的 connect，我们也要预备 connect 成功返回的情况发生。<a id="more"></a><h1 id="16-2-非阻塞读和写：str-cli-函数（修订版）"><a href="#16-2-非阻塞读和写：str-cli-函数（修订版）" class="headerlink" title="16.2 非阻塞读和写：str_cli 函数（修订版）"></a>16.2 非阻塞读和写：str_cli 函数（修订版）</h1></li></ol><p>非阻塞 I/O 使得缓冲区的管理较为复杂，同时避免使用标准 I/O 函数。  </p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">str_cli</span><span class="params">(FILE *fp, <span class="keyword">int</span> sockfd)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">int</span>maxfdp1, val, stdineof;</span><br><span class="line"><span class="keyword">ssize_t</span>n, nwritten;</span><br><span class="line">fd_setrset, wset;</span><br><span class="line"><span class="keyword">char</span>to[MAXLINE], fr[MAXLINE];</span><br><span class="line"><span class="keyword">char</span>*toiptr, *tooptr, *friptr, *froptr;</span><br><span class="line">    <span class="comment">// 使用 fcntl 把所有 3 个描述符都设置为非阻塞，包括连接到服务器的套接字，标准输入和标准输出</span></span><br><span class="line">val = Fcntl(sockfd, F_GETFL, <span class="number">0</span>);</span><br><span class="line">Fcntl(sockfd, F_SETFL, val | O_NONBLOCK);</span><br><span class="line"></span><br><span class="line">val = Fcntl(STDIN_FILENO, F_GETFL, <span class="number">0</span>);</span><br><span class="line">Fcntl(STDIN_FILENO, F_SETFL, val | O_NONBLOCK);</span><br><span class="line"></span><br><span class="line">val = Fcntl(STDOUT_FILENO, F_GETFL, <span class="number">0</span>);</span><br><span class="line">Fcntl(STDOUT_FILENO, F_SETFL, val | O_NONBLOCK);</span><br><span class="line">    <span class="comment">// 初始化指向两个缓冲区的指针，并把最大描述符加一，作为 select 的第一个参数</span></span><br><span class="line">toiptr = tooptr = to;<span class="comment">/* initialize buffer pointers */</span></span><br><span class="line">friptr = froptr = fr;</span><br><span class="line">stdineof = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">maxfdp1 = max(max(STDIN_FILENO, STDOUT_FILENO), sockfd) + <span class="number">1</span>;</span><br><span class="line"><span class="keyword">for</span> ( ; ; ) &#123;</span><br><span class="line">        <span class="comment">// 准备调用 select，两个描述符集都先清零，并注册每个套接字相应的监听事件</span></span><br><span class="line">FD_ZERO(&amp;rset);</span><br><span class="line">FD_ZERO(&amp;wset);</span><br><span class="line"><span class="keyword">if</span> (stdineof == <span class="number">0</span> &amp;&amp; toiptr &lt; &amp;to[MAXLINE])</span><br><span class="line">FD_SET(STDIN_FILENO, &amp;rset);<span class="comment">/* read from stdin */</span></span><br><span class="line"><span class="keyword">if</span> (friptr &lt; &amp;fr[MAXLINE])</span><br><span class="line">FD_SET(sockfd, &amp;rset);<span class="comment">/* read from socket */</span></span><br><span class="line"><span class="keyword">if</span> (tooptr != toiptr)</span><br><span class="line">FD_SET(sockfd, &amp;wset);<span class="comment">/* data to write to socket */</span></span><br><span class="line"><span class="keyword">if</span> (froptr != friptr)</span><br><span class="line">FD_SET(STDOUT_FILENO, &amp;wset);<span class="comment">/* data to write to stdout */</span></span><br><span class="line">        <span class="comment">// 调用 select，等待四个条件之一变为真。这里并没有设置超时</span></span><br><span class="line">Select(maxfdp1, &amp;rset, &amp;wset, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span><br><span class="line">        <span class="comment">// 如果标准输入可读，调用 read，第三个参数指向可用空间</span></span><br><span class="line"><span class="keyword">if</span> (FD_ISSET(STDIN_FILENO, &amp;rset)) &#123;</span><br><span class="line"><span class="keyword">if</span> ( (n = read(STDIN_FILENO, toiptr, &amp;to[MAXLINE] - toiptr)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="comment">// 通常不会发生，意味着 select 说可读，但是又读不出来</span></span><br><span class="line"><span class="keyword">if</span> (errno != EWOULDBLOCK)</span><br><span class="line">err_sys(<span class="string">"read error on stdin"</span>);</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (n == <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="comment">// 标准输入处理结束，设置 stdineof 标志</span></span><br><span class="line">                <span class="comment">// 如果发送缓冲区没有数据发送，就发送 FIN 给服务器，如果还有的话就发送完再发送 FIN</span></span><br><span class="line"><span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"%s: EOF on stdin\n"</span>, gf_time());</span><br><span class="line">stdineof = <span class="number">1</span>;<span class="comment">/* all done with stdin */</span></span><br><span class="line"><span class="keyword">if</span> (tooptr == toiptr)</span><br><span class="line">Shutdown(sockfd, SHUT_WR);<span class="comment">/* send FIN */</span></span><br><span class="line"></span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line"><span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"%s: read %d bytes from stdin\n"</span>, gf_time(), n);</span><br><span class="line">                <span class="comment">// 添加到接收缓冲区中</span></span><br><span class="line">toiptr += n;<span class="comment">/* # just read */</span></span><br><span class="line">                </span><br><span class="line">FD_SET(sockfd, &amp;wset);<span class="comment">/* try and write to socket below */</span></span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">        <span class="comment">// 和 read 部分类似</span></span><br><span class="line"><span class="keyword">if</span> (FD_ISSET(sockfd, &amp;rset)) &#123;</span><br><span class="line"><span class="keyword">if</span> ( (n = read(sockfd, friptr, &amp;fr[MAXLINE] - friptr)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line"><span class="keyword">if</span> (errno != EWOULDBLOCK)</span><br><span class="line">err_sys(<span class="string">"read error on socket"</span>);</span><br><span class="line"></span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (n == <span class="number">0</span>) &#123;</span><br><span class="line"><span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"%s: EOF on socket\n"</span>, gf_time());</span><br><span class="line"><span class="keyword">if</span> (stdineof)</span><br><span class="line"><span class="keyword">return</span>;<span class="comment">/* normal termination */</span></span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">err_quit(<span class="string">"str_cli: server terminated prematurely"</span>);</span><br><span class="line"></span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line"><span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"%s: read %d bytes from socket\n"</span>,</span><br><span class="line">gf_time(), n);</span><br><span class="line">friptr += n;<span class="comment">/* # just read */</span></span><br><span class="line">                <span class="comment">// 打开写描述符集中与标准输出对应的位打开，尝试在第三部分中将这些数据写出到标准输出</span></span><br><span class="line">FD_SET(STDOUT_FILENO, &amp;wset);<span class="comment">/* try and write below */</span></span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">if</span> (FD_ISSET(STDOUT_FILENO, &amp;wset) &amp;&amp; ( (n = friptr - froptr) &gt; <span class="number">0</span>)) &#123;</span><br><span class="line"><span class="keyword">if</span> ( (nwritten = write(STDOUT_FILENO, froptr, n)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line"><span class="keyword">if</span> (errno != EWOULDBLOCK)</span><br><span class="line">err_sys(<span class="string">"write error to stdout"</span>);</span><br><span class="line"></span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line"><span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"%s: wrote %d bytes to stdout\n"</span>,</span><br><span class="line">gf_time(), nwritten);</span><br><span class="line">froptr += nwritten;<span class="comment">/* # just written */</span></span><br><span class="line"><span class="keyword">if</span> (froptr == friptr)</span><br><span class="line">froptr = friptr = fr;<span class="comment">/* back to beginning of buffer */</span></span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (FD_ISSET(sockfd, &amp;wset) &amp;&amp; ( (n = toiptr - tooptr) &gt; <span class="number">0</span>)) &#123;</span><br><span class="line"><span class="keyword">if</span> ( (nwritten = write(sockfd, tooptr, n)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line"><span class="keyword">if</span> (errno != EWOULDBLOCK)</span><br><span class="line">err_sys(<span class="string">"write error to socket"</span>);</span><br><span class="line"></span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line"><span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"%s: wrote %d bytes to socket\n"</span>,</span><br><span class="line">gf_time(), nwritten);</span><br><span class="line">tooptr += nwritten;<span class="comment">/* # just written */</span></span><br><span class="line"><span class="keyword">if</span> (tooptr == toiptr) &#123;</span><br><span class="line">toiptr = tooptr = to;<span class="comment">/* back to beginning of buffer */</span></span><br><span class="line"><span class="keyword">if</span> (stdineof)</span><br><span class="line">Shutdown(sockfd, SHUT_WR);<span class="comment">/* send FIN */</span></span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="下面还提供了使用-fork-来优化性能的版本，两个进程分别处理输入输出："><a href="#下面还提供了使用-fork-来优化性能的版本，两个进程分别处理输入输出：" class="headerlink" title="下面还提供了使用 fork 来优化性能的版本，两个进程分别处理输入输出："></a>下面还提供了使用 fork 来优化性能的版本，两个进程分别处理输入输出：</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">str_cli</span><span class="params">(FILE *fp, <span class="keyword">int</span> sockfd)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">pid_t</span>pid;</span><br><span class="line"><span class="keyword">char</span>sendline[MAXLINE], recvline[MAXLINE];</span><br><span class="line">    <span class="comment">// 创建一个子进程</span></span><br><span class="line"><span class="keyword">if</span> ( (pid = Fork()) == <span class="number">0</span>) &#123;<span class="comment">/* child: server -&gt; stdout */</span></span><br><span class="line">    <span class="comment">// 子进程用来处理数据的接收并写到标准输出</span></span><br><span class="line"><span class="keyword">while</span> (Readline(sockfd, recvline, MAXLINE) &gt; <span class="number">0</span>)</span><br><span class="line">Fputs(recvline, <span class="built_in">stdout</span>);</span><br><span class="line"></span><br><span class="line">kill(getppid(), SIGTERM);<span class="comment">/* in case parent still running */</span></span><br><span class="line"><span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* parent: stdin -&gt; server */</span></span><br><span class="line">        <span class="comment">// 父进程处理标准输出的接收和数据的发送</span></span><br><span class="line"><span class="keyword">while</span> (Fgets(sendline, MAXLINE, fp) != <span class="literal">NULL</span>)</span><br><span class="line">Writen(sockfd, sendline, <span class="built_in">strlen</span>(sendline));</span><br><span class="line">    <span class="comment">// 发送完毕，发送一个 FIN</span></span><br><span class="line">Shutdown(sockfd, SHUT_WR);<span class="comment">/* EOF on stdin, send FIN */</span></span><br><span class="line">    <span class="comment">// 等待子进程接收操作</span></span><br><span class="line">pause();</span><br><span class="line"><span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>两种版本均比 select 加阻塞 I/O 版本快出很多，fork 版本比非阻塞 I/O 版本略慢，但是代码简洁很多，推荐使用 fork 版本。</p><h1 id="16-3-非阻塞-connect"><a href="#16-3-非阻塞-connect" class="headerlink" title="16.3 非阻塞 connect"></a>16.3 非阻塞 connect</h1><p>&emsp;&emsp;当在一个非阻塞的 TCP 套接字上使用 connect 时，会立即返回一个 EINPROGRESS 错误，不过已经发起的三路握手会继续进行。我们接着使用 select 检测这个连接获成功或失败的已建立条件。非阻塞 connect 上有三个用途：</p><ol><li>我们可以把三路握手的时间进行叠加。完成一个 connect 要花费 RTT 时间，而 RTT 的波动较大，从局域网的几毫秒到广域网的几秒，这段时间可以充分利用</li><li>我们可以使用该技术同时建立多个连接。这个用途已随着 web 浏览器变得流行起来</li><li>既然使用 select 等待连接的建立，可以给 select 设置一个时间限制，使得可以缩短 connect 的超时，普通 connect 超时时间是 75s，我们想要一个更短的时间，就可以把 connect 设置为非阻塞状态，对于非阻塞 connect，要做如下处理：<ul><li>如果客户端和服务器处于同一个主机，连接立即建立，需要处理这种情形。</li><li>关于 select 和 非阻塞 connect，当连接成功建立时，描述符变为可写；当遇到错误时，描述符即可读又可写。</li></ul></li></ol><h1 id="16-4-非阻塞-connect：时间获取客户程序"><a href="#16-4-非阻塞-connect：时间获取客户程序" class="headerlink" title="16.4 非阻塞 connect：时间获取客户程序"></a>16.4 非阻塞 connect：时间获取客户程序</h1><p>如下代码执行了非阻塞 connect</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">connect_nonb</span><span class="params">(<span class="keyword">int</span> sockfd, <span class="keyword">const</span> SA *saptr, <span class="keyword">socklen_t</span> salen, <span class="keyword">int</span> nsec)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">int</span>flags, n, error;</span><br><span class="line"><span class="keyword">socklen_t</span>len;</span><br><span class="line">fd_setrset, wset;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">timeval</span><span class="title">tval</span>;</span></span><br><span class="line">    <span class="comment">// 先获取原套接字描述符</span></span><br><span class="line">flags = Fcntl(sockfd, F_GETFL, <span class="number">0</span>);</span><br><span class="line">    <span class="comment">// 调用 fcntl 设置为非阻塞</span></span><br><span class="line">Fcntl(sockfd, F_SETFL, flags | O_NONBLOCK);</span><br><span class="line">    <span class="comment">// 发起非阻塞的 connect，期望的错误是 EINPROGRESS，表示连接建立已经启动但是尚未完成。connect 返回的任何其他错误返回给本函数的调用者。</span></span><br><span class="line">error = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">if</span> ( (n = connect(sockfd, saptr, salen)) &lt; <span class="number">0</span>)</span><br><span class="line"><span class="keyword">if</span> (errno != EINPROGRESS)</span><br><span class="line"><span class="keyword">return</span>(<span class="number">-1</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Do whatever we want while the connect is taking place. */</span></span><br><span class="line">    <span class="comment">// 此时可以在 RTT 时间中做我们想做的事</span></span><br><span class="line">    <span class="comment">// h == 0，连接已经建立，处于同一主机，立即建立连接，直接跳转到 done</span></span><br><span class="line"><span class="keyword">if</span> (n == <span class="number">0</span>)</span><br><span class="line"><span class="keyword">goto</span> done;<span class="comment">/* connect completed immediately */</span></span><br><span class="line">    <span class="comment">// 调用 select 等带连接的建立完成</span></span><br><span class="line">FD_ZERO(&amp;rset);</span><br><span class="line">FD_SET(sockfd, &amp;rset);</span><br><span class="line">wset = rset;</span><br><span class="line">tval.tv_sec = nsec;</span><br><span class="line">tval.tv_usec = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">// select 返回 0，超时情况发生，返回 ETIMEOUT 错误返回给调用者。还需要关闭套接字，防止三路握手继续下去</span></span><br><span class="line"><span class="keyword">if</span> ( (n = Select(sockfd+<span class="number">1</span>, &amp;rset, &amp;wset, <span class="literal">NULL</span>,</span><br><span class="line"> nsec ? &amp;tval : <span class="literal">NULL</span>)) == <span class="number">0</span>) &#123;</span><br><span class="line">close(sockfd);<span class="comment">/* timeout */</span></span><br><span class="line">errno = ETIMEDOUT;</span><br><span class="line"><span class="keyword">return</span>(<span class="number">-1</span>);</span><br><span class="line">&#125;</span><br><span class="line">    <span class="comment">// 变成可读或者可读可写</span></span><br><span class="line"><span class="keyword">if</span> (FD_ISSET(sockfd, &amp;rset) || FD_ISSET(sockfd, &amp;wset)) &#123;</span><br><span class="line">len = <span class="keyword">sizeof</span>(error);</span><br><span class="line">        <span class="comment">// 获取待处理错误，建立成功返回 0.建立出错，返回对应的 error</span></span><br><span class="line"><span class="keyword">if</span> (getsockopt(sockfd, SOL_SOCKET, SO_ERROR, &amp;error, &amp;len) &lt; <span class="number">0</span>)</span><br><span class="line"><span class="keyword">return</span>(<span class="number">-1</span>);<span class="comment">/* Solaris pending error */</span></span><br><span class="line">&#125; <span class="keyword">else</span></span><br><span class="line">err_quit(<span class="string">"select error: sockfd not set"</span>);</span><br><span class="line"></span><br><span class="line">done:</span><br><span class="line">    <span class="comment">// 恢复套接字原来的文件状态标志</span></span><br><span class="line">Fcntl(sockfd, F_SETFL, flags);<span class="comment">/* restore file status flags */</span></span><br><span class="line">    <span class="comment">// 有 error 标志，关闭描述符并返回 -1</span></span><br><span class="line"><span class="keyword">if</span> (error) &#123;</span><br><span class="line">close(sockfd);<span class="comment">/* just in case */</span></span><br><span class="line">errno = error;</span><br><span class="line"><span class="keyword">return</span>(<span class="number">-1</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>判断连接是否建立成功，可以使用下面的方式来代替 getsockopt：</p><ol><li>调用 getpeername 代替 getsockopt。如果 getpeername 以 ENOTCONN 错误失败返回，连接建立失败，接着使用 getsockopt 来获取待处理的错误并进行处理返回</li><li>以值为 0 的长度参数调用 read，如果失败，连接建立失败，read 返回 error 给出失败原因。如果连接建立成功， read 返回 0</li><li>再调用 connect 一次。应该失败，如果错误是 EISCONN，套接字已经连接成功<br>不幸的是，非阻塞 connect 是网络编程中最不好一直的部分，一个简单的办法是为每一个连接创建一个处理线程。</li></ol><h3 id="被中断的-connect"><a href="#被中断的-connect" class="headerlink" title="被中断的 connect"></a>被中断的 connect</h3><p>&emsp;&emsp;对于阻塞的 connect 在三路握手完成前，收到了某种信号导致 connect 中断，假设内核不会重启 connect，会返回 EINTR，我们不能再次调用 connect 等待未连接继续完成。这样做会导致 EADDRINUSE 错误，这种情况只能使用 select，建立成功 select 中套接字可写，失败时 select 返回套接字即可读又可写条件。</p><h1 id="16-5-非阻塞-connect：web-客户程序"><a href="#16-5-非阻塞-connect：web-客户程序" class="headerlink" title="16.5 非阻塞 connect：web 客户程序"></a>16.5 非阻塞 connect：web 客户程序</h1><p>&emsp;&emsp;UNP-16.5 给出了一个很完善的非阻塞 connect web 客户程序，这里就不摘抄了。该例子使用非阻塞 connect 来进行客户端的连接，可以同时并行处理多个连接，优化系统的性能</p><h1 id="16-6-非阻塞-accept"><a href="#16-6-非阻塞-accept" class="headerlink" title="16.6 非阻塞 accept"></a>16.6 非阻塞 accept</h1><p>&emsp;&emsp;当有一个已完成的连接准备好被 accept 时，select 将作为可读描述符返会该连接的监听套接字。因此，如果我们使用 select 在某个监听套接字上等待一个外来链接，那就没有必要把该监听套接字设置为非阻塞，这是因为如果 select 告诉我们该套接字上已有连接就绪，那么随后的 accept 调用不应该阻塞。  </p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">int</span>sockfd;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">linger</span><span class="title">ling</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span><span class="title">servaddr</span>;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (argc != <span class="number">2</span>)</span><br><span class="line">err_quit(<span class="string">"usage: tcpcli &lt;IPaddress&gt;"</span>);</span><br><span class="line"></span><br><span class="line">sockfd = Socket(AF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">bzero(&amp;servaddr, <span class="keyword">sizeof</span>(servaddr));</span><br><span class="line">servaddr.sin_family = AF_INET;</span><br><span class="line">servaddr.sin_port = htons(SERV_PORT);</span><br><span class="line">Inet_pton(AF_INET, argv[<span class="number">1</span>], &amp;servaddr.sin_addr);</span><br><span class="line"></span><br><span class="line">Connect(sockfd, (SA *) &amp;servaddr, <span class="keyword">sizeof</span>(servaddr));</span><br><span class="line">    <span class="comment">// 一旦连接建立，设置 SO_LINGER 套接字选项，把 l_onoff 标志设置为 1，把 l_linger 时间设置为 0，这样的设置导致连接被关闭在 TCP 套接字上发送一个 RST，我们随后关闭该套接字。</span></span><br><span class="line">    <span class="comment">// 同时在服务端模拟延迟，连接建立时（连接还处于队列中，等待调用 accept）收到 RST，会把这个连接从队列中去除，所以等到调用 accept 时，没有连接，服务器重新阻塞</span></span><br><span class="line">ling.l_onoff = <span class="number">1</span>;<span class="comment">/* cause RST to be sent on close() */</span></span><br><span class="line">ling.l_linger = <span class="number">0</span>;</span><br><span class="line">Setsockopt(sockfd, SOL_SOCKET, SO_LINGER, &amp;ling, <span class="keyword">sizeof</span>(ling));</span><br><span class="line">Close(sockfd);</span><br><span class="line"></span><br><span class="line"><span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>解决注释中的问题方法如下：</p><ul><li>当使用 select 获悉某个监听套接字上有任何 accept 时，总把这个套接字设置为非阻塞。</li><li>在后续的调用中忽略一下错误：EWOULDBLOCK、ECONNABORTED、EPROTO 和 EINTR 错误</li></ul><h1 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h1><ul><li>select 通常结合非阻塞 I/O 一起使用，以便判断描述符合适可读可写。这个版本的客户程序时我们给出的所有版本中最快的。</li><li>非阻塞 connect 是我们能够在 TCP 三路握手发生期间做其他处理，而不是阻塞在 connect 上。</li></ul>]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;16-1-概述&quot;&gt;&lt;a href=&quot;#16-1-概述&quot; class=&quot;headerlink&quot; title=&quot;16.1 概述&quot;&gt;&lt;/a&gt;16.1 概述&lt;/h1&gt;&lt;p&gt;&amp;emsp;&amp;emsp;套接字的默认状态是阻塞的。这表示发出一个不能立即完成的套接字调用时，其进程会被投入睡眠，等待相应操作完成。可能阻塞的套接字调用可分为下面 4 种：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;输入操作，包括 read，readv，recv，recvfrom 和 recvmsg 共 5 个函数。如果某个进程对一个阻塞的 TCP 套接字调用这些输入函数之一，并且缓冲区中没有数据可以读取，进程会进入休眠，知道数据到达。TCP 是字节流协议，该进程的唤醒只要一些数据，如果要设置固定的量，可以使用 readn 或者设置 MSG_WAITALL 标志。对于非阻塞的套接字，如果输入操作不被满足，调用会立即返回一个 EWOULDBLOCK 错误&lt;/li&gt;
&lt;li&gt;输出操作，包括 write，writev，send，sendto 和 sendmsg 共 5 个函数，和输入类似。对于一个非阻塞的 TCP 套接字，如果发送缓冲区没有空间，输出函数调用将立即返回一个 EWOULDBLOCK 错误。&lt;/li&gt;
&lt;li&gt;接受外来连接，即 accept 函数。如果对一个阻塞的套接字调用该 accept 函数，并且没有新的连接到达，调用进程将被投入睡眠。如果一个非阻塞的套接字调用 accept 函数，并且尚无新的连接到达，accept 调用将立即返回一个 EWOLULDBLOCK 错误。&lt;/li&gt;
&lt;li&gt;发起外出连接，即用于 TCP 的 connect 函数。TCP 连接的建立涉及一个三路握手过程，而且 connect 函数一直要等到客户收到对于自己的 SYN 的 ACK 才会返回。这意味着 TCP 的每个 connect 总会阻塞其调用进程至少一个 RTT 时间。如果对一个非阻塞的 TCP 套接字调用 connect，并且连接不能立即建立，那么连接的建立能照样发起，不过会返回一个 EINPROGRESS 错误，注意这个错误和上面的错误并不相同。还需要注意同一主机上的连接会立即建立完成，通常发生在同一主机的情况下。因此对于非阻塞的 connect，我们也要预备 connect 成功返回的情况发生。&lt;/li&gt;&lt;/ol&gt;
    
    </summary>
    
    
      <category term="UNP" scheme="http://yoursite.com/categories/UNP/"/>
    
    
  </entry>
  
  <entry>
    <title>第五章：TCP客户&amp;服务器程序示例</title>
    <link href="http://yoursite.com/2019/09/25/05.TCP%E5%AE%A2%E6%88%B7&amp;%E6%9C%8D%E5%8A%A1%E5%99%A8%E7%A8%8B%E5%BA%8F%E7%A4%BA%E4%BE%8B/"/>
    <id>http://yoursite.com/2019/09/25/05.TCP客户&amp;服务器程序示例/</id>
    <published>2019-09-25T14:54:48.819Z</published>
    <updated>2019-09-28T08:46:38.256Z</updated>
    
    <content type="html"><![CDATA[<h1 id="5-1-概述"><a href="#5-1-概述" class="headerlink" title="5.1 概述"></a>5.1 概述</h1><p>&emsp;&emsp;本章使用上一章的函数来编写一个完整的 TCP 客户端服务器端的程序示例（回射服务器）</p><a id="more"></a><h1 id="5-2-TCP回射服务器程序：main函数"><a href="#5-2-TCP回射服务器程序：main函数" class="headerlink" title="5.2 TCP回射服务器程序：main函数"></a>5.2 TCP回射服务器程序：main函数</h1><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"unp.h"</span></span></span><br><span class="line"><span class="keyword">int</span> </span><br><span class="line">main(<span class="keyword">int</span> argc, <span class="keyword">char</span>** argv)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">int</span> listenfd, connfd;</span><br><span class="line">    <span class="keyword">pid_t</span> childpid;</span><br><span class="line">    <span class="keyword">socklen_t</span> clilen;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">cliaddr</span>,<span class="title">servaddr</span>;</span></span><br><span class="line">    <span class="comment">//创建一个TCP套接字，并捆绑通配地址，并将其转换为监听套接字</span></span><br><span class="line">    listenfd = Socket(AF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    <span class="comment">// 填充地址字段</span></span><br><span class="line">    bzero(&amp;servaddr, <span class="keyword">sizeof</span>(servaddr));</span><br><span class="line">    servaddr.sin_family = AF_INET;</span><br><span class="line">    servaddr.sin_addr.s_addr = htonl(INADDR_ANY);</span><br><span class="line">    servaddr.sin_port = htons(SERV_PORT);</span><br><span class="line">    <span class="comment">// 调用 bind 函数来将描述符绑定至本地协议地址上</span></span><br><span class="line">    Bind(listenfd, (SA*)&amp;servaddr, <span class="keyword">sizeof</span>(servaddr));</span><br><span class="line">    <span class="comment">// 转换成 listen 状态</span></span><br><span class="line">    Listen(listenfd, LISTENQ);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span>(;;)&#123;</span><br><span class="line">        client = <span class="keyword">sizeof</span>(cliaddr);</span><br><span class="line">        <span class="comment">//到达这一步，服务器阻塞在accept调用，并等待客户连接的完成</span></span><br><span class="line">        connfd = Accept(listenfd, (SA*) &amp;cliaddr, &amp;client);</span><br><span class="line">        <span class="comment">//服务器的并发部分处理，这里子进程并没有Close（connfd);</span></span><br><span class="line">        <span class="keyword">if</span>( (childpid = Fork()) == <span class="number">0</span>)&#123;</span><br><span class="line">            <span class="comment">// 关闭监听套接字描述符</span></span><br><span class="line">            Close(listenfd);</span><br><span class="line">            <span class="comment">// 处理客户</span></span><br><span class="line">            str_echo(connfd);</span><br><span class="line">            <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 父进程关闭套接字描述符</span></span><br><span class="line">        Close(connfd);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="5-3-TCP回射服务器程序：str-echo函数"><a href="#5-3-TCP回射服务器程序：str-echo函数" class="headerlink" title="5.3 TCP回射服务器程序：str_echo函数"></a>5.3 TCP回射服务器程序：str_echo函数</h1><p>回射服务器逻辑部分：从客户读入数据，并回射给客户</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"unp.h"</span></span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">str_echo</span><span class="params">(<span class="keyword">int</span> sockfd)</span></span>&#123;</span><br><span class="line">    <span class="keyword">ssize_t</span> n;</span><br><span class="line">    <span class="keyword">char</span> buf[MAXLINE];</span><br><span class="line">again:</span><br><span class="line">    <span class="comment">//如果客户关闭连接，则read会返回 0，则此函数结束，否则，继续循环。。。</span></span><br><span class="line">    <span class="keyword">while</span>( (n = read(sockfd, buf, MAXLINE)) &gt; <span class="number">0</span>)</span><br><span class="line">    <span class="comment">// 回射给客户</span></span><br><span class="line">        Writen(Sockfd, buf, n);</span><br><span class="line">    <span class="keyword">if</span>(n &lt; <span class="number">0</span> &amp;&amp; errno == EINTR)</span><br><span class="line">        <span class="keyword">goto</span> again;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span>(n &lt; <span class="number">0</span>)</span><br><span class="line">        err_sys(<span class="string">"str_echo: read error"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="5-4-TCP回射客户程序：main函数"><a href="#5-4-TCP回射客户程序：main函数" class="headerlink" title="5.4 TCP回射客户程序：main函数"></a>5.4 TCP回射客户程序：main函数</h1><p>话不多说直接上代码。。。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"unp.h"</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> sockfd;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">servaddr</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(argc != <span class="number">2</span>)&#123;</span><br><span class="line">        err_quit(<span class="string">"usage: tcpcli &lt;IPaddress&gt;"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    sockfd = Socket(AF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    bzero(&amp;servaddr, <span class="keyword">sizeof</span>(servaddr));</span><br><span class="line">    servaddr.sin_family = AF_INET;</span><br><span class="line">    servaddr.sin_port = htons(SERV_PORT);   <span class="comment">//这里是提前约定好的</span></span><br><span class="line">    Inet_pton(AF_INET, argv[<span class="number">1</span>], &amp;servaddr.sin_addr);</span><br><span class="line">    <span class="comment">// 连接服务器</span></span><br><span class="line">    Connect(sockfd, (SA*) &amp;servaddr, <span class="keyword">sizeof</span>(servaddr));</span><br><span class="line">    <span class="comment">// 客户逻辑</span></span><br><span class="line">    str_cli(<span class="built_in">stdin</span>, sockfd);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>创建一个TCP套接字，用服务器的IP地址和端口号装填一个网际网套接字地址结构，我们可从命令行参数取得服务器的IP地址，从头文件中获取端口号：SERV_PORT<br>str_cli是客户端的逻辑代码部分</p><h1 id="5-5-TCP回射客户程序：str-cli函数"><a href="#5-5-TCP回射客户程序：str-cli函数" class="headerlink" title="5.5 TCP回射客户程序：str_cli函数"></a>5.5 TCP回射客户程序：str_cli函数</h1><p>从标准输入读入一行文本，写到服务器上，读回服务器对该行的回射，并把回射写到标准输出上</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"unp.h"</span></span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">str_cli</span><span class="params">(FILE* fp, <span class="keyword">int</span> sockfd)</span></span>&#123;</span><br><span class="line">    <span class="keyword">char</span> sendline[MAXLINE], resvline[MAXLINE];</span><br><span class="line">    <span class="comment">// 不断地读入文本</span></span><br><span class="line">    <span class="keyword">while</span>(Fgets(sendline, MAXLINE, fp) != <span class="literal">NULL</span>)&#123;</span><br><span class="line">        <span class="comment">// 把文本发送给服务器</span></span><br><span class="line">        Writen(sockfd, sendline, <span class="built_in">strlen</span>(sndline));</span><br><span class="line">        <span class="comment">// 从服务器读取数据</span></span><br><span class="line">        <span class="keyword">if</span>(Readline(sockfd, recvline, MAXLINE) == <span class="number">0</span>)&#123;</span><br><span class="line">            err_quit(<span class="string">"something"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 写到标准输出</span></span><br><span class="line">        Fputs(recvline, <span class="built_in">stdout</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>读入一行文本，把该行发送给服务器，使用readline从服务器读入回射行，fputs把它写到标准输出<br>fgets 出错返回空指针，循环终止，遇到文件终止符 EOF 也会返回空指针</p><h1 id="5-8-POSIX信号处理"><a href="#5-8-POSIX信号处理" class="headerlink" title="5.8 POSIX信号处理"></a>5.8 POSIX信号处理</h1><p>信号就是某个进程发生了某个事件的通知，有时也称为软件中断，信号通常是异步发生的<br>信号可以由另一个进程发送给一个进程（或自身），也可以由内核发送给一个进程<br>SIGCHLD 是由内核在子进程终止时发送给其父进程的  </p><ul><li>调用 sigaction 处理信号有关的三种选择<ul><li>提供一个函数，只有特定的信号才进行调用（捕获信号），有两个信号不能被捕获（SIGKILL，SIGSTOP）信号处理函数由信号值这个单一的整数参数进行调用，且没有返回值：void handler(int signo)</li><li>可以将信号的处置设为SIG_IGN来进行忽略，SIGKILL和SIGSTOP这两个信号不能进行忽略</li><li>可以吧某个信号的处置设定为SIG_DEF来进行默认处理，通产是在收到信号后进行终止进程（SIGCHID和SIGURG的默认处理是忽略）</li></ul></li></ul><p><strong>一旦安装了信号处理函数，就一直安装着</strong><br><strong>在一个信号处理函数运行期间，被递交的信号阻塞</strong><br><strong>多个相同信号阻塞，恢复后仅递交一次</strong><br><strong>可以使用 sigprocmask 选择性的阻塞或解阻塞一组信号，带一些特殊代码片段进行操作</strong></p><h1 id="5-9-处理SIGCHLD信号"><a href="#5-9-处理SIGCHLD信号" class="headerlink" title="5.9 处理SIGCHLD信号"></a>5.9 处理SIGCHLD信号</h1><pre><code>设置僵死状态的子进程是为了维护子进程的信息，以便父进程随时获取。这些信息包括子进程的进程ID，终止状态以及资源利用信息（CPU，内存使用等等），如果父进程终止，那么它所有的僵死子进程的父进程ID会被重置为1（init进程）。继承这些子进程并清理它们（init进程会wait它们，从而去除它们的僵死状态）</code></pre><h2 id="处理僵死进程"><a href="#处理僵死进程" class="headerlink" title="处理僵死进程"></a>处理僵死进程</h2><pre><code>僵死进程会占用内核空间，会导致资源耗尽。无论何时fork子进程都需要wait它们以等待资源的释放，防止其变为僵死进程。要达到此目的，我们需要捕获SIGCHLD信号的信号处理函数。在函数中我们调用wait。</code></pre><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"unp.h"</span></span></span><br><span class="line">Signal(SIGCHLD, sig_chld);</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">sig_chld</span><span class="params">(<span class="keyword">int</span> signo)</span></span>&#123;</span><br><span class="line">    <span class="keyword">pid_t</span> pid;</span><br><span class="line">    <span class="keyword">int</span> stat;</span><br><span class="line">    pid = wait(&amp;stat);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"child %d terminated\n"</span>, pid);</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在信号处理函数中不建议使用printf。</p><ol><li>我们键入EOF来终止客户，客户TCP发送一个FIN给服务器，服务器响应一个ACK</li><li>收到客户的FIN导致服务器TCP递送一个EOF给子进程阻塞中的readline，从而子进程终止</li><li>当SIGCHLD信号递交是，父进程阻塞于accept调用。sig_chld函数执行，其wait调用取到子进程的PID和终止状态，随后是printf调用并返回</li><li>既然信号是在父进程阻塞于慢系统调用（accept）时由父进程捕获的，内核就会使accept返回一个EINTR错误（被中断的系统调用），而父进程不处理该错误，于是中止</li></ol><h5 id="在信号处理函数中加入return语句虽然对于void返回函数来说并没有用处，但可以帮助我们来确定是那个函数来进行的处理（方便调试）"><a href="#在信号处理函数中加入return语句虽然对于void返回函数来说并没有用处，但可以帮助我们来确定是那个函数来进行的处理（方便调试）" class="headerlink" title="在信号处理函数中加入return语句虽然对于void返回函数来说并没有用处，但可以帮助我们来确定是那个函数来进行的处理（方便调试）"></a>在信号处理函数中加入return语句虽然对于void返回函数来说并没有用处，但可以帮助我们来确定是那个函数来进行的处理（方便调试）</h5><h3 id="处理被中断的系统调用"><a href="#处理被中断的系统调用" class="headerlink" title="处理被中断的系统调用"></a>处理被中断的系统调用</h3><p>我们用术语慢系统调用来描述accept函数，该属于也适用于那些可能永远阻塞的系统调用。永远阻塞的系统调用是指调用可能永远无法返回，多数网络支持函数都属于这一类，如果没有客户连接到服务器上，那么服务器的accept调用就没有返回的保证。</p><h5 id="适用于慢系统调用的基本规则是：当阻塞于某个慢系统调用的一个进程捕获某个信号且响应信号处理函数返回时，系统调用可能返回一个EINTR错误，有些内核自动重启被中断的系统调用。不过为了便于移植，当我们编写捕获信号时，我们必须对慢系统调用返回EINTR有所准备，例子如下："><a href="#适用于慢系统调用的基本规则是：当阻塞于某个慢系统调用的一个进程捕获某个信号且响应信号处理函数返回时，系统调用可能返回一个EINTR错误，有些内核自动重启被中断的系统调用。不过为了便于移植，当我们编写捕获信号时，我们必须对慢系统调用返回EINTR有所准备，例子如下：" class="headerlink" title="适用于慢系统调用的基本规则是：当阻塞于某个慢系统调用的一个进程捕获某个信号且响应信号处理函数返回时，系统调用可能返回一个EINTR错误，有些内核自动重启被中断的系统调用。不过为了便于移植，当我们编写捕获信号时，我们必须对慢系统调用返回EINTR有所准备，例子如下："></a>适用于慢系统调用的基本规则是：当阻塞于某个慢系统调用的一个进程捕获某个信号且响应信号处理函数返回时，系统调用可能返回一个EINTR错误，有些内核自动重启被中断的系统调用。不过为了便于移植，当我们编写捕获信号时，我们必须对慢系统调用返回EINTR有所准备，例子如下：</h5><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span>( ; ; )&#123;</span><br><span class="line">    client = <span class="keyword">sizeof</span>(cliaddr);</span><br><span class="line">    <span class="keyword">if</span>( (connfd = accept(listenfd, (SA*) &amp;cliaddr, &amp;clilen)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span>(errno == EINTR)</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            err_sys(<span class="string">"accept error"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段代码自己重启被中断的系统调用。对于accept以及read，write。select,open之类的函数来说合适，有一个函数我们不能重启：connect。如果该函数返回EINTR，不能再次调用它，会报错。当connect捕获一个信号并且不自动重启时，我们必须调用select来等待连接完成</p><h1 id="5-10-wait和waitpid函数"><a href="#5-10-wait和waitpid函数" class="headerlink" title="5.10 wait和waitpid函数"></a>5.10 wait和waitpid函数</h1><p>使用这两个函数来处理已终止的子进程</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/wait.h&gt;</span></span></span><br><span class="line"><span class="keyword">pid_t</span> wait(<span class="keyword">int</span> *statloc);</span><br><span class="line"><span class="keyword">pid_t</span> waitpid(<span class="keyword">pid_t</span> pid, <span class="keyword">int</span>* statloc, <span class="keyword">int</span> options);</span><br><span class="line"><span class="comment">//均返回：成功返回1，出错为0或-1</span></span><br></pre></td></tr></table></figure><p>函数wait和waitpid均返回两个值：已终止子进程的进程ID号，以及通过statloc指针返回的子进程终止状态（一个整数）。<br>如果调用wait的进程没有已终止的子进程，不过有一个或多个子进程仍在执行，那么wait将阻塞到现有子进程第一个终止出现为止<br>waitpid函数就等待那个进程以及是否阻塞给了更多控制方式。pid 允许我们指定想等待的进程 ID，-1 代表第一个终止的子进程。options 可以添加选项，常用的有 WNOHANG 表示内核在没有终止子进程时不阻塞。</p><h4 id="函数wait和waitpid的区别"><a href="#函数wait和waitpid的区别" class="headerlink" title="函数wait和waitpid的区别"></a>函数wait和waitpid的区别</h4><p>wait函数可能在多个信号同时接收时只会执行一次，这个时候在信号处理函数中使用一个循环来调用waitpid来将所有僵死进程释放</p><h4 id="重点速记"><a href="#重点速记" class="headerlink" title="重点速记"></a>重点速记</h4><ol><li>当fork子进程时必须捕获SIGCHLD信号</li><li>当捕获信号时，必须处理被中断的系统调用</li><li>SIGCHLD的信号处理函数必须正确编写，应使用waitpid函数，以免留下僵死进程</li></ol><p>服务器终极版本：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"unp.h"</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span>** argv)</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> listenfd, connfd;</span><br><span class="line">    <span class="keyword">pid_t</span> childpid;</span><br><span class="line">    <span class="keyword">socklen_t</span> client;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">cliaddr</span>, <span class="title">servaddr</span>;</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">sig_chld</span><span class="params">(<span class="keyword">int</span>)</span></span>;</span><br><span class="line">    listenfd = Socket(AF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    bzero(&amp;servaddr, <span class="keyword">sizeof</span>(servaddr));</span><br><span class="line">    servaddr.sin_family = AF_INET;</span><br><span class="line">    servaddr.sin_addr.s_addr = htonl(INADDR_ANY);</span><br><span class="line">    servaddr.sin_port =htons(SERV_PORT);</span><br><span class="line">    Bind(listenfd, (SA*) &amp;servaddr, <span class="keyword">sizeof</span>(servaddr));</span><br><span class="line">    Listen(listenfd, LISTENQ);</span><br><span class="line">    Signal(SIGCHLD, sig_chld);</span><br><span class="line">    <span class="keyword">for</span>(; ; )&#123;</span><br><span class="line">        clilen = <span class="keyword">sizeof</span>(cliaddr);</span><br><span class="line">        <span class="keyword">if</span>( (connfd = accept(listenfd, (SA*) &amp;cliaddr, &amp;clilen)) &lt; <span class="number">0</span>)&#123;</span><br><span class="line">            <span class="keyword">if</span>(errno == EINTR)</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                err_sys(<span class="string">"accept error"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>( (childpid = Fork()) == <span class="number">0</span>)&#123;</span><br><span class="line">            Close(listenfd);</span><br><span class="line">            str_echo(connfd);</span><br><span class="line">            <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        Close(connfd);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="5-11-accept-返回前连接终止"><a href="#5-11-accept-返回前连接终止" class="headerlink" title="5.11 accept 返回前连接终止"></a>5.11 accept 返回前连接终止</h1><p>当三次握手完毕，服务器准备调用 accept 时，客户端发送了 RST<br>不同的系统处理方式不同，有的不做回应，有的会返回特定错误</p><h1 id="5-12-服务器进程终止"><a href="#5-12-服务器进程终止" class="headerlink" title="5.12 服务器进程终止"></a>5.12 服务器进程终止</h1><p>服务器进程崩溃处理</p><ol><li>当服务器子进程终止时，子进程中所有打开的描述符都会被关闭。这导致向客户发送一个FIN，而客户TCP则响应一个ACK。完成TCP断开连接的前半部分。</li><li>SIGCHLD信号被发送给服务器父进程，并正确处理</li><li>这时客户还阻塞在获取输入的状态，此时输入一行字符，这时客户会尝试向服务器发送数据，但是服务器已经关闭会响应一个RST，但是客户端看不到，因为它还在等待信息，所以阻塞在了readline，并且由于之前接收了FIN，所调用的readline立即返回0，我们的客户此时并未收到EOF，所以报错<h4 id="本节重点：当FIN到达套接字时，客户阻塞在fgets上，客户实际在应对两个描述符，（套接字和用户输入），它不能单纯阻塞在这两个源中某个特定源的输入上。而是应该阻塞在其中任何一个源的输入上。这正是poll和select的目的之一，后面的处理方式是：一旦杀死服务器子程序，客户会立即被告知已收到FIN"><a href="#本节重点：当FIN到达套接字时，客户阻塞在fgets上，客户实际在应对两个描述符，（套接字和用户输入），它不能单纯阻塞在这两个源中某个特定源的输入上。而是应该阻塞在其中任何一个源的输入上。这正是poll和select的目的之一，后面的处理方式是：一旦杀死服务器子程序，客户会立即被告知已收到FIN" class="headerlink" title="本节重点：当FIN到达套接字时，客户阻塞在fgets上，客户实际在应对两个描述符，（套接字和用户输入），它不能单纯阻塞在这两个源中某个特定源的输入上。而是应该阻塞在其中任何一个源的输入上。这正是poll和select的目的之一，后面的处理方式是：一旦杀死服务器子程序，客户会立即被告知已收到FIN"></a>本节重点：当FIN到达套接字时，客户阻塞在fgets上，客户实际在应对两个描述符，（套接字和用户输入），它不能单纯阻塞在这两个源中某个特定源的输入上。而是应该阻塞在其中任何一个源的输入上。这正是poll和select的目的之一，后面的处理方式是：一旦杀死服务器子程序，客户会立即被告知已收到FIN</h4></li></ol><h1 id="5-13-SIGPIPE信号"><a href="#5-13-SIGPIPE信号" class="headerlink" title="5.13 SIGPIPE信号"></a>5.13 SIGPIPE信号</h1><p>当一个进程向某个已收到RST的套接字执行写操作时，内核向该进程发送一个SIGPIPE信号，默认行为是终止进程，所以必须要捕获它，防止进程被意外终止，忽略即可</p><h1 id="5-14-服务器主机崩溃"><a href="#5-14-服务器主机崩溃" class="headerlink" title="5.14 服务器主机崩溃"></a>5.14 服务器主机崩溃</h1><ol><li>服务器崩溃，此时客户写入一行文本，并且由writen写入内核，再由TCP客户作为一个数据分节送出，客户随后阻塞于readline调用，等待回射应答</li><li>这时客户会持续重传文本，试图从服务器接受一个ACK。当客户TCP最终放弃重传的时候（这段时间里，服务器没有重启成功），给客户进程返回一个错误。则调用readline返回一个错误，如果是服务器崩溃不可达，返回ETIMEOUT，如果是中间路由器表示不可达则响应一个“destination unreachable”的ICMP消息，这时返回的错误为EHOSTUNREACH或ENETUNREACH。</li></ol><p>使用 keepalive 选项来检测之际是否连通</p><p>如果觉得客户重传时间过久，可以设置一个超时即可</p><h1 id="5-15-服务器主机崩溃后重启"><a href="#5-15-服务器主机崩溃后重启" class="headerlink" title="5.15 服务器主机崩溃后重启"></a>5.15 服务器主机崩溃后重启</h1><p>这里的关键之处在于如果客户端不知道服务器崩溃了，在服务器重启后，服务器会丢失所有连接信息，此时客户发送TCP，服务器会响应一个RST，这时阻塞于readline的客户会返回ECONNRESET错误。<br>如果客户需要实时监测服务器是否崩溃，则要采用一些技术（SO_KEEPALIVE套接字选项或/某些客户/服务器心博函数。</p><h1 id="5-16-服务器主机关机"><a href="#5-16-服务器主机关机" class="headerlink" title="5.16 服务器主机关机"></a>5.16 服务器主机关机</h1><p>unix系统关机时，init进程会给所有进程发送SIGTERM信号，然后等待一段时间，然后给所有仍在运行的进程发送SIGKILL信号（该信号不可捕获），这么做是为了将一小段时间给进程做清除和终止，它所有打开的描述符都需要关闭释放，我们必须在客户中使用select或poll函数，使得服务器一终止，客户端可以检查到，否则会阻塞在 fgets 上。。。</p><h1 id="5-18-数据格式"><a href="#5-18-数据格式" class="headerlink" title="5.18 数据格式"></a>5.18 数据格式</h1><p>书上给了两个例子</p><ol><li>在客户与服务器之间传递文本串，其中包含空格分开的两个数字，服务器返回二者之和</li><li>在客户与服务器之间传递二进制结构<br>但是存在问题<ol><li>不同机器存在大端小端问题</li><li>不同实现存储的C数据类型上可能存在差异，它们各自的大小不确定相同</li><li>不同实现打包方式存在差异  </li></ol></li></ol><p>解决办法</p><ul><li>把数值数据作为文本串来传递，确保机器上的字符集相同即可</li><li>显示定义所支持数据类型的二进制格式（位数，大小端字节序）RPC 通常使用这种方式</li></ul><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>本章解决了几个问题 point</p><ol><li>僵死子进程问题，通过捕获 SIGCHLD 处理，</li><li>处理信号的函数推荐使用 waitpid 函数进行处理</li><li>防止服务器崩溃，客户端不知道的情况</li><li>在网络中传输数值数据会产生一些问题</li><li>等等。。。</li></ol>]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;5-1-概述&quot;&gt;&lt;a href=&quot;#5-1-概述&quot; class=&quot;headerlink&quot; title=&quot;5.1 概述&quot;&gt;&lt;/a&gt;5.1 概述&lt;/h1&gt;&lt;p&gt;&amp;emsp;&amp;emsp;本章使用上一章的函数来编写一个完整的 TCP 客户端服务器端的程序示例（回射服务器）&lt;/p&gt;
    
    </summary>
    
    
      <category term="UNP" scheme="http://yoursite.com/categories/UNP/"/>
    
    
  </entry>
  
  <entry>
    <title>第六章：IO复用</title>
    <link href="http://yoursite.com/2019/09/25/06.IO%E5%A4%8D%E7%94%A8%EF%BC%9Aselect%E5%92%8Cpoll%E5%87%BD%E6%95%B0/"/>
    <id>http://yoursite.com/2019/09/25/06.IO复用：select和poll函数/</id>
    <published>2019-09-25T14:54:48.819Z</published>
    <updated>2019-09-28T08:46:38.256Z</updated>
    
    <content type="html"><![CDATA[<h1 id="6-1-概述"><a href="#6-1-概述" class="headerlink" title="6.1 概述"></a>6.1 概述</h1><p>&emsp;&emsp;上一章的问题在于TCP客户端同时处理两个套接字，标准输入和TCP套接字。在客户阻塞于fgets调用期间，服务器进程会被杀死。服务器TCP虽然正确的给客户TCP发送了一个FIN，但是客户进程既然阻塞于标准输入读入的过程，他将看不到这个EOF。这样的进程需要一种预先告知内核的能力，使得内核一旦发现进程指定的一个或多个I/O条件接续，就通知进程，这被称为IO复用</p><a id="more"></a><p>IO服用的典型使用场合</p><ol><li>当客户处理多个描述符（通常是交互式输入和网络套接字），必须使用IO复用。这是我们上一章强调的</li><li>一个客户同时处理多个套接字是可能的，但较为少见。可以使用select等方式来进行处理</li><li>如果一个TCP服务器既要处理监听套接字，又要处理已连接套接字。一般要使用IO复用</li><li>如果一个服务器既要处理TCP又要处理UDP，一般就要使用IO复用</li><li>如果一个服务器要处理多个服务或者多个协议，一般要使用IO复用</li></ol><p>I/O 复用并非只限于网络编程，许多应用程序也使用这项技术（eg：redis 的事件处理）</p><h1 id="6-2-I-O模型"><a href="#6-2-I-O模型" class="headerlink" title="6.2 I/O模型"></a>6.2 I/O模型</h1><p>Unix下可用的I/O模型有五种</p><ol><li>阻塞式 I/O；</li><li>非阻塞式 I/O；</li><li>I/O 复用（select 和 poll）；</li><li>信号驱动式 I/O（SIGIO）；</li><li>异步 I/O（POSIX 的 aio_ 系列函数）；</li></ol><p>一个输入操作通常包括两个不同的阶段：</p><ol><li>等待数据准备好</li><li>从内核向进程复制数据</li></ol><p>对于套接字来说：  </p><ol><li>通常是等待数据从网络中到达，当到达后，被复制到内核的缓冲区。  </li><li>是把数据从内核缓冲区复制到应用进程缓冲区。</li></ol><h4 id="阻塞式I-O模型"><a href="#阻塞式I-O模型" class="headerlink" title="阻塞式I/O模型"></a>阻塞式I/O模型</h4><pre><code>在阻塞式IO模型中，所有套接字都是阻塞的（在内核空间中等待数据），在该函数开始调用以及返回的时间内。</code></pre><h4 id="非阻塞式IO模型"><a href="#非阻塞式IO模型" class="headerlink" title="非阻塞式IO模型"></a>非阻塞式IO模型</h4><pre><code>    进程把一个套接字设置成非阻塞是在通知内核：当请求的IO操作非要把本进程投入睡眠中才可完成时，不要把进程投入睡眠，而是返回一个错误  当进程循环进行确认时，称之为轮询，这么做十分耗费 CPU</code></pre><h4 id="IO复用模型"><a href="#IO复用模型" class="headerlink" title="IO复用模型"></a>IO复用模型</h4><pre><code>    有IO复用了，我们就可以调用 select 或 poll，阻塞在这两个系统调用的某一个上，而不是阻塞在真正的IO系统调用上    使用了IO复用，阻塞于 select 的调用，等待数据报套接字变为可读。当 select 返回套接字可读这一条件时，我们调用 recvfrom 把所有数据报复制到应用进程缓冲区。对于单个的描述符来说，Io复用甚至效果还不如阻塞IO，但它在多描述符的等待上会更有优势。  与 I/O 复用类似的是使用多线程来进行阻塞式 I/O</code></pre><h4 id="信号驱动式IO模型"><a href="#信号驱动式IO模型" class="headerlink" title="信号驱动式IO模型"></a>信号驱动式IO模型</h4><pre><code>我们可以使用信号，让内核在描述符就绪时发送SIGIO信号通知我们。这样我们就不会阻塞在数据的等待是，只会阻塞在复制数据的过程上。</code></pre><h4 id="异步IO模型"><a href="#异步IO模型" class="headerlink" title="异步IO模型"></a>异步IO模型</h4><pre><code>告知内核某个操作，，并让内核在整个操作完成后进行通知即可，信号驱动式IO告诉我们可以进行一个IO操作，而异步IO告诉我们IO操作已经结束</code></pre><h4 id="各种IO模型比较"><a href="#各种IO模型比较" class="headerlink" title="各种IO模型比较"></a>各种IO模型比较</h4><p>前四种模型主要区别在第一阶段，第二阶段均相同：在数据从内核复制到调用者的缓冲区期间，进程阻塞于recvfrom调用，相反，异步IO模型在这两个阶段都要处理，从而不同于其余的模型</p><h4 id="同步IO和异步IO的对比"><a href="#同步IO和异步IO的对比" class="headerlink" title="同步IO和异步IO的对比"></a>同步IO和异步IO的对比</h4><p>同步IO导致请求进程阻塞，直到IO操作完成<br>异步IO不导致请求阻塞</p><h4 id="同步与异步，阻塞与非阻塞"><a href="#同步与异步，阻塞与非阻塞" class="headerlink" title="同步与异步，阻塞与非阻塞"></a>同步与异步，阻塞与非阻塞</h4><p>同步与异步：同步与异步是针对应用程序与内核的交互而言的。同步过程中进程触发IO操作并等待或者轮询的去查看IO操作是否完成。异步过程中进程触发IO操作以后，直接返回，做自己的事情，IO交给内核来处理，完成后内核通知进程IO完成，这里是针对第二个阶段的区分</p><p>阻塞与非阻塞：应用进程请求I/O操作时，如果数据未准备好，如果请求立即返回就是非阻塞，不立即返回就是阻塞。简单说就是做一件事如果不能立即获得返回，需要等待，就是阻塞，否则就可以理解为非阻塞。这里主要为第一个阶段的区分</p><p>虽然吧同步异步，阻塞非阻塞放在一起，但二者无必然联系</p><h1 id="6-3-select函数"><a href="#6-3-select函数" class="headerlink" title="6.3 select函数"></a>6.3 select函数</h1><p>该函数允许进程指示内核等待多个事件中的任何一个发生，并只有在一个或多个事件发生或经历一段指定的时间后才能唤醒它。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/select.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/time.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">select</span><span class="params">(<span class="keyword">int</span> maxfdpl, fd_set* readset, fd_set* writeset, fd_set* exceptset, <span class="keyword">const</span> struct                  timeval* timeout)</span></span>;</span><br><span class="line"><span class="comment">//返回:若有就绪描述符则返回其数目，若超时则为0，若出错则为-1</span></span><br></pre></td></tr></table></figure><p>参数解析：</p><ul><li><p>timeout：告知内核等待所指定描述符中的任何一个就绪可花多长时间，其timeval结构如下</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">struct timeval&#123;</span><br><span class="line">    long tv_sec;    //秒数</span><br><span class="line">    long tv_usec;   //微秒数</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p>timeout 参数有以下用法：</p><ol><li>永远等待下去：仅在有一个描述符转备好IO时才返回，此时应置位空指针</li><li>等待一个固定的时间：在有一个描述符准备好IO时返回，但是不能超过该参数所指向的timeval结构中指定的秒数和微秒数</li><li>根本不等待：检查描述符之后立即返回，称为轮询（polling），该指针需要指向一个值为0的结构</li></ol></li><li><p>中间的三个参数告诉内核我们指定在该描述符上的操作，读，写，异常条件的判断<br>  目前的异常条件只有两个：</p><ol><li>某个套接字的带外数据的到达</li><li>某个已置位分组模式的伪终端存在可从其主端读取的控制状态信息  </li></ol></li><li><p>要想给这三个参数指定描述符集，select使用的是一个整数数组，其中每一位对应一个描述符。</p></li></ul><p>*使用select时一定要注意对最大描述符加一</p><h4 id="描述符就绪条件-UNP-p130"><a href="#描述符就绪条件-UNP-p130" class="headerlink" title="描述符就绪条件(UNP.p130)"></a>描述符就绪条件(UNP.p130)</h4><p>满足以下四个条件之一，一个套接字准备好读</p><ol><li>该套接字接收缓冲区中的数据字节数大于等于套接字接收缓冲区低水位标记的当前大小。对这样的套接字执行读操作不会阻塞并返回一个大于0的值（也就是返回准备好读入的数据）。我们可以使用SO_RCLOWAT套接字选项设置套接字的低水位标记。对于TCP，UDP来说，默认为1.</li><li>该链接的读半部关闭（也就是接受了FIN的TCP连接）。对于这样的操作读操作不会阻塞并返回0（EOF）。</li><li>该套接字是一个监听套接字，并且已完成的连接数不为0.对于这样的套接字accept通常不会阻塞</li><li>其上有一个套接字错误待处理，对于这样的套接字的读操作不阻塞并返回-1（返回一个错误），同时设置error为确切的错误操作</li></ol><p>满足以下四个条件之一，一个套接字准备好写</p><ol><li>该套接字发送缓冲区中的可用空间大于等于套接字发送缓冲区低水位标记的当前大小，并且或者该套接字已经连接，或者该套接字不需要连接。如果我们把这样的套接字设置为非阻塞的，写操作不会阻塞一个正值。我们可以使用SO_SNDLOWAT套接字选项设置套接字的低水位标记。对于TCP，UDP来说，默认为2048.</li><li>该连接的写半部关闭。对这样的套接字的写操作将产生SIGPIPE信号。</li><li>使用非阻塞式connect的套接字以建立连接，或者connect已经以失败告终</li><li>其上有一个套接字错误待处理，对于这样的套接字的写操作不阻塞并返回-1（返回一个错误），同时设置error为确切的错误操作</li></ol><p>如果一个套接字存在外带数据或者仍处于带外标记，那么它有异常条件待处理</p><ul><li>注意：如果某个套接字上发生错误时，它将由select标记为既可读又可写</li></ul><p>总结表：<br>条件 | 可读吗？ | 可写吗？ | 异常吗？<br>-|-|-|-<br>有数据可读 | √ | |<br>关闭连接的读一半 | √ | |<br>给监听套接口转备好新链接 | √ | |<br>有可用于写的空间 | | √ | |<br>关闭连接的写一半 | | √ | |<br>待处理错误 | √ | √| |<br>TCP带外数据 | | | √ |</p><h4 id="select的最大描述符"><a href="#select的最大描述符" class="headerlink" title="select的最大描述符"></a>select的最大描述符</h4><p>最开始select设置为进程中最大支持描述符个数。但随着系统的版本的迭代，单进程所支持的描述符个数已经大大增加，但是select并不能随意增加。</p><h1 id="6-4-str-cli函数"><a href="#6-4-str-cli函数" class="headerlink" title="6.4 str_cli函数"></a>6.4 str_cli函数</h1><p>使用select来重新写str_cli函数，这样服务一旦终止，客户就可以马上得到通知</p><p>客户的套接字上三个条件处理如下</p><ol><li>如果对端TCP发送数据，那么该套接字变为可读，并且read返回一个大于0的值（即读入数据的字节数）</li><li>如果对端TCP发送一个FIN（对端进程终止），那么该套接字变为可读，并且read返回0（EOF）</li><li>如果对端TCP发送一个RST（对端主机崩溃并重现启动）那么该套接字变为可读，并且read返回-1，而errno中含有确切的错误代码</li></ol><p>使用select来重写该函数</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"unp.h"</span></span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">str_cli</span><span class="params">(FILE* fp, <span class="keyword">int</span> sockfd)</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> maxfdp1;</span><br><span class="line">    fd_set rset;</span><br><span class="line">    <span class="keyword">char</span> sendline[MAXLINE], recvline[MAXLINE];</span><br><span class="line"></span><br><span class="line">    FD_ZERO(&amp;rset);</span><br><span class="line">    <span class="keyword">for</span>( ; ; )&#123;</span><br><span class="line">        FD_SET(fileno(fp), &amp;rest);  <span class="comment">//fileno函数把标准IO读写文件转换为对应的描述符</span></span><br><span class="line">        FD_SET(sockfd, &amp;rest);</span><br><span class="line">        maxfdp1 = max(fileno(fp), sockfd) + <span class="number">1</span>;</span><br><span class="line">        Select(maxfdp1, &amp;rest, <span class="literal">NULL</span>, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span>(FD_ISSET(sockfd, &amp;rset)) &#123;</span><br><span class="line">            <span class="keyword">if</span>(Readline(sockfd, recvline, MAXLINE) == <span class="number">0</span>)</span><br><span class="line">                err_quit(<span class="string">"str_cli :..."</span>);</span><br><span class="line">            Fputs(recvline, <span class="built_in">stdout</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(FD_ISSET(fileno(fp), &amp;rset)) &#123;</span><br><span class="line">            <span class="keyword">if</span>(Fgets(sendline, MAXLINE, fp) == <span class="literal">NULL</span>)</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            Writen(sockfd, sendline, <span class="built_in">strlen</span>(sendline));</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="6-5-批量输入"><a href="#6-5-批量输入" class="headerlink" title="6.5 批量输入"></a>6.5 批量输入</h1><p>我们最初的版本中，以停-等的方式工作，这对交互式使用来说是合适的：发送一行文本给服务器，然后等待应答。这段时间是往返时间，加服务器处理时间。<br>本节不完整，查看UNP6.5节。</p><h1 id="6-6-shutdown函数"><a href="#6-6-shutdown函数" class="headerlink" title="6.6 shutdown函数"></a>6.6 shutdown函数</h1><p>终止网络连接的方式一把使用close函数，但close函数有两个限制，可以使用shutdown避免</p><ol><li>close把描述符的引用计数减一，仅在该计数变为0时关闭套接字。使用shutdown可以不管引用计数就激发TCP的正常连接中止序列。</li><li>close终止读和写两个方向的数据传送。既然TCP连接是全双工的，有时我们需要告诉对端我们已经完成了数据发送，即使对端仍有数据要发送给我们。这就是前一节遇到的str_cli函数在批量输入时的情况<br>调用shutdown关闭一半的连接<figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">shutdown</span><span class="params">(<span class="keyword">int</span> sockfd, <span class="keyword">int</span> howto)</span></span>;</span><br></pre></td></tr></table></figure></li></ol><p>该函数的行为依赖于howto参数的值  </p><ul><li>SHUT_RD(0)： 关闭连接读这一半–套接字中不再有数据可接收，而且套接字接收缓冲区中的现有数据都被丢弃。进程不在对这样的套接字调用任何函数。对一个TCP套接字这样调用shutdown函数后，由该套接字接受的来自对端的任何数据都会被确认，然后悄然丢弃</li><li>SHUT_WR(1)： 关闭连接的写部分–对于TCP套接字，这叫半关闭。当前留在套接字发送缓冲区的数据将会被发送，之后进程不能再对改套接字调用任何写函数</li><li>SHUE_RDWR(2)： 连接的读半部和写半部均关闭–这与调用shutdown两次等效：第一次调用指定SHUT_RD，第二次调用指定SHUT_WR</li></ul><h1 id="6-7-str-cli函数再修改"><a href="#6-7-str-cli函数再修改" class="headerlink" title="6.7 str_cli函数再修改"></a>6.7 str_cli函数再修改</h1><p>这次加入了 select 和 shutdown 函数，前者允许只要服务器关闭就会通知我们，后者允许我们正确的处理批量输入，该版本还废弃了以文本行为中心的代码，从而针对缓冲区操作</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"unp.h"</span></span></span><br><span class="line"><span class="keyword">void</span></span><br><span class="line">str_cli(FILE* fp, <span class="keyword">int</span> sockfd)&#123;</span><br><span class="line">    <span class="keyword">int</span> maxfdp1, stdineof;</span><br><span class="line">    fd_set rset;</span><br><span class="line">    <span class="keyword">char</span> buf[MAXLINE];</span><br><span class="line">    <span class="keyword">int</span> n;</span><br><span class="line"></span><br><span class="line">    stdineof = <span class="number">0</span>;</span><br><span class="line">    FD_ZERO(&amp;rset);   <span class="comment">// 初始化监听描述符集合</span></span><br><span class="line">    <span class="keyword">for</span>(; ; )&#123;</span><br><span class="line">        <span class="keyword">if</span>(stdineof == <span class="number">0</span>)&#123;</span><br><span class="line">            FD_SET(fileno(fp), &amp;rset);</span><br><span class="line">        &#125;</span><br><span class="line">        FD_SET(sockfd, &amp;rset);</span><br><span class="line">        maxfdp1 = max(fileno(fp), sockrd) + ;</span><br><span class="line">        <span class="comment">// 每次循环都要重新调用 SELECT 来进行判断</span></span><br><span class="line">        Select(maxfdp1, &amp;rset, <span class="literal">NULL</span>, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span><br><span class="line">        <span class="keyword">if</span>(FD_ISSET(sockfd, &amp;rset))&#123;   <span class="comment">// socket 可读</span></span><br><span class="line">            <span class="keyword">if</span>( (n = Read(sockfd, buf, MAXLINE)) == <span class="number">0</span>)&#123;</span><br><span class="line">                <span class="keyword">if</span>(stdineof == <span class="number">1</span>)</span><br><span class="line">                    <span class="keyword">return</span>;</span><br><span class="line">                <span class="keyword">else</span></span><br><span class="line">                    err_quit(<span class="string">""</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            Write(fileno(<span class="built_in">stdout</span>), buf, n);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(FD_ISSET(fileno(fp), &amp;rset))&#123;   <span class="comment">// input 可读</span></span><br><span class="line">            <span class="keyword">if</span>( n = (Read(fileno(fp), buf, MAXLINE)) == <span class="number">0</span>)&#123;</span><br><span class="line">                stdineof = <span class="number">1</span>;</span><br><span class="line">                Shutdown(sockfd, SHUT_WR);</span><br><span class="line">                FD_CLR(fileno(fp), &amp;rset);</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            Writen(sockfd, buf, n);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="6-8-TCP-回射服务器程序（修订）"><a href="#6-8-TCP-回射服务器程序（修订）" class="headerlink" title="6.8 TCP 回射服务器程序（修订）"></a>6.8 TCP 回射服务器程序（修订）</h1><p>代码见书，这里不做 copy  </p><p><strong>拒绝服务型攻击</strong><br>当一个服务器处理多个客户时，绝对不能阻塞于单个客户的相关函数调用。否则可能导致服务器挂起，拒绝为所有客户端服务。</p><ul><li>解决方法<ul><li>使用非阻塞式 I/O</li><li>让每个客户由单独线程提供服务</li><li>对 I/O 操作设置一个定时</li></ul></li></ul><h1 id="6-9-pselect-函数"><a href="#6-9-pselect-函数" class="headerlink" title="6.9 pselect 函数"></a>6.9 pselect 函数</h1><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;sys/select.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;time.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pselect</span><span class="params">(<span class="keyword">int</span> macfdp1, fd_set *readset, fd_set *writeset, fd_set* exceptset,</span></span></span><br><span class="line"><span class="function"><span class="params">            <span class="keyword">const</span> struct timespec * timeout, <span class="keyword">const</span> <span class="keyword">sigset_t</span>* sigmask)</span></span></span><br><span class="line"><span class="function"><span class="comment">// 返回：若有已经就绪的描述符，返回数目，超时返回 0，出错返回 -1</span></span></span><br></pre></td></tr></table></figure><p>相比 select 的变化：</p><ul><li>pselect 使用 timespec 结构，而不是 timeval 结构。仅仅是第二个时间值从微秒转换为纳秒。</li><li>pselect 增加了一个参数，指向信号掩码的指针。</li></ul><h1 id="6-10-poll-函数"><a href="#6-10-poll-函数" class="headerlink" title="6.10 poll 函数"></a>6.10 poll 函数</h1><p>poll 函数提供的功能与 select 类似。但在处理流设备时，能够提供额外的信息</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;poll.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">poll</span><span class="params">(struct pollfd* fdarray, <span class="keyword">unsigned</span> <span class="keyword">long</span> nfds, <span class="keyword">int</span> timeout)</span></span></span><br><span class="line"><span class="function"><span class="comment">// 返回：若有已经就绪的描述符，返回数目，超时返回 0，出错返回 -1</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">struct pollfd</span>&#123;</span><br><span class="line">    <span class="keyword">int</span> fd;</span><br><span class="line">    <span class="keyword">short</span> events;</span><br><span class="line">    <span class="keyword">short</span> revents;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>fdarray 时指向 pollfd 结构体数组的指针，用来保存一系列的描述符，返回后判断相应 pollfd 结构体中的 revents 来确定是否有何种类型的输出即可。</p><h1 id="6-11-TCP-回射服务器程序（修改）"><a href="#6-11-TCP-回射服务器程序（修改）" class="headerlink" title="6.11 TCP 回射服务器程序（修改）"></a>6.11 TCP 回射服务器程序（修改）</h1><p>使用 poll 代替 select 来重写服务器程序<br>代码见书，这里不做 copy  </p><h1 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h1><p>unix 有五种不同的 I/O 模型：</p><ul><li>阻塞式 I/O 模型</li><li>非阻塞式 I/O 模型</li><li>I/O 复用模型</li><li>信号驱动式 I/O 模型</li><li>异步 I/O 模型</li></ul><p>默认是阻塞式 I/O 模型， I/O 复用模型常使用 select，但 epoll 是较好的选择</p>]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;6-1-概述&quot;&gt;&lt;a href=&quot;#6-1-概述&quot; class=&quot;headerlink&quot; title=&quot;6.1 概述&quot;&gt;&lt;/a&gt;6.1 概述&lt;/h1&gt;&lt;p&gt;&amp;emsp;&amp;emsp;上一章的问题在于TCP客户端同时处理两个套接字，标准输入和TCP套接字。在客户阻塞于fgets调用期间，服务器进程会被杀死。服务器TCP虽然正确的给客户TCP发送了一个FIN，但是客户进程既然阻塞于标准输入读入的过程，他将看不到这个EOF。这样的进程需要一种预先告知内核的能力，使得内核一旦发现进程指定的一个或多个I/O条件接续，就通知进程，这被称为IO复用&lt;/p&gt;
    
    </summary>
    
    
      <category term="UNP" scheme="http://yoursite.com/categories/UNP/"/>
    
    
  </entry>
  
</feed>
